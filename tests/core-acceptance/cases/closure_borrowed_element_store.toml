[[case]]
id = "closure_borrowed_element_store"
intent = "Vec element reads are borrows: the vec slot keeps ownership of the element's pair box and environment."
source = "../vertical-slice/reject/closure_borrowed_element_store.hew"
suites = ["acceptance"]
timeout_seconds = 30
kind = "reject"

[[case.expected.diagnostics]]
code = "E_OWN_CONSUME_BORROWED"
line = 15
column = 16
message = "E_OWN_CONSUME_BORROWED: cannot consume borrowed collection element `first`"
