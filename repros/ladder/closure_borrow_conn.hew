#[resource]
type Conn { fd: i64 }
impl Conn {
    fn close(consume self) { println(f"close {self.fd}"); }
}
fn main() {
    let c = Conn { fd: 4 };
    let f = move || { c.fd };
    println(f());
    println("after");
}
