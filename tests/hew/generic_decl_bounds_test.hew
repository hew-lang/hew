//! Declaration-level generic bounds are enforced without blocking valid uses.

import std::testing;

type Box<T: Display> { value: T; }

#[test]
fn test_decl_bound_satisfied_by_display_type() {
    let b = Box { value: 42 };
    testing.assert_eq(b.value, 42);
}
