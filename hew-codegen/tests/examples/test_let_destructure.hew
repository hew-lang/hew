fn make_pair() -> (int, int) {
    (10, 20)
}

fn main() {
    let pair = make_pair();
    let (a, b) = pair;
    println(a);
    println(b);
    let (x, y) = (100, 200);
    println(x);
    println(y);
}
