//! Hew `std::text::unicode` — character classification and UTF-8 helpers.
//!
//! All classification logic is implemented in pure Hew (`unicode.hew`),
//! backed by codepoint-level functions in `hew-runtime`. This crate exists
//! as a workspace placeholder; no additional FFI functions are defined here.
