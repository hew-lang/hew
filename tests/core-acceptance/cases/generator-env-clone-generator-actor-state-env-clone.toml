[[case]]
id = "generator-env-clone-generator-actor-state-env-clone"
intent = "Standalone memory-safety oracle for owned generator environment snapshots. Shape `generator_actor_state_env_clone` (generator actor state env clone has zero leaks) runs to its exact outcome with no leak, double free or use after free under ASan/LSan."
source = "cases/generator-env-clone-generator-actor-state-env-clone.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 60

[case.expected]
stdout = "32:OK"
exit = 0
