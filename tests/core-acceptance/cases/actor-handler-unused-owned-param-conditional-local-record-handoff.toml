[[case]]
id = "actor-handler-unused-owned-param-conditional-local-record-handoff"
intent = "Actor-handler owned-parameter disposition oracle. Shape `conditional_local_record_handoff` (conditional local record handoff releases exactly once on both edges) runs to its exact outcome with no leak, double free or use after free under ASan/LSan."
source = "cases/actor-handler-unused-owned-param-conditional-local-record-handoff.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 60

[case.expected]
stdout = ""
exit = 0
