[[case]]
id = "conditional-move-drop-vec-double-destination-not-taken"
intent = "Conditionally-moved collection locals — leak-slope oracles plus poisoned-allocator exactly-once pins (#2418). Shape `vec_double_destination_not_taken_high` (vec double destination not taken no per call leak slope) runs to its exact outcome with no leak, double free or use after free under ASan/LSan."
source = "cases/conditional-move-drop-vec-double-destination-not-taken.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 60

[case.expected]
stdout = ""
exit = 50
