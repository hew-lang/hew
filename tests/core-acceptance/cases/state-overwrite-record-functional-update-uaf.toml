[[case]]
id = "state-overwrite-record-functional-update-uaf"
intent = "State-field overwrite leak oracles for collection, record, and enum actor state fields, plus alias-shape use-after-free pins. Shape `record_functional_update_uaf` (record functional update keeps aliased label alive, record state overwrite no per frame leak slope) runs to its exact outcome with no leak, double free or use after free under ASan/LSan."
source = "cases/state-overwrite-record-functional-update-uaf.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 60

[case.expected]
stdout = ""
exit = 50
