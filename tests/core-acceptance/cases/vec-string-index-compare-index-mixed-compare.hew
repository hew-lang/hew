fn main() -> i64 {
    var xs: Vec<string> = Vec.new();
    xs.push("elem-0");
    xs.push("elem-1");
    xs.push("elem-2");
    xs.push("elem-3");
    xs.push("elem-4");
    xs.push("elem-5");
    xs.push("elem-6");
    xs.push("elem-7");
    var hits: i64 = 0;
    if xs[0] != "zzz" { hits = hits + 1; }
    if xs[1] < "zzz" { hits = hits + 1; }
    if xs[2] <= "zzz" { hits = hits + 1; }
    if xs[3] > "zzz" { hits = hits + 1; }
    if xs[4] >= "zzz" { hits = hits + 1; }
    if xs[5] == "zzz" { hits = hits + 1; }
    if xs[6] != "zzz" { hits = hits + 1; }
    if xs[7] < "zzz" { hits = hits + 1; }
    if hits < 0 { return 82; }
    0
}
