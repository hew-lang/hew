[[case]]
id = "ownership-balance-j-join-successor"
intent = "J: two string vars reassigned in a loop that also has a continue edge. The ownership checker balances every obligation, so the program checks with no diagnostics."
source = "fixtures/ownership-balance/j_join_successor.hew"
suites = ["acceptance"]
timeout_seconds = 30
kind = "check"
