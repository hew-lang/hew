type Pair<A, B> { a: A, b: B, }

fn build(seed: string) -> i64 {
    let p = Pair { a: seed + "-a", b: seed + "-b" };
    let q = clone p;
    q.a.len() + q.b.len() + p.a.len() + p.b.len()
}

fn main() -> i64 {
    var total: i64 = 0;
    for i in 0..64 {
        total = total + build("seed");
    }
    if total != 1536 { return 71; }
    0
}
