[[case]]
id = "wire-cbor-codec-wire-cbor-vec-owned-enum-decode-baseline"
intent = "Wire CBOR codec ownership leak oracles: decode-owned field drop and the decode-failure partial free-on-error path. Shape `wire_cbor_vec_owned_enum_decode_baseline_high` (vec owned enum decode failure frees partials no under free) runs to its exact outcome with no leak, double free or use after free under ASan/LSan."
source = "cases/wire-cbor-codec-wire-cbor-vec-owned-enum-decode-baseline.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 60

[case.expected]
stdout = ""
exit = 94
