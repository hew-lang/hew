fn main() {
    let k: Result<i64, string> = Result.Ok(6);
    let m: HashMap<Result<i64, string>, i64> = HashMap.new();
    m.insert(k, 9);
    match m.get(k) {
        Option.Some(v) => println(f"got {v}"),
        None => println("missing"),
    }
}
