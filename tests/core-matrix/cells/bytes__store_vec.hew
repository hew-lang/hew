fn main() {
    let x: bytes = bytes.new();
    x.push(65);
    x.push(66);
    let y: bytes = bytes.new();
    y.push(65);
    y.push(66);
    y.push(67);
    let v: Vec<bytes> = Vec.new();
    v.push(x);
    v.push(y);
    let n = v.len();
    println(f"len={n}");
}
