[[case]]
id = "ownership-balance-retained-call-carriers"
intent = "Ownership-balance fixture. The ownership checker balances every obligation, so the program checks with no diagnostics."
source = "fixtures/ownership-balance/retained_call_carriers/main.hew"
suites = ["acceptance"]
timeout_seconds = 30
kind = "check"
