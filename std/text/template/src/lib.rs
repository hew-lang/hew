//! Hew `std::text::template` — Go-style text/template rendering.
//!
//! All parsing and rendering logic is implemented in pure Hew
//! (`template.hew`). This crate exists as a placeholder for the workspace
//! build; no FFI functions are required.
