[[case]]
id = "regex_literal_dual_alias"
intent = "The fixture runs to completion and prints exactly its recorded output."
source = "../vertical-slice/accept/regex_literal_dual_alias.hew"
suites = ["acceptance"]
timeout_seconds = 30

[case.env]
HEW_WORKERS = "4"

[case.expected]
stdout = "123\n456\n"
exit = 0
