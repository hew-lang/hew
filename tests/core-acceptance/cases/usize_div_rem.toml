[[case]]
id = "usize_div_rem"
intent = "The fixture runs to completion and exits 0."
source = "../vertical-slice/accept/usize_div_rem.hew"
suites = ["acceptance"]
timeout_seconds = 30

[case.env]
HEW_WORKERS = "4"

[case.expected]
stdout = ""
exit = 0
