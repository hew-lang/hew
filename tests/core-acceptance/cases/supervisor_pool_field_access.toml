[[case]]
id = "supervisor_pool_field_access"
intent = "The fixture runs to completion and prints exactly its recorded output."
source = "../vertical-slice/accept/supervisor_pool_field_access.hew"
suites = ["acceptance"]
timeout_seconds = 30

[case.env]
HEW_WORKERS = "4"

[case.expected]
stdout = "37\n"
exit = 0
