type Rec { s: string }
fn g(r: Rec) -> i64 { r.s.len() }
fn main() -> i64 {
    var total: i64 = 0;
    for i in 0..50 {
        let r = Rec { s: "a" + "b" };
        total = total + g(r);
    }
    if total != 50 * 2 { return 81; }
    0
}
