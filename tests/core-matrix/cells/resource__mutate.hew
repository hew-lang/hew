#[resource]
type Tok {
    id: i64
}

impl Tok {
    fn close(self) {
        println(f"close {self.id}");
    }
}

fn main() {
    var x = Tok { id: 1 };
    x = Tok { id: 2 };
    let x_i = x.id;
    println(f"tok {x_i}");
}
