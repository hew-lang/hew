# Workspace .hew files that do not parse, so they are not formatter input.
# A path under a reject/ directory needs no row here (fmt_roundtrip_corpus.rs
# exempts it automatically): it is a deliberate parse-refusal fixture. This
# file holds only the remainder, migrated into tests/expected-failures.tsv
# once xtask's ratchet `Expect` enum gains a `skip`/`unparsed` kind (blocked
# on L1's ledger work); until then fmt_roundtrip_corpus.rs requires this list
# to match exactly: remove a file when it starts to parse, and fix rather
# than add a file that stops parsing.
hew-lsp/tests/fixtures/v05_async_await.hew
hew-parser/fuzz/corpus/fuzz_machine_decls/seed.hew
hew-parser/fuzz/corpus/fuzz_supervisor_decls/seed.hew
tests/core-acceptance/cases/machine-composite-depth-refused.hew
tests/core-acceptance/cases/named-argument-after-positional-gap.hew
tests/core-acceptance/cases/spread-parse-refusals.hew
