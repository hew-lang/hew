fn main() {
    let x: f64 = 3.5;
    let v: Vec<f64> = Vec.new();
    v.push(x);
    match v.get(0) {
        .Some(e) => {
            println(f"{e}");
        },
        .None => println("missing"),
    }
}
