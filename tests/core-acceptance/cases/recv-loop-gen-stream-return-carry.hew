actor Maker {
    receive gen fn items() -> string {
        yield "escaped";
        yield "rest";
    }
}
fn first(m: Maker) -> string {
    for v in m.items() {
        return v;
    }
    "none"
}
fn main() {
    println("before");
    let m = spawn Maker;
    let s = first(m);
    println(s);
    println("after");
}
