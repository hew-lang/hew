[[case]]
id = "error_prop_combined"
intent = "The fixture runs to completion and prints exactly its recorded output."
source = "../vertical-slice/accept/error_prop_combined.hew"
suites = ["acceptance"]
timeout_seconds = 30

[case.env]
HEW_WORKERS = "4"

[case.expected]
stdout = "config: localhost:8080\nparse-failed: port\nmissing-field: host\n"
exit = 0
