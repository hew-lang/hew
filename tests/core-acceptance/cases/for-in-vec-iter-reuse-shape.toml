[[case]]
id = "for-in-vec-iter-reuse-shape"
intent = "`for x in vec` cursor (`VecIter<T>`) scope-exit drop oracles: per-iteration leak slope plus a poisoned-allocator no-use-after-free pin for the place-source reuse shape (issue #1949). Shape `reuse_shape` (for in place source reuse runs clean under malloc scribble) runs to its exact outcome with no leak, double free or use after free under ASan/LSan."
source = "cases/for-in-vec-iter-reuse-shape.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 60

[case.expected]
stdout = "86OK"
exit = 0
