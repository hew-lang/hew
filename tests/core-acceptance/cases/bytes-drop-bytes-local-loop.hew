fn main() -> i64 {
    var total: i64 = 0;
    var i: i64 = 0;
    while i < 50 {
        let p = "sender-local-bytes-payload".to_bytes();
        total = total + p.len();
        i = i + 1;
    }
    total
}
