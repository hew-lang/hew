[[case]]
id = "generic-trait-bound-dispatch"
intent = "A trait method reached through a where-clause bound resolves to the concrete impl of the monomorphised instance, including a generic impl whose parameters are spelled in the opposite order to its self type; the same impls reached through a `dyn` value keep vtable dispatch."
source = "cases/generic-trait-bound-dispatch.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 15

[case.expected]
stdout = "square(4) = 16\nrect(3x5) = 15\n46\ntag(hinge) = 11\npair = 7\nsquare(9)\n16\n"
exit = 0
