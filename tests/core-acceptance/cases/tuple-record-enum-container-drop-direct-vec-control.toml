[[case]]
id = "tuple-record-enum-container-drop-direct-vec-control"
intent = "Regression oracle for an owned record transferred out of a returned tuple. Shape `direct_vec_control` (record only and direct vec controls are exactly clean) runs to its exact outcome with no leak, double free or use after free under ASan/LSan."
source = "cases/tuple-record-enum-container-drop-direct-vec-control.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 60

[case.expected]
stdout = "len=2\n"
exit = 0
