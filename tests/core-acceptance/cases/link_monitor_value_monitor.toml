[[case]]
id = "link_monitor_value_monitor"
intent = "A MonitorRef built inside a handler closes at scope exit and the program exits 0."
source = "../vertical-slice/accept/link_monitor_value_monitor.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 30

[case.expected]
stdout = ""
exit = 0
