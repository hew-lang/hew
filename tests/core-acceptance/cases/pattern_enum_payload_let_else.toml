[[case]]
id = "pattern_enum_payload_let_else"
intent = "Let-else over an enum record variant with one bound owned field and one wildcarded owned sibling."
source = "cases/pattern_enum_payload_let_else.hew"
suites = ["acceptance"]
kind = "run"
timeout_seconds = 60

[case.expected]
stdout = ""
exit = 0
