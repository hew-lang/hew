[[case]]
id = "indirect-enum-drop-request-scalar"
intent = "Indirect-enum heap-node drop leak oracle. Shape `request_scalar_high` (indirect enum actor request carriers leak slope below tolerance) runs to its exact outcome with no leak, double free or use after free under ASan/LSan."
source = "cases/indirect-enum-drop-request-scalar.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 60

[case.expected]
stdout = "ok"
exit = 0
