actor TestSink {
    var hits: i64;

    receive fn take(v: i32) -> i64 {
        println(f"{v}");
        hits = hits + 1;
        hits
    }
}

fn main() {
    let x: i32 = 7;
    let s = spawn TestSink(hits: 0);
    match await s.take(x) {
        .Ok(n) => println(f"hits {n}"),
        .Err(_) => println("err"),
    }
}
