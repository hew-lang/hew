fn main() {
    let m: HashMap<String, int> = HashMap::new();
    m.insert("alpha", 10);
    m.insert("beta", 20);
    m.insert("gamma", 30);

    // Iterate over key-value pairs; HashMapKeysOp is emitted for for-in over a HashMap.
    var total: int = 0;
    for (k, v) in m {
        total = total + v;
    }

    // Deterministic assertions that don't depend on HashMap iteration order.
    println(m.len());
    println(total);
}
