fn main() {
    let x: i64 = 42;
    let m: HashMap<string, i64> = HashMap.new();
    m.insert("k", x);
    match m.get("k") {
        Option.Some(e) => {
            println(f"{e}");
        },
        Option.None => println("missing"),
    }
}
