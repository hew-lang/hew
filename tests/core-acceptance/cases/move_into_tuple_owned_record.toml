[[case]]
id = "move_into_tuple_owned_record"
intent = "The fixture is refused, and the refusal names InvalidOperation."
source = "../vertical-slice/reject/move_into_tuple_owned_record.hew"
suites = ["acceptance"]
timeout_seconds = 30
kind = "reject"

[[case.expected.diagnostics]]
code = "InvalidOperation"
line = 3
column = 1
message = "process entry must return `()`, an integer, or `Result<(), E>`; found `string`"
