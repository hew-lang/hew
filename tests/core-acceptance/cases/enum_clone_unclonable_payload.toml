[[case]]
id = "enum_clone_unclonable_payload"
intent = "Reject fixture: enum_clone_unclonable_payload — an enum whose variant payload is an `#[opaque]` runtime handle must fail closed when cloned."
source = "../vertical-slice/reject/enum_clone_unclonable_payload.hew"
suites = ["acceptance"]
timeout_seconds = 30
kind = "reject"

[[case.expected.diagnostics]]
code = "UndefinedMethod"
line = 31
column = 13
message = "type `Resource` cannot be cloned because member `Live.0` contains opaque value `Handle`"
