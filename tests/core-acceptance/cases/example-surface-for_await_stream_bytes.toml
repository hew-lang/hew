[[case]]
id = "example-surface-for_await_stream_bytes"
intent = "The published example compiles, runs and prints exactly what it advertises."
source = "../../examples/v05/surfaces/for_await_stream_bytes.hew"
suites = ["acceptance"]
timeout_seconds = 30

[case.env]
HEW_WORKERS = "4"

[case.expected]
stdout_file = "../../examples/v05/surfaces/for_await_stream_bytes.expected"
exit = 0
