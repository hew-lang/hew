[[case]]
id = "fresh-composite-borrow-param-fresh-composite-borrow-block-return"
intent = "Leak oracle for P3 — a fresh COMPOSITE call-result passed directly into a BORROWING parameter, with no user `let`. Shape `fresh_composite_borrow_block_return_high` (borrow block return composite call does not double free, borrow block return composite call leak slope below tolerance) runs to its exact outcome with no leak, double free or use after free under ASan/LSan."
source = "cases/fresh-composite-borrow-param-fresh-composite-borrow-block-return.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 60

[case.expected]
stdout = ""
exit = 0
