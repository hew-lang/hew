fn main() {
    let x: Vec<i64> = Vec::new();
    x.push(10);
    x.push(20);
    let y = clone x;
    let y_l = y.len();
    let y_0 = y[0];
    println(f"len={y_l} v0={y_0}");
    let x_l = x.len();
    let x_0 = x[0];
    println(f"len={x_l} v0={x_0}");
}
