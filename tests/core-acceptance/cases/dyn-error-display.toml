[[case]]
id = "dyn-error-display"
intent = "A failure erased to `dyn Error` reaches its `handle` block and the recovery value flows on."
source = "cases/dyn-error-display.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 15

[case.expected]
stdout = "caught\nwidth=0\n"
exit = 0
