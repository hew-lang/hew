[[case]]
id = "collection-borrowed-cow-ingress-reassigned-param-ingress"
intent = "Carrier-general collection-ingress exactly-once oracles: the borrowed `bytes` parameter, the REASSIGNED parameter generation, and the `VecIter` `string` yield binder. Shape `reassigned_param_ingress` (reassigned param ingress reads back exact values) runs to its exact outcome with no leak, double free or use after free under ASan/LSan."
source = "cases/collection-borrowed-cow-ingress-reassigned-param-ingress.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 60

[case.expected]
stdout = "1|2|-1"
exit = 0
