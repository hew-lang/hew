fn main() {
    let x: Result<i64, string> = Result.Ok(6);
    let y: Result<i64, string> = Result.Ok(7);
    var v: Vec<Result<i64, string>> = Vec.new();
    v.push(x);
    v.push(y);
    let n = v.len();
    println(f"len={n}");
}
