[[case]]
id = "spread-machine-state"
intent = "A machine state literal in a transition spreads the source state with `..self`, so the transition writes only the fields that change and the rest carry through unchanged."
source = "cases/spread-machine-state.hew"
suites = ["acceptance"]
timeout_seconds = 30

[case.expected]
stdout = "empty\nopen:1\nopen:3\nvoided:3\nempty\n"
exit = 0
