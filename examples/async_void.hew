//! Status: aspirational (Hew roadmap: v0.6 concurrency surfaces plus codegen-rs parity)
//! Description: Async Void example.

fn print_message(msg: i32) {
    println(msg);
    println(42);
}

fn main() {
    await print_message(100);
    println(999);
}
