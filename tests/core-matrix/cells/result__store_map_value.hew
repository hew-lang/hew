fn main() {
    let x: Result<i64, string> = Result.Ok(6);
    let m: HashMap<string, Result<i64, string>> = HashMap.new();
    m.insert("k", x);
    match m.get("k") {
        Option.Some(e) => {
            match e { Result.Ok(e_v) => println(f"ok {e_v}"), Result.Err(e_e) => println(f"err {e_e}"), }
        },
        Option.None => println("missing"),
    }
}
