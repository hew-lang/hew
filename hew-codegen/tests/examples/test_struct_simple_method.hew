type Point {
    x: f64;
    y: f64;
}

trait PointOps {
    fn sum(pt: Point) -> f64;
}

impl PointOps for Point {
    fn sum(pt: Point) -> f64 {
        pt.x + pt.y
    }
}

fn main() {
    let p = Point { x: 3.0, y: 4.0 };
    let s = p.sum();
    println(s);
}
