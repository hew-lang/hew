[[case]]
id = "actor_receive_fn_named_send"
intent = "The fixture runs to completion and exits 42."
source = "../vertical-slice/accept/actor_receive_fn_named_send.hew"
suites = ["acceptance"]
timeout_seconds = 30

[case.env]
HEW_WORKERS = "4"

[case.expected]
stdout = ""
exit = 42
