[[case]]
id = "carrier_transfer_sibling_path_release"
intent = "The fixture runs to completion and prints exactly its recorded output."
source = "../vertical-slice/accept/carrier_transfer_sibling_path_release.hew"
suites = ["acceptance"]
timeout_seconds = 30

[case.env]
HEW_WORKERS = "4"

[case.expected]
stdout = "then=1\nelse=2\nagain=4\n"
exit = 0
