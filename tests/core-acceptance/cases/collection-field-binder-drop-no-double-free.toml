[[case]]
id = "collection-field-binder-drop-no-double-free"
intent = "Leak oracle for a heap-owning collection reached as a record FIELD or tuple MEMBER and then read through a BORROWING method (`.len()`, `.get(i)`, …). Shape `no_double_free` (record collection field read release is exactly once under malloc scribble) runs to its exact outcome with no leak, double free or use after free under ASan/LSan."
source = "cases/collection-field-binder-drop-no-double-free.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 60

[case.expected]
stdout = "14OK"
exit = 0
