[[case]]
id = "move_out_no_double_free"
intent = "W5-011 safety proof — a `string` returned from a user function and an aliasing call result must be freed exactly once, never twice."
source = "../vertical-slice/accept/move_out_no_double_free.hew"
suites = ["acceptance"]
timeout_seconds = 30

[case.env]
HEW_WORKERS = "4"

[case.expected]
stdout = "done\n"
exit = 0
