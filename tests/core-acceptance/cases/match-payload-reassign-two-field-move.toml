[[case]]
id = "match-payload-reassign-two-field-move"
intent = "Projected-payload move-out UAF / double-free oracle (issue #2523). Shape `two_field_move` (two field move out single free) runs to its exact outcome with no leak, double free or use after free under ASan/LSan."
source = "cases/match-payload-reassign-two-field-move.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 60

[case.expected]
stdout = ""
exit = 6
