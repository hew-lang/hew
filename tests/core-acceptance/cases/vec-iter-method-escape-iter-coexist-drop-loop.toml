[[case]]
id = "vec-iter-method-escape-iter-coexist-drop-loop"
intent = "`Vec<T>::iter()` cursor drop-safety oracles: the `iter()` cursor must own an INDEPENDENT snapshot of its backing buffer, so it can coexist with — or outlive — the source vec without a double-free or use-after-free. Shape `iter_coexist_drop_loop_high` (iter coexist cursor no per frame leak slope) runs to its exact outcome with no leak, double free or use after free under ASan/LSan."
source = "cases/vec-iter-method-escape-iter-coexist-drop-loop.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 60

[case.expected]
stdout = ""
exit = 100
