[[case]]
id = "machine_state_resource_payload_heap_sibling"
intent = "A direct `#[resource]` state payload has no independent value copy, so a step cannot stage it yet; the refusal names the machine, state, field, type and the transition that takes it."
source = "../vertical-slice/reject/machine_state_resource_payload_heap_sibling.hew"
suites = ["acceptance"]
timeout_seconds = 30
kind = "reject"

[[case.expected.diagnostics]]
code = "MachineExhaustivenessError"
line = 24
column = 40
message = "machine `Mixed` state `Active` field `h` holds `Handle`, which has no independent value copy"

[[case.expected.diagnostics]]
code = "MachineExhaustivenessError"
line = 39
column = 22
message = "machine `Plain` state `Live` field `h` holds `Handle`, which has no independent value copy"
