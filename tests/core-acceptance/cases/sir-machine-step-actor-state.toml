[[case]]
id = "sir-machine-step-actor-state"
intent = "A machine held in an actor var field is stepped from a receive fn: the staged receiver replacement roots at the actor-state place, the emitted owning outputs are observed and each transition releases the replaced state payload once."
source = "cases/sir-machine-step-actor-state.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 30

[case.expected]
stdout = "FRONT/Ajar\nFRONT/Shut\n"
stderr = ""
exit = 0
