[[case]]
id = "param-record-field-loop-drop-projection-store-mid-loop"
intent = "Regression oracle for a `for` loop iterating a field of a BY-VALUE record parameter. Shape `projection_store_mid_loop` (projection store mid loop replaces the field without corrupting the cursor) runs to its exact outcome with no leak, double free or use after free under ASan/LSan."
source = "cases/param-record-field-loop-drop-projection-store-mid-loop.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 60

[case.expected]
stdout = "40\n2\n"
exit = 0
