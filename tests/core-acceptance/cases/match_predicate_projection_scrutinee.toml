[[case]]
id = "match_predicate_projection_scrutinee"
intent = "Accept: a literal-predicate record match on a projection scrutinee."
source = "../vertical-slice/accept/match_predicate_projection_scrutinee.hew"
suites = ["acceptance"]
timeout_seconds = 30

[case.env]
HEW_WORKERS = "4"

[case.expected]
stdout = "2\nby\n"
exit = 0
