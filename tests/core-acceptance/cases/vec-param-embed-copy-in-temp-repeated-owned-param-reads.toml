[[case]]
id = "vec-param-embed-copy-in-temp-repeated-owned-param-reads"
intent = "Prepared parameter carriers embedded in anonymous Vec COPY-IN source temps. Shape `repeated_owned_param_reads` (repeated owned parameter reads survive native cleanup, repeated string parameter reads stay on the borrow spine) runs to its exact outcome with no leak, double free or use after free under ASan/LSan."
source = "cases/vec-param-embed-copy-in-temp-repeated-owned-param-reads.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 60

[case.expected]
stdout = "15|CARRIER-GUARD"
exit = 0
