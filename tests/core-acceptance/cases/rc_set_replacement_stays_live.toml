[[case]]
id = "rc_set_replacement_stays_live"
intent = "The fixture runs to completion and prints exactly its recorded output."
source = "../vertical-slice/accept/rc_set_replacement_stays_live.hew"
suites = ["acceptance"]
timeout_seconds = 30

[case.env]
HEW_WORKERS = "4"

[case.expected]
stdout = "new\n"
exit = 0
