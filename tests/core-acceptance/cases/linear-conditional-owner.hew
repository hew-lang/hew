#[linear]
type Tx { label: string }
impl Tx {
    fn commit(consume self) { println(self.label); }
}
fn partial(consume tx: Tx, finish: bool) {
    if finish { tx.commit(); }
}
fn main() { partial(Tx { label: "conditional".to_upper() }, true); }
