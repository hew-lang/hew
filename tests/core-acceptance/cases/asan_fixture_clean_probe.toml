[[case]]
id = "asan_fixture_clean_probe"
intent = "ASan fixture gate: clean probe."
source = "../vertical-slice/accept/asan_fixture_clean_probe.hew"
suites = ["acceptance"]
timeout_seconds = 30

[case.env]
HEW_WORKERS = "4"

[case.expected]
stdout = ""
exit = 0
