[[case]]
id = "for-in-vec-iter-return-yielded-value"
intent = "`for x in vec` cursor (`VecIter<T>`) scope-exit drop oracles: per-iteration leak slope plus a poisoned-allocator no-use-after-free pin for the place-source reuse shape (issue #1949). Shape `return_yielded_value` (for in return yielded value runs clean under malloc scribble) runs to its exact outcome with no leak, double free or use after free under ASan/LSan."
source = "cases/for-in-vec-iter-return-yielded-value.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 60

[case.expected]
stdout = "yield-returned-valueOK"
exit = 0
