[[case]]
id = "aggregate-cleanup-resource-fault"
intent = "A bounds fault unwinds the live resource resource owner exactly once."
source = "cases/aggregate-cleanup-resource-fault.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 30

[case.expected]
stdout = "before trap\nclose 1\n"
stderr = "hew: failure: IndexOutOfBounds (205)\n"
exit = 1
