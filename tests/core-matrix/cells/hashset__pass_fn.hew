fn use_it(v: HashSet<i64>) {
    let v_l = v.len();
    let v_h = v.contains(1);
    println(f"len={v_l} has1={v_h}");
}

fn main() {
    let x: HashSet<i64> = HashSet::new();
    x.insert(1);
    x.insert(2);
    use_it(x);
}
