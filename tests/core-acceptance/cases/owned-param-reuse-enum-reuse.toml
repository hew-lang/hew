[[case]]
id = "owned-param-reuse-enum-reuse"
intent = "Leak oracle for caller-side owned by-value param drop under REUSE. Shape `enum_reuse_df` (enum param reuse does not double free, enum param reuse leak slope below tolerance) runs to its exact outcome with no leak, double free or use after free under ASan/LSan."
source = "cases/owned-param-reuse-enum-reuse.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 60

[case.expected]
stdout = ""
exit = 0
