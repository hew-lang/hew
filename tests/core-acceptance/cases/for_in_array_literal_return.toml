[[case]]
id = "for_in_array_literal_return"
intent = "#2356 companion: an explicit `-> i64` main with a `return 0` after the loop must also exit 0 against the built native binary — the invalid free fired at scope-exit regardless of whether the function ends via a tail expression or an explicit `return`, so both shapes are proven."
source = "../vertical-slice/accept/for_in_array_literal_return.hew"
suites = ["acceptance"]
timeout_seconds = 30

[case.env]
HEW_WORKERS = "4"

[case.expected]
stdout = "99\n6\n100\n"
exit = 0
