[[case]]
id = "stream_lazy_adapter_unsupported"
intent = "The lazy stream adapters are gone from the pipe surface: `Stream<T>.map` is refused at the method lookup with exactly one error and no cascade."
source = "../vertical-slice/reject/stream_lazy_adapter_unsupported.hew"
suites = ["acceptance"]
timeout_seconds = 30
kind = "check"

[[case.expected.diagnostics]]
code = "UndefinedMethod"
line = 11
column = 14
message = "no method `map` on `Stream<string>`"

[[case.expected.diagnostics]]
code = "dead_code"
line = 10
column = 1
