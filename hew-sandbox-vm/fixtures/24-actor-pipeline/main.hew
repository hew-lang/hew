actor TestSink { receive fn collect(value: i64) { println(value); } }
actor Doubler { receive fn process(next: TestSink, value: i64) { next.collect(value * 2); } }
fn main() { let sink = spawn TestSink; let doubler = spawn Doubler; doubler.process(sink, 21); }
