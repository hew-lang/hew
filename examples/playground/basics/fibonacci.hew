//! @name Fibonacci
//! @description Calculate Fibonacci numbers recursively

fn fib(n: i64) -> i64 {
    if n <= 1 {
        n
    } else {
        fib(n - 1) + fib(n - 2)
    }
}

fn main() {
    for i in 0 .. 10 {
        print(f"fib({i}) = ");
        println(fib(i));
    }
}
