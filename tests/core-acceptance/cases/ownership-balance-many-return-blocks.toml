[[case]]
id = "ownership-balance-many-return-blocks"
intent = "Ownership-balance fixture. The ownership checker balances every obligation, so the program checks with no diagnostics."
source = "fixtures/ownership-balance/many_return_blocks.hew"
suites = ["acceptance"]
timeout_seconds = 30
kind = "check"
