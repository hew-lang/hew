[[case]]
id = "nested-collection-actor-state-outer-iteration"
intent = "Nested-collection exactly-once ownership oracles for #2545 and #2546. Shape `actor_state_outer_iteration_high` (actor state outer iteration has no per row leak slope) runs to its exact outcome with no leak, double free or use after free under ASan/LSan."
source = "cases/nested-collection-actor-state-outer-iteration.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 60

[case.expected]
stdout = ""
exit = 0
