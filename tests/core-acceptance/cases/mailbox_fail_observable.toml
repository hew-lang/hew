[[case]]
id = "mailbox_fail_observable"
intent = "A `.Reject` mailbox view refuses a submission to a full queue with SendError.Full."
source = "../vertical-slice/accept/mailbox_fail_observable.hew"
suites = ["acceptance"]
timeout_seconds = 30

[case.env]
HEW_WORKERS = "4"

[case.expected]
stdout = "FAIL_VISIBLE\n"
exit = 43
