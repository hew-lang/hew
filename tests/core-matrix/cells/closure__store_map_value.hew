fn main() {
    let x = |v: i64| v * 2;
    let m: HashMap<string, fn(i64) -> i64> = HashMap.new();
    m.insert("k", x);
    match m.get("k") {
        Some(e) => {
            let e_r = e(21);
            println(f"{e_r}");
        },
        None => println("missing"),
    }
}
