fn make() -> Vec<i64> {
    let m: Vec<i64> = Vec::new();
    m.push(10);
    m.push(20);
    m
}

fn main() {
    let x = make();
    let x_l = x.len();
    let x_0 = x[0];
    println(f"len={x_l} v0={x_0}");
}
