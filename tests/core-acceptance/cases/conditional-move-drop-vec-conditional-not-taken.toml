[[case]]
id = "conditional-move-drop-vec-conditional-not-taken"
intent = "Conditionally-moved collection locals — leak-slope oracles plus poisoned-allocator exactly-once pins (#2418). Shape `vec_conditional_not_taken_high` (vec conditional move not taken no per call leak slope) runs to its exact outcome with no leak, double free or use after free under ASan/LSan."
source = "cases/conditional-move-drop-vec-conditional-not-taken.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 60

[case.expected]
stdout = ""
exit = 50
