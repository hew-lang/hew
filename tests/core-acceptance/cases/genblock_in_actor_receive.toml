[[case]]
id = "genblock_in_actor_receive"
intent = "Reject fixture: E_GENBLOCK_IN_ACTOR_RECEIVE A gen{} block inside an actor receive handler is forbidden."
source = "../vertical-slice/reject/genblock_in_actor_receive.hew"
suites = ["acceptance"]
timeout_seconds = 30
kind = "reject"

[[case.expected.diagnostics]]
code = "GenBlockInActorReceive"
line = 9
column = 18
message = "`gen { }` blocks are forbidden inside actor receive handlers — the scheduler holds the actor-state lock for the entire handler invocation; use a named generator function outside the handler instead"
