#[resource]
type Conn { fd: i64 }
impl Conn {
    fn close(c: Conn) { println(f"close {c.fd}"); }
}
fn peek(c: Conn) -> i64 {
    c.fd
}
fn main() {
    let conn = Conn { fd: 3 };
    println(peek(conn));
    println(conn.fd);
}
