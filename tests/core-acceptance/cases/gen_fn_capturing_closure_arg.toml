[[case]]
id = "gen_fn_capturing_closure_arg"
intent = "Reject: a CAPTURING closure passed where a generator declares a `fn(..)` parameter fails CLOSED at check time."
source = "../vertical-slice/reject/gen_fn_capturing_closure_arg.hew"
suites = ["acceptance"]
timeout_seconds = 30
kind = "reject"

[[case.expected.diagnostics]]
code = "E_SIR_VERIFY"
line = 0
column = 0
message = "SIR verification failed: [SirDiagnostic { function: \"__hew_fn_mapped\", callable: Some(CallableId(2)), kind: InvalidOperation { op: OpId(1), reason: \"`fn(i64) -> i64` has no copy operation\" } }]"
