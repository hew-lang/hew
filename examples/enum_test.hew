enum Colour {
    Red;
    Green;
    Blue;
}

enum Shape {
    Circle(i32);
    Rectangle(i32);
}

fn describe_colour(tag: i32) -> i32 {
    match tag {
        0 => {
            println("Red");
            0
        },
        1 => {
            println("Green");
            1
        },
        2 => {
            println("Blue");
            2
        },
        _ => {
            println("Unknown");
            -1
        },
    }
}

fn main() {
    println("=== Enum Test ===");
    describe_colour(0);
    describe_colour(1);
    describe_colour(2);
    describe_colour(99);
}
