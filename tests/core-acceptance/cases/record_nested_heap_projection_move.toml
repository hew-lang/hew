[[case]]
id = "record_nested_heap_projection_move"
intent = "The fixture runs to completion and exits 0."
source = "../vertical-slice/accept/record_nested_heap_projection_move.hew"
suites = ["acceptance"]
timeout_seconds = 30

[case.env]
HEW_WORKERS = "4"

[case.expected]
stdout = ""
exit = 0
