[[case]]
id = "usize_shift"
intent = "The fixture runs to completion and exits 0."
source = "../vertical-slice/accept/usize_shift.hew"
suites = ["acceptance"]
timeout_seconds = 30

[case.env]
HEW_WORKERS = "4"

[case.expected]
stdout = ""
exit = 0
