[[case]]
id = "tuple_heap_return"
intent = "The fixture runs to completion and exits 42."
source = "../vertical-slice/accept/tuple_heap_return.hew"
suites = ["acceptance"]
timeout_seconds = 30

[case.env]
HEW_WORKERS = "4"

[case.expected]
stdout = ""
exit = 42
