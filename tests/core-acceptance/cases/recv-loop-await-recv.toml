[[case]]
id = "recv-loop-await-recv"
intent = "Per-iteration recv-result drop on the loop back-edge. Shape `await_recv_low` (source await recv string loop no per frame leak slope) runs to its exact outcome with no leak, double free or use after free under ASan/LSan."
source = "cases/recv-loop-await-recv.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 60

[case.expected]
stdout = "f0\nf1\nf2\n"
exit = 0
