// Lambda actor - inline actor definition

fn main() {
    let worker = spawn (msg: int) => {
        println(msg * 2);
    };

    worker.send(5);
    worker.send(10);
    worker.send(15);

    close(worker);
    await worker;
}
