/// A helper function in a user module.
pub fn greet(name: String) -> String {
    "hello " + name
}

/// Add two integers.
pub fn add(a: int, b: int) -> int {
    a + b
}

/// This is private and should NOT be accessible.
fn secret() -> int {
    42
}
