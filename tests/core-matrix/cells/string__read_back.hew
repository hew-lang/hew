fn main() {
    let x: string = "ab";
    let v: Vec<string> = Vec::new();
    v.push(x);
    match v.get(0) {
        Some(e) => {
            println(f"{e}");
        },
        None => println("missing"),
    }
}
