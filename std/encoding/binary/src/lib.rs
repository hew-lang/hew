//! Hew `std::encoding::binary` — fixed-width integer encoding and decoding.
//!
//! All encoding and decoding logic is implemented in pure Hew (`binary.hew`).
//! This crate exists as a placeholder for the workspace build; no FFI
//! functions are required.
