[[case]]
id = "state-overwrite-projected-state-field-iter"
intent = "State-field overwrite leak oracles for collection, record, and enum actor state fields, plus alias-shape use-after-free pins. Shape `projected_state_field_iter` (projected state field for loop frees state buffer once) runs to its exact outcome with no leak, double free or use after free under ASan/LSan."
source = "cases/state-overwrite-projected-state-field-iter.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 60

[case.expected]
stdout = ""
exit = 100
