[[case]]
id = "hashset_managed_record_elem"
intent = "The fixture runs to completion and exits 0."
source = "../vertical-slice/accept/hashset_managed_record_elem.hew"
suites = ["acceptance"]
timeout_seconds = 30

[case.env]
HEW_WORKERS = "4"

[case.expected]
stdout = ""
exit = 0
