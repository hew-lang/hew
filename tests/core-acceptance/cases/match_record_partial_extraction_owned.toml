[[case]]
id = "match_record_partial_extraction_owned"
intent = "Non-BitCopy record match destructure — PARTIAL extraction (wildcard on an owned `string` sibling)."
source = "../vertical-slice/accept/match_record_partial_extraction_owned.hew"
suites = ["acceptance"]
timeout_seconds = 30

[case.env]
HEW_WORKERS = "4"

[case.expected]
stdout = "done\n"
exit = 0
