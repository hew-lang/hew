actor Worker {
    receive fn greet(label: String) {
        println(label);
    }
}

fn main() {
    let w = spawn Worker;
    w.greet("hello");
    w.greet("world");
    await close(w);
    println("done");
}
