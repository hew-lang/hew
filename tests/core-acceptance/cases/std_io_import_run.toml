[[case]]
id = "std_io_import_run"
intent = "The fixture runs to completion and prints exactly what it printed here."
source = "../vertical-slice/accept/std_io_import_run.hew"
suites = ["acceptance"]
timeout_seconds = 30

[case.env]
HEW_WORKERS = "4"

[case.expected]
stdout = "stdlib-io:write\n"
exit = 0
