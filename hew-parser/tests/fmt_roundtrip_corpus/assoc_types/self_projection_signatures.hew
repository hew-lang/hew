trait Factory {
    type Product = i32;

    fn make(factory: Self) -> Self::Product;
}

type WidgetFactory {
}

impl Factory for WidgetFactory {
    type Product = i32;

    fn make(factory: WidgetFactory) -> Self::Product {
        1
    }
}
