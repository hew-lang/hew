[[case]]
id = "ownership-balance-actor-state-vec-seat"
intent = "An actor state `Vec` mutated and drained in its own seat. Driven 200,000 times, it releases every obligation exactly once with no leak under ASan/LSan."
source = "fixtures/ownership-balance/actor_state_vec_seat.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 120

[case.expected]
stdout = "1800004\n"
exit = 0
