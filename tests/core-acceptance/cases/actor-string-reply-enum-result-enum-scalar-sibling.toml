[[case]]
id = "actor-string-reply-enum-result-enum-scalar-sibling"
intent = "Actor string-reply / nested-enum-payload leak oracle (#2717). Shape `result_enum_scalar_sibling_high` (result enum scalar sibling leak slope below tolerance, result enum scalar sibling no double free under malloc scribble) runs to its exact outcome with no leak, double free or use after free under ASan/LSan."
source = "cases/actor-string-reply-enum-result-enum-scalar-sibling.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 60

[case.expected]
stdout = ""
exit = 0
