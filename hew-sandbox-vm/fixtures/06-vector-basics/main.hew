fn main() {
    let values: Vec<i64> = Vec::new();
    values.push(10);
    values.push(20);
    println(values.len());
    match values.get(1) {
        Some(v) => println(v),
        None => println(-1),
    }
}
