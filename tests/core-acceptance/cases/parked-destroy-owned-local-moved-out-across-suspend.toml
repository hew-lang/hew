[[case]]
id = "parked-destroy-owned-local-moved-out-across-suspend"
intent = "Destroy-while-parked owned-local leak / double-free oracle (#2395). Shape `moved_out_across_suspend` (moved out across suspend no double free) runs to its exact outcome with no leak, double free or use after free under ASan/LSan."
source = "cases/parked-destroy-owned-local-moved-out-across-suspend.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 60

[case.expected]
stdout = "stopped\n"
exit = 0
