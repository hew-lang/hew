[[case]]
id = "lambda_log_collision"
intent = "The fixture runs to completion and prints exactly its recorded output."
source = "../vertical-slice/accept/lambda_log_collision.hew"
suites = ["acceptance"]
timeout_seconds = 30

[case.env]
HEW_WORKERS = "4"

[case.expected]
stdout = "LOG: hello\nLOG: ask 42\n"
exit = 0
