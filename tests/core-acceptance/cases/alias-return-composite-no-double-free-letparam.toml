[[case]]
id = "alias-return-composite-no-double-free-letparam"
intent = "ADVERSARIAL double-free pin — the load-bearing guard against a future over-widening of the fresh-composite sole-owner admission. Shape `letparam` (letparam alias return does not double free) runs to its exact outcome with no leak, double free or use after free under ASan/LSan."
source = "cases/alias-return-composite-no-double-free-letparam.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 60

[case.expected]
stdout = ""
exit = 0
