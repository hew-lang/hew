[[case]]
id = "actor_method_from_hook_and_init"
intent = "The fixture runs to completion and prints exactly its recorded output."
source = "../vertical-slice/accept/actor_method_from_hook_and_init.hew"
suites = ["acceptance"]
timeout_seconds = 30

[case.env]
HEW_WORKERS = "4"

[case.expected]
stdout = "hello world\n3\n"
exit = 0
