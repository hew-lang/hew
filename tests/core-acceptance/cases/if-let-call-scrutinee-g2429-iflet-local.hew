fn main() -> i64 {
    var total = 0;
    var i = 0;
    while i < 50 {
        let opt: Option<string> = .Some("g2429-local-owned".to_upper());
        if let .Some(b) = opt { total = total + b.len(); }
        i = i + 1;
    }
    if total > 0 { 0 } else { 1 }
}
