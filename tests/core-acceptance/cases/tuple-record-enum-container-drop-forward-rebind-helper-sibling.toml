[[case]]
id = "tuple-record-enum-container-drop-forward-rebind-helper-sibling"
intent = "Regression oracle for an owned record transferred out of a returned tuple. Shape `forward_rebind_helper_sibling` (helper frame forward rebind releases transferred and sibling vecs) runs to its exact outcome with no leak, double free or use after free under ASan/LSan."
source = "cases/tuple-record-enum-container-drop-forward-rebind-helper-sibling.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 60

[case.expected]
stdout = "checksum=3616\n"
exit = 0
