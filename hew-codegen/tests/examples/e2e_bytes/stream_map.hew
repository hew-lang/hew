import std::stream;

fn main() {
    let (sink, input) = stream.channel(8);

    sink.write("hello");
    sink.write("world");
    sink.close();

    let mapped = input.map((s) => s + "!");

    for await item in mapped {
        println(item);
    }
}
