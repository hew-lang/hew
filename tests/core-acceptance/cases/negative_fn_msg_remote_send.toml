[[case]]
id = "negative_fn_msg_remote_send"
intent = "Reject: a function value has no wire representation, so it must not cross a RemotePid<T>::send boundary."
source = "../vertical-slice/reject/negative_fn_msg_remote_send.hew"
suites = ["acceptance"]
timeout_seconds = 30
kind = "reject"

[[case.expected.diagnostics]]
code = "BoundsNotSatisfied"
line = 17
column = 5
message = "remote actor `Worker` cannot carry `fn(i64) -> i64`: a value sent to a remote actor needs an explicit wire schema, and `fn(i64) -> i64` has none"
