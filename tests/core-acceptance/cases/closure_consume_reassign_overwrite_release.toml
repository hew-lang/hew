[[case]]
id = "closure_consume_reassign_overwrite_release"
intent = "The fixture runs to completion and exits 0."
source = "../vertical-slice/accept/closure_consume_reassign_overwrite_release.hew"
suites = ["acceptance"]
timeout_seconds = 30

[case.env]
HEW_WORKERS = "4"

[case.expected]
stdout = ""
exit = 0
