machine Door {
    events {
        Toggle;
    }

    state Closed;
    state Open;

    on Toggle: Closed => Open {
        gen { yield Open; }
    }
    on Toggle: Open => Closed {
        Closed
    }
}

fn gen_transition_rejection_probe() -> i32 {
    7
}
