// Lambda functions
fn apply(f: fn(i64) -> i64, x: i64) -> i64 {
    f(x)
}

fn double_fn(n: i64) -> i64 {
    n * 2
}

fn triple_fn(n: i64) -> i64 {
    n * 3
}

fn main() {
    println(apply(double_fn, 10));
    println(apply(triple_fn, 10));
}
