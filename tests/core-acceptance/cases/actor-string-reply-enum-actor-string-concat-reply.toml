[[case]]
id = "actor-string-reply-enum-actor-string-concat-reply"
intent = "Actor string-reply / nested-enum-payload leak oracle (#2717). Shape `actor_string_concat_reply_high` (actor string concat reply leak slope below tolerance, actor string concat reply no double free under malloc scribble) runs to its exact outcome with no leak, double free or use after free under ASan/LSan."
source = "cases/actor-string-reply-enum-actor-string-concat-reply.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 60

[case.expected]
stdout = ""
exit = 0
