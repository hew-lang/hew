[[case]]
id = "snapshot-send-snapshot-send-projection"
intent = "Compiled leak and poisoned-allocator oracle for actor snapshot sends. Shape `snapshot-send-projection_high` (snapshot send projection source has flat leak slope, snapshot send projection source is scribble clean with exact output) runs to its exact outcome with no leak, double free or use after free under ASan/LSan."
source = "cases/snapshot-send-snapshot-send-projection.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 60

[case.expected]
stdout = "50:50"
exit = 0
