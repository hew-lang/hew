# Sandbox VM golden fixtures

The deferred metadata below describes this sandbox emitter and its fixture
provenance. Its old task/actor wording is not the current Hew language grammar;
ordinary calls wait, `fork` creates tasks, and `await` joins tasks. Preserve the
machine-read provenance markers when updating the surrounding guidance.

Each fixture directory contains:

- `main.hew` — the learner-visible Hew source.
- `expected.trace.json` — the deterministic Trace Schema v0 contract,
  generated by `make sandbox-fixtures-record`.
- `bytecode.json` — generated by `make baselines`, unless the fixture is
  listed in the enforced deferred block below.

Neither artifact is hand-edited. `make baselines` refreshes bytecode alone and
needs no Node, so a fixture whose bytecode changed there still has last
recording's trace and the conformance suites will say so; run
`make sandbox-fixtures-record` to bring both back in step.
`make sandbox-fixtures-record` recompiles the source and runs the VM, carrying each fixture's replay configuration (seed,
step budget, virtual clock and inputs) through unchanged, so a recording
changes only what the VM observed. Review a regenerated golden by its trace
family sequence and its `final_state`; the full equality the conformance suites
assert is the drift detector, not the review.

The fixture catalog is declared in [`manifest.json`](manifest.json) and described
in [`../specs/golden-fixtures-v0.md`](../specs/golden-fixtures-v0.md).

Regenerate compiled fixture bytecode from the repository root:

```sh
make baselines
```

Check mode is the CI gate:

```sh
make sandbox-fixtures-check
```

Fixtures that exercise diagnostics only (`09-compile-type-error`,
`10-sandbox-reject-filesystem` and `29-mixed-scalar-compare-rejected`)
intentionally have no bytecode artifact. Fixtures
below are deferred because the checked-in source is ahead of the canonical
sandbox bytecode emitter or, for fixture 30, no source fixture exists yet. The
JSON block is parsed by the build tool and provenance test; bytecode fixtures in
this list must carry the exact `compiler_version` marker shown here.

```json sandbox-fixtures-deferred
{
  "deferred": [
    {
      "fixture": "15-actor-counter",
      "feature": "actor-scheduler-emitter",
      "compiler_version": "handcrafted-pending-actor-scheduler-emitter",
      "reason": "actor declarations, spawn, and await are profile-reserved"
    },
    {
      "fixture": "16-actor-ask-reply",
      "feature": "actor-ask-await-emitter",
      "compiler_version": "handcrafted-pending-actor-ask-await-emitter",
      "reason": "actor ask/reply and await are profile-reserved"
    },
    {
      "fixture": "17-pipe-select",
      "feature": "pipe-select-parser-emitter",
      "reason": "select syntax is not accepted by the canonical parser/emitter path yet"
    },
    {
      "fixture": "18-task-await",
      "feature": "task-await-parser-emitter",
      "reason": "spawn task and await syntax are not accepted by the canonical parser/emitter path yet"
    },
    {
      "fixture": "19-supervisor-restart",
      "feature": "supervisor-parser-emitter",
      "reason": "supervisor syntax is not accepted by the canonical parser/emitter path yet"
    },
    {
      "fixture": "20-machine-traffic-light",
      "feature": "machine-emitter",
      "reason": "machine declarations and methods are profile-reserved"
    },
    {
      "fixture": "24-actor-pipeline",
      "feature": "actor-message-emitter",
      "compiler_version": "handcrafted-pending-actor-message-emitter",
      "reason": "typed actor message lowering is not covered by the canonical emitter yet"
    },
    {
      "fixture": "25-actor-crash",
      "feature": "actor-lifecycle-parser-emitter",
      "compiler_version": "handcrafted-pending-actor-lifecycle-parser-emitter",
      "reason": "on(crash) lifecycle syntax is not accepted by the canonical parser/emitter path yet"
    },
    {
      "fixture": "27-actor-crash-restart-ignored",
      "feature": "actor-lifecycle-parser-emitter",
      "compiler_version": "handcrafted-pending-actor-lifecycle-parser-emitter",
      "reason": "actor lifecycle hook syntax is not accepted by the canonical parser/emitter path yet"
    },
    {
      "fixture": "28-actor-state-arity-trap",
      "feature": "actor-state-parser-emitter",
      "compiler_version": "handcrafted-pending-actor-state-parser-emitter",
      "reason": "actor state constructor syntax is not accepted by the canonical parser/emitter path yet"
    },
    {
      "fixture": "30-child-crash-supervisor-restart-decision",
      "feature": "supervisor-source-fixture",
      "compiler_version": "handcrafted-pending-supervisor-source-fixture",
      "reason": "the fixture has no main.hew source yet and remains handcrafted pending source and emitter coverage"
    }
  ]
}
```
