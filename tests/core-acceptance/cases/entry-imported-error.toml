[[case]]
id = "entry-imported-error"
intent = "Imported errors with the same leaf name retain distinct Display signatures and render the selected entry error."
source = "cases/entry-imported-error/main.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 15

[case.expected]
stdout = "text: auxiliary\n"
stderr = "error: number: 42\n"
exit = 1
