[[case]]
id = "escaped-sibling-field-drop-read-after-escape"
intent = "Escaped-sibling field-drop oracle (#2212) — the empirical (compiled-binary) half of the sibling-discharge contract whose MIR side lives in `tests/core-acceptance/cases/escaped_sibling_field_drop.hew`. Shape `read_after_escape` (record read after escape not discharged under malloc scribble) runs to its exact outcome with no leak, double free or use after free under ASan/LSan."
source = "cases/escaped-sibling-field-drop-read-after-escape.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 60

[case.expected]
stdout = "k"
exit = 0
