[[case]]
id = "state-overwrite-enum-overwrite"
intent = "State-field overwrite leak oracles for collection, record, and enum actor state fields, plus alias-shape use-after-free pins. Shape `enum_overwrite_high` (enum state overwrite no per frame leak slope) runs to its exact outcome with no leak, double free or use after free under ASan/LSan."
source = "cases/state-overwrite-enum-overwrite.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 60

[case.expected]
stdout = ""
exit = 1
