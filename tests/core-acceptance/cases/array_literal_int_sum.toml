[[case]]
id = "array_literal_int_sum"
intent = "The fixture runs to completion and exits 6."
source = "../vertical-slice/accept/array_literal_int_sum.hew"
suites = ["acceptance"]
timeout_seconds = 30

[case.env]
HEW_WORKERS = "4"

[case.expected]
stdout = ""
exit = 6
