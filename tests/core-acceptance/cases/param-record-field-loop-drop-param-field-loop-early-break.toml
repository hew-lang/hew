[[case]]
id = "param-record-field-loop-drop-param-field-loop-early-break"
intent = "Regression oracle for a `for` loop iterating a field of a BY-VALUE record parameter. Shape `param_field_loop_early_break` (param field loop early break keeps both fields live and leak free) runs to its exact outcome with no leak, double free or use after free under ASan/LSan."
source = "cases/param-record-field-loop-drop-param-field-loop-early-break.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 60

[case.expected]
stdout = "v=13\n"
exit = 0
