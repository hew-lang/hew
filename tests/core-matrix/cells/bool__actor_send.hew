actor TestSink {
    var hits: i64;

    receive fn take(v: bool) -> i64 {
        println(f"{v}");
        hits = hits + 1;
        hits
    }
}

fn main() {
    let x: bool = true;
    let s = spawn TestSink(hits: 0);
    match await s.take(x) {
        Ok(n) => println(f"hits {n}"),
        Err(_) => println("err"),
    }
}
