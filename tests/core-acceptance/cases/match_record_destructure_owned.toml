[[case]]
id = "match_record_destructure_owned"
intent = "Non-BitCopy record match destructure — full extraction."
source = "../vertical-slice/accept/match_record_destructure_owned.hew"
suites = ["acceptance"]
timeout_seconds = 30

[case.env]
HEW_WORKERS = "4"

[case.expected]
stdout = "done\n"
exit = 0
