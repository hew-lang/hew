[[case]]
id = "projected-tuple-owner-active-projected-tuple-owner-cancel"
intent = "Ownership oracle for reading a heap leaf out of a still-live tuple owner. Shape `projected_tuple_owner_cancel` (projected owner paths are exactly once under malloc scribble) runs to its exact outcome with no leak, double free or use after free under ASan/LSan."
source = "cases/projected-tuple-owner-active-projected-tuple-owner-cancel.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 60

[case.expected]
stdout = ""
exit = 0
