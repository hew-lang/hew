fn main() {
    let a: Option<i64> = Option.Some(5);
    let b: Option<i64> = Option.Some(6);
    let v: Vec<Option<i64>> = Vec.new();
    v.push(a);
    v.push(b);
    for e in v {
        match e { Option.Some(e_v) => println(f"some {e_v}"), None => println("none"), }
    }
}
