// The retired Rust-style turbofish spelling must produce the cutover diagnostic
// with the equivalent dotted Hew spelling.
//
// Expected: compile error (exit non-zero from `hew check`)
fn main() -> i64 {
    let _v = Vec::<i64, i32>::new();
    0
}
