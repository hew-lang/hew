[[case]]
id = "match-payload-reassign-aggregate-payload"
intent = "Projected-payload move-out UAF / double-free oracle (issue #2523). Shape `aggregate_payload` (aggregate payload move out single free) runs to its exact outcome with no leak, double free or use after free under ASan/LSan."
source = "cases/match-payload-reassign-aggregate-payload.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 60

[case.expected]
stdout = ""
exit = 9
