[[case]]
id = "example-algo-longest_common_prefix"
intent = "The published example compiles, runs and prints exactly what it advertises."
source = "../../examples/algos/longest_common_prefix.hew"
suites = ["acceptance"]
timeout_seconds = 30

[case.env]
HEW_WORKERS = "4"

[case.expected]
stdout_file = "../../examples/algos/longest_common_prefix.expected"
exit = 0
