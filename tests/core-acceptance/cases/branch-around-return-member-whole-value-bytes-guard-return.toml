[[case]]
id = "branch-around-return-member-whole-value-bytes-guard-return"
intent = "Branch-around returned-member leak — slope oracles plus poisoned-allocator exactly-once pins. Shape `whole_value_bytes_guard_return_high` (whole value bytes guard return no per call leak slope) runs to its exact outcome with no leak, double free or use after free under ASan/LSan."
source = "cases/branch-around-return-member-whole-value-bytes-guard-return.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 60

[case.expected]
stdout = ""
exit = 0
