[[case]]
id = "match_record_literal_predicate"
intent = "The fixture runs to completion and prints exactly its recorded output."
source = "../vertical-slice/accept/match_record_literal_predicate.hew"
suites = ["acceptance"]
timeout_seconds = 30

[case.env]
HEW_WORKERS = "4"

[case.expected]
stdout = "107\n7\n"
exit = 0
