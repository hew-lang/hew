// Negative test: monitor() should be rejected on WASM
actor Worker {
    receive fn ping() {
        println("pong");
    }
}

fn main() {
    let w = spawn Worker;
    let _m = monitor(w);
    w.ping();
}
