fn factorial(n: int) -> int {
    var result = 1;
    for i in 1..(n + 1) {
        result = result * i;
    }
    result
}

const ITERS: int = 1000000;

fn main() {
    var sum = 0;
    for i in 0..ITERS {
        sum = sum + factorial(20);
    }
    println(ITERS);
    println(sum);
}
