enum PayloadOption {
    Value(int);
    Empty;
}

fn main() {
    let a = Value(42);
    let b = Empty;
    match a {
        Value(x) => println(x),
        Empty => println(0),
    }
    match b {
        Value(x) => println(x),
        Empty => println(-1),
    }
}
