[[case]]
id = "std_panic_wrappers_success"
intent = "The fixture runs to completion and exits 0."
source = "../vertical-slice/accept/std_panic_wrappers_success.hew"
suites = ["acceptance"]
timeout_seconds = 30

[case.env]
HEW_WORKERS = "4"

[case.expected]
stdout = ""
exit = 0
