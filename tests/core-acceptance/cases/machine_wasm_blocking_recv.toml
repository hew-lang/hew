[[case]]
id = "machine_wasm_blocking_recv"
intent = "W4.002 reject fixture: `.recv()` inside a machine transition body must be rejected at HIR-lower time when compiling for wasm32, exercising the Item::Machine arm of `check_wasm_blocking_recv_gate` added in this change."
source = "../vertical-slice/reject/machine_wasm_blocking_recv.hew"
suites = ["acceptance"]
timeout_seconds = 30
kind = "reject"

[[case.expected.diagnostics]]
code = "MachineExhaustivenessError"
line = 19
column = 15
message = "machine evaluator is not demonstrably pure: `Result<(Sender<i64>, Receiver<i64>), string>` can carry external identity, effects or unclassified payloads"
