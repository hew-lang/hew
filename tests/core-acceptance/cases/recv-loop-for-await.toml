[[case]]
id = "recv-loop-for-await"
intent = "Per-iteration recv-result drop on the loop back-edge. Shape `for_await_low` (for await recv string loop no per frame leak slope) runs to its exact outcome with no leak, double free or use after free under ASan/LSan."
source = "cases/recv-loop-for-await.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 60

[case.expected]
stdout = "got\ngot\ngot\n"
exit = 0
