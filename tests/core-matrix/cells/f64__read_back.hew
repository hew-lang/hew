fn main() {
    let x: f64 = 3.5;
    var v: Vec<f64> = Vec.new();
    v.push(x);
    match v.get(0) {
        Option.Some(e) => {
            println(f"{e}");
        },
        Option.None => println("missing"),
    }
}
