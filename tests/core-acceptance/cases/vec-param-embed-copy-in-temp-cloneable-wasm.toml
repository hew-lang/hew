[[case]]
id = "vec-param-embed-copy-in-temp-cloneable-wasm"
intent = "Prepared parameter carriers embedded in anonymous Vec COPY-IN source temps. Shape `cloneable_wasm32` (enum text borrows balance on native and wasm) runs to its exact outcome with no leak, double free or use after free under ASan/LSan."
source = "cases/vec-param-embed-copy-in-temp-cloneable-wasm.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 60

[case.expected]
stdout = ""
exit = 7
