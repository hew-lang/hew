fn main() {
    let k: f64 = 3.5;
    let m: HashMap<f64, i64> = HashMap.new();
    m.insert(k, 9);
    match m.get(k) {
        .Some(v) => println(f"got {v}"),
        .None => println("missing"),
    }
}
