[[case]]
id = "wire_cbor_bytes"
intent = "The fixture runs to completion and exits 0."
source = "../vertical-slice/accept/wire_cbor_bytes.hew"
suites = ["acceptance"]
timeout_seconds = 30

[case.env]
HEW_WORKERS = "4"

[case.expected]
stdout = ""
exit = 0
