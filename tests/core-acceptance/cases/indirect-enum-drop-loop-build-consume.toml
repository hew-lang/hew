[[case]]
id = "indirect-enum-drop-loop-build-consume"
intent = "Indirect-enum heap-node drop leak oracle. Shape `loop_build_consume_df` (indirect enum loop build consume leak slope below tolerance, indirect enum loop build consume no corruption under malloc scribble) runs to its exact outcome with no leak, double free or use after free under ASan/LSan."
source = "cases/indirect-enum-drop-loop-build-consume.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 60

[case.expected]
stdout = "ok"
exit = 0
