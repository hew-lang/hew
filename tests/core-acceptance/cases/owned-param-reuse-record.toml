[[case]]
id = "owned-param-reuse-record"
intent = "Leak oracle for caller-side owned by-value param drop under REUSE. Shape `owned_param_reuse_record_high` (record param reuse does not double free, record param reuse leak slope below tolerance) runs to its exact outcome with no leak, double free or use after free under ASan/LSan."
source = "cases/owned-param-reuse-record.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 60

[case.expected]
stdout = ""
exit = 0
