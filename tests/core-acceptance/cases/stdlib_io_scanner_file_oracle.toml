[[case]]
id = "stdlib_io_scanner_file_oracle"
intent = "The fixture runs to completion and prints exactly its recorded output."
source = "../vertical-slice/accept/stdlib_io_scanner_file_oracle.hew"
suites = ["acceptance"]
timeout_seconds = 30

[case.env]
HEW_WORKERS = "4"

[case.expected]
stdout = "first=red\nblank=\nthird=blue\neof=<eof>\nmissing=NotFound(2)\n"
exit = 0
