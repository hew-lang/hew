[[case]]
id = "bytes_slice_open_right"
intent = "An open-right byte-slice `b[2..]` yields the trailing bytes and its length is the exit code."
source = "../vertical-slice/accept/bytes_slice_open_right.hew"
suites = ["acceptance"]
timeout_seconds = 30

[case.env]
HEW_WORKERS = "4"

[case.expected]
stdout = "cdef\n"
exit = 4
