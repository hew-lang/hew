[[case]]
id = "heap-owning-record-in-tuple-enum-composite-record-payload"
intent = "Constant-leak oracle for a heap-owning record reached as a TUPLE MEMBER. Shape `enum_composite_record_payload` (enum composite record payload no leak) runs to its exact outcome with no leak, double free or use after free under ASan/LSan."
source = "cases/heap-owning-record-in-tuple-enum-composite-record-payload.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 60

[case.expected]
stdout = ""
exit = 0
