[[case]]
id = "mailbox_bounded_drop_new"
intent = "A bounded mailbox with `DropNew` overflow accepts fewer messages than the flood it is sent."
source = "../vertical-slice/accept/mailbox_bounded_drop_new.hew"
suites = ["acceptance"]
timeout_seconds = 30

[case.env]
HEW_WORKERS = "4"

[case.expected]
stdout = ""
exit = 0
