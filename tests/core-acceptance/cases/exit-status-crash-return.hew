// Returning from `main` after an unrecovered crash reports the fault, not 0.
actor Worker {
    receive fn boom() { panic("worker failed"); }
}

fn main() {
    let w = spawn Worker();
    let _ = w.boom();
    println("returned");
}
