name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  checks: write
  pull-requests: read
  security-events: write

env:
  # Pinned LLVM upstream release tag, used by hew-codegen-rs (inkwell ->
  # llvm-sys-221) build script. Bumps must be reflected in
  # .github/actions/setup-llvm/action.yml (asset SHA256 pins) too.
  LLVM_VERSION: "22.1.5"
  # Pinned wasmtime release tag. Every job that provisions a WASI runner
  # downloads its per-platform asset from THIS tag. It is pinned, and pinned in
  # one place, for two reasons: resolving "latest" per job (as the Linux job
  # did) or taking whatever a package manager ships (as macOS did with brew)
  # let the three platforms straddle different wasmtime releases inside a
  # single run, so a WASI behaviour difference could be read as a platform
  # difference; and an unpinned runtime changes the tested surface without a
  # commit. Assets used: x86_64-linux.tar.xz, aarch64-macos.tar.xz,
  # x86_64-windows.zip.
  WASMTIME_VERSION: "v47.0.2"
  CARGO_TERM_COLOR: always

jobs:
  # ─────────────────────────────────────────────────────────────────────────
  # Path change detection — gates Windows/macOS and playground jobs
  # ─────────────────────────────────────────────────────────────────────────
  changes:
    name: Detect changes
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    outputs:
      # platform_smoke drives the Windows/macOS build+smoke required checks;
      # platform_full additionally promotes those platform jobs to the full
      # workspace nextest when a change can touch native linking / ABI /
      # toolchain. playground drives the browser WASM job.
      # The lightweight docs/scripts gate is always reported, and its steps
      # select coverage from these two outputs.
      platform_smoke: ${{ steps.filter.outputs.platform_smoke }}
      platform_full: ${{ steps.filter.outputs.platform_full }}
      playground: ${{ steps.filter.outputs.playground }}
      docs: ${{ steps.filter.outputs.docs }}
      scripts: ${{ steps.filter.outputs.scripts }}
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10  # v6.0.3

      - uses: dorny/paths-filter@fbd0ab8f3e69293af611ebaee6363fc25e6d187d  # v4.0.1
        id: filter
        with:
          filters: |
            # Rule 4 (platform parity, weak set): any Rust change or CI/toolchain
            # change runs the platform build + smoke on Windows/macOS. A Rust change
            # anywhere can change native linking, paths, or process behaviour, so
            # platform build/link coverage stays fail-closed.
            platform_smoke:
              - '**/*.rs'
              - 'tests/**'
              - 'std/**'
              - 'examples/**'
              - 'hew-sandbox-vm/**'
              - 'hew-sandbox-wasm/**'
              - 'hew-runtime-testkit/**'
              - 'hew-testutil/**'
              - 'tools/**'
              - 'xtask/**'
              - '.config/nextest.toml'
              - '.cargo/**'
              - 'rust-toolchain.toml'
              - 'Cargo.toml'
              - '**/Cargo.toml'
              - 'Cargo.lock'
              - '**/Cargo.lock'
              - 'Makefile'
              # The public-stdlib execution-proof mapping is a release gate,
              # not a docs-only script: changes to either authority must run
              # the code-path step that invokes its Make target below.
              - 'scripts/stdlib-execution-proof.sh'
              - 'scripts/stdlib-execution-proofs.tsv'
              - '.github/workflows/**'
              - '.github/actions/**'
            # Rule 4 (platform parity, strong set): native linking / LLVM / C ABI
            # / runtime scheduling / process / filesystem / networking / codegen
            # paths force the FULL workspace nextest on Windows/macOS (not just
            # smoke). Toolchain/dependency changes also force full, since they can
            # shift the linked surface on any platform. Anything narrower than the
            # smoke set but in this set runs full; everything else runs smoke.
            platform_full:
              - 'hew-runtime/**'
              - 'hew-lib/**'
              - 'hew-cabi/**'
              - 'hew-codegen-rs/**'
              - 'hew-compile/**'
              - 'hew-cli/**'
              - 'adze-cli/**'
              - 'hew-capability-gen/**'
              - 'std/net/**'
              - '.cargo/**'
              - 'rust-toolchain.toml'
              - 'Cargo.toml'
              - '**/Cargo.toml'
              - 'Cargo.lock'
              - '**/Cargo.lock'
              - '.github/actions/setup-llvm/**'
              - '.github/actions/setup-msvc/**'
              - '.github/actions/setup-rust-build/**'
              - '.github/workflows/ci.yml'
            playground:
              - 'hew-wasm/**'
              - 'hew-analysis/**'
              - 'hew-parser/**'
              - 'hew-types/**'
              - 'hew-lexer/**'
              - 'hew-lsp/tests/fixtures/**'
              - 'examples/playground/**'
              - 'scripts/gen-playground-manifest.py'
              - 'Cargo.toml'
              - 'Cargo.lock'
              - 'Makefile'
              - '.github/workflows/ci.yml'
            docs:
              - 'docs/**'
            scripts:
              - 'scripts/**'
              - '.github/workflows/freebsd.yml'
              - '.github/workflows/release-gate.yml'
              - '.github/workflows/release.yml'

  # ─────────────────────────────────────────────────────────────────────────
  # Lightweight docs/scripts coverage — required signal for narrow PRs.
  # ─────────────────────────────────────────────────────────────────────────
  docs-and-scripts:
    name: Docs & scripts gates
    needs: changes
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    steps:
      - name: No-op when docs and scripts are unchanged
        if: needs.changes.outputs.docs != 'true' && needs.changes.outputs.scripts != 'true'
        run: echo "No docs or scripts changes detected; lightweight gates are not needed."

      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10  # v6.0.3
        if: needs.changes.outputs.docs == 'true' || needs.changes.outputs.scripts == 'true'

      - name: Install LLVM ${{ env.LLVM_VERSION }}
        if: needs.changes.outputs.docs == 'true'
        uses: ./.github/actions/setup-llvm
        with:
          version: ${{ env.LLVM_VERSION }}

      - uses: ./.github/actions/setup-rust-build
        if: needs.changes.outputs.docs == 'true'
        with:
          cache-shared-key: docs-scripts-gates

      # >>> CI-PARITY-STEPS
      - name: Typecheck documentation fences
        if: needs.changes.outputs.docs == 'true'
        run: make test-doc-examples

      - name: Exercise O2 differential gate
        if: needs.changes.outputs.scripts == 'true'
        run: make o2-differential-selftest

      - name: Exercise doc-ratchet membership gate
        if: needs.changes.outputs.scripts == 'true'
        run: make doc-ratchet-selftest

      - name: Verify release workflow contract
        if: needs.changes.outputs.scripts == 'true'
        run: make test-release-workflow-contract

      # <<< CI-PARITY-STEPS

      # Redundant fast feedback for workflow/script-only changes. The
      # unconditional copy in the required Clippy & format job below is the
      # authoritative merge-blocking path.
      - name: Verify FreeBSD workflow contract
        if: needs.changes.outputs.scripts == 'true'
        run: make freebsd-workflow-contract-check

  # ─────────────────────────────────────────────────────────────────────────
  # Lint — fast feedback on code quality (always runs, ~1.5 min)
  # Produces SARIF output for GitHub Code Scanning inline annotations.
  # ─────────────────────────────────────────────────────────────────────────
  lint:
    name: Clippy & format
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10  # v6.0.3
        with:
          # fetch-depth: 0 required so origin/main is available for the
          # orchestration-token commit-message scan (--scan-commits range
          # origin/main..HEAD) that runs in the leak-scan step below.
          fetch-depth: 0

      - uses: ./.github/actions/setup-rust-build
        with:
          components: 'clippy, rustfmt'
          cache-shared-key: lint

      # hew-codegen-rs depends on inkwell -> llvm-sys-221, whose build
      # script requires LLVM_SYS_221_PREFIX or llvm-config on PATH for
      # `cargo clippy --workspace --tests` to compile the workspace.
      - name: Install LLVM ${{ env.LLVM_VERSION }}
        uses: ./.github/actions/setup-llvm
        with:
          version: ${{ env.LLVM_VERSION }}

      - name: Check formatting
        run: cargo fmt --all -- --check

      # Explicit acquisition is cacheable but never implicit in local lint.
      - name: Bootstrap pinned structural lint toolchain
        run: make structural-lint-bootstrap

      # >>> CI-PARITY-STEPS
      # This job is an active required status check on main. Keep the workflow
      # contract here unconditionally so nightly/release-gate drift cannot merge
      # even when the lightweight docs/scripts job is skipped or not required.
      - name: Verify FreeBSD workflow contract
        run: make freebsd-workflow-contract-check

      - name: Verify sanitizer gate wiring
        run: make check-sanitizer-gate

      - name: Verify gate reachability and documented make targets
        run: make check-gate-reachability

      - name: Verify every corpus floor has a live call site
        run: make corpus-floor-check

      - name: Run structural authority lint
        run: make structural-lint
      # <<< CI-PARITY-STEPS

      # Format-check std/ and examples/ .hew sources with the in-tree hew
      # formatter. The hew-fmt-check target builds the debug hew binary first;
      # the workspace is already compiled for clippy here, so this is just the
      # final-link cost. Without this, an unformatted .hew file passes CI (which
      # only checks Rust formatting) while failing local `make lint`.
      - name: Check .hew formatting
        run: make hew-fmt-check

      - name: Install SARIF tools
        run: |
          gh release download clippy-sarif-latest --repo psastras/sarif-rs \
            --pattern "clippy-sarif-x86_64-unknown-linux-gnu" --output clippy-sarif --clobber
          gh release download sarif-fmt-latest --repo psastras/sarif-rs \
            --pattern "sarif-fmt-x86_64-unknown-linux-gnu" --output sarif-fmt --clobber
          chmod +x clippy-sarif sarif-fmt
          mv clippy-sarif sarif-fmt /usr/local/bin/
        env:
          GH_TOKEN: ${{ github.token }}

      - name: Clippy
        run: |
          set -o pipefail
          cargo clippy --workspace --tests --message-format=json -- -D warnings \
            | clippy-sarif \
            | tee clippy-results.sarif \
            | sarif-fmt

      - name: Verify JIT host ABI classification
        run: make verify-ffi test-verify-ffi test-python310-toml-compat

      - name: Upload Clippy SARIF
        if: always() && hashFiles('clippy-results.sarif') != ''
        uses: github/codeql-action/upload-sarif@c35d1b164463ee62a100735382aaaa525c5d3496  # codeql-bundle-v2.25.6 (v4)
        with:
          sarif_file: clippy-results.sarif
          category: clippy
          wait-for-processing: true

      - name: Runtime poison-safe lint
        run: make runtime-poison-safe-lint

      - name: WASM-TODO backlog-authority lint
        # Parse the offline manifest authority, exercise the synthetic teeth,
        # then validate every actionable source marker against a stable backlog
        # id. No GitHub/API state is needed.
        run: make lint-wasm-todo

      - name: Codegen carried-identity gate
        # Assert the codegen carried-identity invariants. Also a `make lint`
        # prerequisite that no CI job invoked.
        run: make codegen-carried-identity-gate

      - name: Orchestration-token leak scan
        # Scan tracked source files and commit-message bodies of all PR commits
        # for orchestration tokens (lane IDs, Q-tags, .tmp/ paths).  Placed outside
        # CI-PARITY-STEPS so the preflight↔CI parity assertion is unaffected.
        # origin/main is available because the checkout above uses fetch-depth: 0.
        run: make leak-scan

      - name: Preflight↔CI parity self-test
        # Assert that the dispatcher's fallback lane mirrors the CI build-and-test
        # step list.  A new unconditional CI step not reflected in CI_REQUIRED_CHECKS
        # or CI_BUILD_AND_TEST_STEPS fails here before it can silently skip in local
        # preflights.
        #
        # The self-test runs first: it proves the parity checker still fails on a
        # synthetic drift. Without it the checker could degrade into a no-op and
        # the parity step below would pass by vacuum.
        run: |
          make preflight-parity-selftest
          scripts/check-preflight-ci-parity.sh

      - name: ll-byte-identity normaliser self-test
        # Prove the ll-diff oracle's normaliser still catches real IR changes
        # (string-content edits, numeric-const NAME changes) while staying
        # transparent to pool-id reorderings.  No compiler build required —
        # exercises scripts/ll-byte-identity.sh against synthetic .ll snippets.
        # Guards the ll-diff gate itself from silently degrading into a no-op.
        run: make ll-identity-selftest

      - name: Sandbox parity coverage check
        # Assert every VM-dependent hew-sandbox-wasm test binary (one
        # containing a function that spawns the hew-sandbox-vm Node runner)
        # is excluded WHOLE from the generic nextest default-filter AND has
        # every one of its tests run by the provisioned `make sandbox-parity`
        # gate. Catches a new VM-dependent binary landing in only one of
        # those two states -- failing unprovisioned generic runs on every
        # platform, or silently never running on a provisioned one.
        # Classification is binary-level, not per-test (a same-file call
        # graph cannot safely attribute VM-dependence to individual tests).
        # Runs the checker's own self-test first
        # (test-sandbox-parity-coverage-check).
        run: make sandbox-parity-coverage-check

  # ─────────────────────────────────────────────────────────────────────────
  # License gate — enforce the deny.toml allow-list so no copyleft or unknown
  # dep silently enters the shipping graph, and assert THIRD-PARTY-LICENSES is
  # current relative to Cargo.lock. Runs on every push/PR; fast (~30 s).
  # ─────────────────────────────────────────────────────────────────────────
  license-check:
    name: License policy (cargo deny + THIRD-PARTY-LICENSES freshness)
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10  # v6.0.3

      - uses: ./.github/actions/setup-rust-build
        with:
          cache-shared-key: deny

      - name: Install cargo-deny
        run: cargo install cargo-deny --locked --version 0.19.6

      - name: Check license policy
        run: cargo deny check licenses

      - name: Check bans and sources
        run: cargo deny check bans sources

      - name: Check security advisories
        run: cargo deny check advisories

      - name: Install cargo-about
        run: cargo install cargo-about --locked --version 0.9.0 --features cli

      - name: Verify THIRD-PARTY-LICENSES is current
        run: make licenses-check

  # ─────────────────────────────────────────────────────────────────────────
  # Browser/playground smoke — repo-local browser tooling coverage only.
  # Runs full hew-wasm suite (lib + fixture-coverage integration), checks
  # manifest freshness, and builds the browser WASM artifact.
  # ─────────────────────────────────────────────────────────────────────────
  playground-wasm-build:
    name: Playground WASM build
    needs: changes
    if: needs.changes.outputs.playground == 'true'
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10  # v6.0.3

      - uses: ./.github/actions/setup-rust-build
        with:
          cache-shared-key: playground-wasm

      - name: Install wasm-pack v0.13.1
        run: |
          set -euo pipefail
          WASM_PACK_VERSION=0.13.1
          TARBALL="wasm-pack-v${WASM_PACK_VERSION}-x86_64-unknown-linux-musl.tar.gz"
          curl -fsSL "https://github.com/rustwasm/wasm-pack/releases/download/v${WASM_PACK_VERSION}/${TARBALL}" \
            -o "/tmp/${TARBALL}"
          tar -xzf "/tmp/${TARBALL}" -C /tmp
          echo "/tmp/wasm-pack-v${WASM_PACK_VERSION}-x86_64-unknown-linux-musl" >> "$GITHUB_PATH"
          /tmp/wasm-pack-v${WASM_PACK_VERSION}-x86_64-unknown-linux-musl/wasm-pack --version

      - name: Check sandbox fixtures are current
        # The committed sandbox fixtures are generated from in-tree sources; a
        # stale fixture makes the parity gate assert against the wrong baseline.
        run: make sandbox-fixtures-check

      - name: Check repo-local playground browser/tooling smoke
        run: make playground-check

  # ─────────────────────────────────────────────────────────────────────────
  # Build & test — Rust workspace
  # Produces JUnit XML for GitHub Actions test reporting.
  # ─────────────────────────────────────────────────────────────────────────
  build-and-test:
    name: Build & test (Linux)
    needs: changes
    runs-on: ubuntu-24.04
    # Was 60. Successful runs on main land at 51-56 min wall time (checked
    # 2026-07-04 across 5 recent green runs), leaving only 4-9 min of
    # headroom against normal runner-to-runner variance -- PRs #2396 and
    # #2397 both got CANCELLED at exactly 60m00-16s. 90 min restores real
    # headroom without masking a genuine regression (see the diagnosis in
    # the commit that introduced this change).
    timeout-minutes: 90
    env:
      # mold is installed by the "Install build tools" step below.
      # Use gcc's -fuse-ld=mold flag (no clang-as-driver dependency) to
      # activate mold for all Rust compilations in this job.  Scoped here
      # rather than .cargo/config.toml so other workflows (release-gate,
      # coverage-nightly, sanitizers) that run on runners without mold are
      # unaffected.
      CARGO_TARGET_X86_64_UNKNOWN_LINUX_GNU_RUSTFLAGS: "-C link-arg=-fuse-ld=mold"
      # platform_smoke gates the whole Linux build+test required check, same
      # boundary Windows/macOS already use (ci.yml:547-548,647-648) — it
      # covers every glob that can affect Linux behaviour ('**/*.rs',
      # 'tests/**', 'std/**', 'examples/**', 'Cargo.*', 'Makefile', workflow
      # files), so a docs/comment-only PR (matching none of those globs)
      # skips this job's ~51-90 min of work. STEP-LEVEL gating, not a
      # job-level `if:` — a job whose `if:` is false reports skipped/missing,
      # not a satisfied required context (LESSONS.md
      # ci-required-gate-sequencing), so every step below carries its own
      # `if: env.RUN_CODE_PATH == 'true'` and the no-op step below still
      # reports a green required check on skip.
      RUN_CODE_PATH: ${{ needs.changes.outputs.platform_smoke }}
    steps:
      - name: No-op for docs-only changes
        if: env.RUN_CODE_PATH != 'true'
        run: echo "No code changes detected; skipping heavy Linux CI while satisfying the required check."

      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10  # v6.0.3
        if: env.RUN_CODE_PATH == 'true'

      - name: Free runner disk
        if: env.RUN_CODE_PATH == 'true'
        # This job installs LLVM 22.1.5 (~1.5 GB), builds the release hew-runtime
        # + hew-lsp binaries, the wasm32-wasip1 runtime, and runs the full debug
        # workspace nextest suite — all on one ubuntu-24.04 runner (~14 GB free).
        # The runner died of ENOSPC twice before this step was added (PR #1994).
        # Code coverage moved to the nightly workflow (c24721fa) so target/llvm-cov-target/
        # no longer accumulates here; the preinstalled-toolchain purge below still
        # recovers the headroom the LLVM download + workspace nextest target/ needs.
        run: |
          sudo rm -rf /usr/local/lib/android /usr/share/dotnet /opt/ghc /usr/local/.ghcup \
            /opt/hostedtoolcache/CodeQL /usr/local/share/boost /usr/share/swift || true
          sudo docker system prune -af >/dev/null 2>&1 || true
          df -h /

      - name: Install build tools
        if: env.RUN_CODE_PATH == 'true'
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y -qq libssl-dev pkg-config mold zlib1g-dev libzstd-dev

      - name: Install MQTT E2E clients
        if: env.RUN_CODE_PATH == 'true'
        # mqtt-broker-e2e drives the Hew broker with real MQTT 3.1.1 clients;
        # do not let an absent client turn that behavioural oracle into an
        # implicit local-only target.
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y -qq mosquitto-clients
          command -v mosquitto_pub
          command -v mosquitto_sub

      - name: Install LLVM ${{ env.LLVM_VERSION }}
        if: env.RUN_CODE_PATH == 'true'
        uses: ./.github/actions/setup-llvm
        with:
          version: ${{ env.LLVM_VERSION }}

      - uses: ./.github/actions/setup-rust-build
        if: env.RUN_CODE_PATH == 'true'
        with:
          tools: nextest
          cache-shared-key: build-test-linux

      - name: Build Rust runtime and frontend (release)
        if: env.RUN_CODE_PATH == 'true'
        run: cargo build -p hew-runtime -p hew-lsp --release

      - name: Smoke test hew-lsp
        if: env.RUN_CODE_PATH == 'true'
        # Drive the shipped release binary through a real initialize → didOpen →
        # publishDiagnostics session, not just --version. HEW_LSP_BIN points the
        # protocol test at the artifact built above.
        run: |
          ./target/release/hew-lsp --version
          HEW_LSP_BIN="$PWD/target/release/hew-lsp" \
            cargo nextest run -p hew-lsp --test protocol_smoke

      - name: Build WASM runtime
        if: env.RUN_CODE_PATH == 'true'
        run: |
          rustup target add wasm32-wasip1
          cargo build -p hew-runtime --target wasm32-wasip1 --no-default-features --release

      - name: Install wasmtime
        if: env.RUN_CODE_PATH == 'true'
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          WASMTIME_DIR="wasmtime-${WASMTIME_VERSION}-x86_64-linux"
          gh release download "$WASMTIME_VERSION" --repo bytecodealliance/wasmtime \
            --pattern "${WASMTIME_DIR}.tar.xz" --clobber
          tar -xJf "${WASMTIME_DIR}.tar.xz"
          echo "$PWD/${WASMTIME_DIR}" >> "$GITHUB_PATH"
          "./${WASMTIME_DIR}/wasmtime" --version

      - name: Run WASM runtime actual-target libtests
        if: env.RUN_CODE_PATH == 'true'
        env:
          CARGO_TARGET_WASM32_WASIP1_RUNNER: wasmtime run
        run: cargo test -p hew-runtime --target wasm32-wasip1 --no-default-features --lib

      # Default features include "full" (encryption + profiler + quic),
      # so all 33 QUIC transport tests run without an explicit flag.
      # hew-wasm browser/tooling smoke stays isolated in playground-wasm-build.
      # hew-cabi is excluded — its #[no_mangle] symbols conflict with
      # hew-runtime when both link into a test binary.
      #
      # >>> CI-PARITY-STEPS
      # The steps from here to <<< CI-PARITY-STEPS are the build-and-test gates
      # that scripts/check-preflight-ci-parity.sh asserts are mirrored in the
      # dispatcher's CI_REQUIRED_CHECKS array.  When adding a new unconditional
      # gating step, add it inside this block AND update CI_REQUIRED_CHECKS in
      # scripts/ci-preflight-dispatcher.sh so the local preflight predicts CI.
      # The parity self-test in the lint job parses this block directly — the
      # maintained CI_BUILD_AND_TEST_STEPS list in the parity script is now
      # derived from here instead of a hand-maintained copy.
      - name: Run Rust workspace tests
        if: env.RUN_CODE_PATH == 'true'
        # line-tables-only: the release build + wasm build + full debug
        # workspace nextest run together leave the runner disk-marginal
        # (ENOSPC twice on this exact job — see PR #1994). Full debug info
        # is the dominant target/ cost and nothing in CI reads it.
        env:
          CARGO_PROFILE_DEV_DEBUG: "line-tables-only"
          CARGO_INCREMENTAL: "0"
        # `make test` owns the complete native/WASI/libhew prerequisite graph
        # and the workspace nextest policy. Keeping that edge real makes the
        # hosted suite exercise the same fresh artifacts as local preflight.
        run: make test

      - name: Run compiler pipeline and local opaque-resource lifecycle evidence
        if: env.RUN_CODE_PATH == 'true'
        # The focused compiler ladder owns the source-derived lifecycle matrix;
        # keep this explicit CI edge so neither target can become local-only.
        run: make test-compiler-pipeline

      - name: Run external-network opaque-resource lifecycle evidence
        if: env.RUN_CODE_PATH == 'true'
        # These are bounded loopback/network lifecycle proofs intentionally
        # deferred by the local profile, not live-service smoke tests.
        run: make test-opaque-resource-lifecycle-matrix-external

      - name: Run C-ABI crate tests
        if: env.RUN_CODE_PATH == 'true'
        # hew-cabi is excluded from every workspace run above because its
        # cfg(test) runtime stubs collide with hew-runtime's real symbols at
        # link time.  That exclusion is a link workaround; this step is the
        # coverage.  Without it the FFI ownership-contract crate — including
        # the HewTypeOwnershipKind discriminant-stability test that codegen's
        # emitted layout globals depend on — runs nowhere in CI.
        run: make test-cabi

      - name: Run vertical-slice end-to-end oracle
        if: env.RUN_CODE_PATH == 'true'
        run: make test-vertical-slice

      - name: Run cross-module package-import oracle
        if: env.RUN_CODE_PATH == 'true'
        run: make test-pkg-import

      - name: Run fuzz-to-run completeness oracle (ratcheted)
        if: env.RUN_CODE_PATH == 'true'
        # Compiles and runs every checker-valid program in the regressions
        # corpus and the vertical-slice accept fixtures.  Known failures are
        # tracked in tests/fuzz-oracle/expected-failures.txt (ratchet contract:
        # unexpected pass OR unexpected fail → exit 1).  Raw cargo-fuzz corpus
        # is excluded from CI (nondeterministic); run locally with
        # `make fuzz-oracle FUZZ_ORACLE_FULL=1`.
        run: make fuzz-oracle

      - name: Run Hew language test suite (ratcheted)
        if: env.RUN_CODE_PATH == 'true'
        # Runs tests/hew/ through scripts/hew-suite-ratchet.sh.
        # Known failures are tracked in scripts/hew-suite-expected-failures.txt;
        # any unexpected failure OR unexpected pass (ratchet forward) exits 1.
        # When all entries in the expected-failures file are cleared, this step
        # gates on a fully-green suite with no special handling.
        #
        # HEW_O0_OUTCOMES_FILE captures this run's O0 outcome set so the
        # differential-exec gate below reuses it instead of re-running the
        # byte-identical `hew test tests/hew/` invocation at O0 a second time.
        env:
          HEW_O0_OUTCOMES_FILE: ${{ runner.temp }}/hew-o0-outcomes.txt
        run: make test-hew-ratchet

      - name: Run -O0-vs-O2 differential-exec parity gate
        if: env.RUN_CODE_PATH == 'true'
        # Every compiled `.hew` program must behave identically at -O0 and -O2;
        # a divergence is a miscompile in the LLVM middle-end optimizer pipeline.
        # Runs the Linux/x86_64 portion of the cross-platform parity matrix
        # tracked in #2104; Windows-MSVC and wasm32 remain separate platform gates.
        #
        # Reuses the O0 outcome set the ratchet step above just captured
        # (same env var) instead of re-running the O0 leg — the redundant
        # re-run was ~9-10 min of the ~19 min this step used to cost.
        env:
          HEW_O0_OUTCOMES_FILE: ${{ runner.temp }}/hew-o0-outcomes.txt
        run: make test-o2-differential

      - name: Run O2-differential gate self-test
        if: env.RUN_CODE_PATH == 'true'
        # Proves the differential gate itself has teeth: three independently
        # failable cases (a forced O0/O2 outcome divergence, an identical
        # baseline, and a runner-crash no-summary case) run against a stub
        # binary, asserting the gate exits non-zero on divergence and zero on
        # a matching outcome set. Guards against a future refactor silently
        # reducing the gate to a no-op.
        run: make o2-differential-selftest

      - name: Run stdlib type-check suite (ratcheted)
        if: env.RUN_CODE_PATH == 'true'
        # Type-checks every std/*.hew file through scripts/stdlib-ratchet.sh.
        # Known failures tracked in scripts/stdlib-expected-failures.txt.
        run: make test-stdlib-ratchet

      - name: Verify public stdlib execution proofs
        if: env.RUN_CODE_PATH == 'true'
        # Enforces the README public-index → executed-fixture mapping, including
        # parser-backed import evidence and the manifest's exact corpus floor.
        run: make test-stdlib-execution-proofs

      - name: Run doc-fence typecheck suite (ratcheted)
        if: env.RUN_CODE_PATH == 'true'
        # Extracts every ```hew fence from docs/ and runs `hew check` on each.
        # Known failures are tracked in scripts/doc-test-expected-failures.txt;
        # any unexpected failure OR unexpected pass (ratchet forward) exits 1.
        # Skip-annotated fences (<!-- doctest: skip --> or NYI callout) are
        # excluded from the check to allow aspirational spec fences.
        # When the expected-failures list is empty, this gate is fully green.
        run: make test-doc-examples

      - name: Setup Node.js for sandbox parity
        if: env.RUN_CODE_PATH == 'true'
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e  # v6.4.0
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: hew-sandbox-vm/package-lock.json

      - name: Run sandbox VM parity
        if: env.RUN_CODE_PATH == 'true'
        # make sandbox-parity provisions hew-sandbox-vm's npm dependencies
        # (sandbox-vm-deps) and then runs all four VM-dependent test binaries
        # (parity, parity_ratchet, playground, ios_subset) directly via plain
        # `cargo test`. Those binaries are excluded from the generic nextest
        # default-filter (.config/nextest.toml) on every platform, so this is
        # the one place their real native-vs-VM behavior is exercised.
        run: make sandbox-parity

      - name: Verify checked-MIR golden corpus
        if: env.RUN_CODE_PATH == 'true'
        # Re-dumps every examples/v05/checked-mir/*.hew fixture through
        # --dump-mir and byte-diffs against the committed goldens.  A drift
        # here means either the renderer changed without regenerating the
        # corpus (make checked-mir-golden) or a non-determinism bug crept
        # into dump_mir.  The hew binary is already built by this point.
        run: make checked-mir-verify

      - name: Execute checked-MIR corpus
        if: env.RUN_CODE_PATH == 'true'
        # Builds and runs every examples/v05/checked-mir/*.hew fixture and
        # diffs exit status + stdout against its committed .expected sibling.
        # checked-mir-verify above only diffs dumped text; it never loads the
        # program, so a fixture can segfault on every run while the goldens
        # stay byte-identical.  This step is the one that would have caught
        # that.  Runnability is read back from the compiler (raw MIR declares
        # main), and the expectation set is closed both ways.
        run: make checked-mir-run

      - name: Verify per-function .ll byte-identity corpus
        if: env.RUN_CODE_PATH == 'true'
        # Recompiles every tests/ll-oracle/corpus/*.hew fixture (native and
        # wasm32) and diffs the normalised per-function IR against the committed
        # goldens.  A drift here means emitted LLVM IR changed: either an
        # intentional codegen change that must regenerate the goldens in the
        # same commit (make ll-golden, diff justified in the body), or an
        # unintended regression.  This is a byte-identity gate — it stays green
        # only while codegen output is unchanged.  The hew binary is already
        # built by this point.
        run: make ll-diff

      - name: Run ux + progressive tutorial oracle
        if: env.RUN_CODE_PATH == 'true'
        # Replays every examples/ux and examples/progressive tutorial and
        # byte-compares against its .expected file, so a dialect regression
        # cannot ship past the tutorials users read first.
        run: make test-ux-examples

      - name: Run surface-example ledger gate
        if: env.RUN_CODE_PATH == 'true'
        # Runs the examples/surface corpus against its closed-both-ways
        # ledger: an ungated example that starts passing, or a gated one that
        # starts failing, both exit 1. The ledger was hardened after a leak
        # advisory shipped unnoticed; this step is what makes it bite.
        run: make test-surface-examples

      - name: Run package-manager consumer oracle
        if: env.RUN_CODE_PATH == 'true'
        # Publishes a local package, installs it with `hew package install`,
        # and asserts lock/materialization state plus exact `hew run` stdout
        # under an isolated HOME.
        run: make test-package-install

      - name: Run runtime tests without the QUIC/TLS/profiler stack
        if: env.RUN_CODE_PATH == 'true'
        # The workspace run above builds hew-runtime with default features.
        # This proves the crate still compiles and passes with those features
        # off — the configuration embedders without the heavy stack get.
        run: make test-runtime-unit

      - name: Run fuzz-oracle gate self-test
        if: env.RUN_CODE_PATH == 'true'
        # Proves the fuzz-oracle ratchet has teeth: forced unexpected-pass and
        # unexpected-fail cases must each exit non-zero.
        run: make fuzz-oracle-selftest

      - name: Run libhew link-race gate
        if: env.RUN_CODE_PATH == 'true'
        # Concurrent links against libhew.a must not observe a partially
        # written archive. Regression cover for the fresh-checkout race.
        run: make libhew-link-race-test

      - name: Run repo-wide hew check corpus sweep (ratcheted)
        if: env.RUN_CODE_PATH == 'true'
        # Runs `hew check` over every tracked .hew file (excluding intentional
        # reject fixtures already covered by test-vertical-slice/test-pkg-import
        # /fuzz-oracle).  Known failures tracked in
        # scripts/hew-corpus-expected-failures.txt; any unexpected failure (new
        # regression) OR unexpected pass (entry fixed — remove it from the list)
        # exits 1.  Catches the migrate-forward-miss class: a symbol rename or
        # type change landed in the compiler but a fixture file silently left
        # behind with the old call-site.
        run: make hew-check-all
      # <<< CI-PARITY-STEPS

      - name: Run MQTT broker end-to-end oracle
        if: env.RUN_CODE_PATH == 'true'
        # The client tools were provisioned explicitly above. This is a real
        # broker/client exchange, not the compile-only example coverage in the
        # workspace suite. It stays outside CI-PARITY-STEPS because a local
        # preflight cannot assume the external Mosquitto clients are installed.
        run: make mqtt-broker-e2e

      - name: Run hew observe functional harness
        if: env.RUN_CODE_PATH == 'true'
        run: make observe-functional-test

      - name: Upload test reports
        if: always() && env.RUN_CODE_PATH == 'true'
        uses: mikepenz/action-junit-report@3a81627bfac62268172037048872e8ebd4207e6d  # v6.4.1
        with:
          report_paths: target/nextest/ci/junit.xml
          check_name: Test Results (Linux)

  # Code coverage moved off the per-PR path: it was the flakiest required check
  # (cargo-llvm-cov instrumentation + the duplicated target/llvm-cov-target/
  # tree on top of LLVM install exited 100 / ENOSPC on sound PRs). The real
  # correctness tests run in "Build & test (Linux)"; coverage now runs nightly
  # in .github/workflows/coverage-nightly.yml (same llvm-cov command + report).

  # ─────────────────────────────────────────────────────────────────────────
  # Build & test — Windows
  # ─────────────────────────────────────────────────────────────────────────
  build-and-test-windows:
    name: Build & test (Windows)
    needs: changes
    runs-on: windows-2022
    timeout-minutes: 60
    env:
      # platform_smoke gates the whole Windows build+smoke required check;
      # platform_full additionally promotes the test step to the full workspace
      # nextest. Linux owns full correctness on every code change — Windows is a
      # platform build/link/smoke gate, with the full duplicated workspace
      # nextest reserved for platform/toolchain/ABI changes here and the nightly
      # cross-platform matrix.
      RUN_CODE_PATH: ${{ needs.changes.outputs.platform_smoke }}
      RUN_FULL_NEXTEST: ${{ needs.changes.outputs.platform_full }}
      # aws-lc-sys (rustls under hew-runtime, jsonwebtoken under hew-lib) wants
      # NASM on windows-msvc; the documented escape consumes the crate's
      # pregenerated assembly objects, so the test-time hew-lib bootstrap links
      # without a NASM toolchain. (Parity with release-gate.yml / release.yml.)
      AWS_LC_SYS_PREBUILT_NASM: "1"
      # Cap concurrent rustc invocations on the Windows runner to avoid OOM.
      # hew-codegen-rs/llvm.rs (~33k lines) consumes ~2-3 GB of RSS per rustc
      # process under the LLVM backend; with the default job count equal to the
      # runner's CPU count (4), peak concurrent RSS exceeds the runner's 16 GB
      # and the kernel kills rustc (STATUS_STACK_BUFFER_OVERRUN / 0xc0000409).
      # Two concurrent jobs fit comfortably (2 × 3 GB = 6 GB peak for codegen,
      # leaving headroom for the OS, LLVM headers, and the linker pass).
      # Linux and macOS runners have more headroom at their default parallelism
      # and are unaffected. Revisit when hew-codegen-rs/llvm.rs is split into
      # smaller modules, or when the Windows runner gains more memory.
      CARGO_BUILD_JOBS: "2"
    steps:
      - name: No-op for docs-only changes
        if: env.RUN_CODE_PATH != 'true'
        run: echo "No code changes detected; skipping heavy Windows CI while satisfying the required check."
        shell: pwsh

      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10  # v6.0.3
        if: env.RUN_CODE_PATH == 'true'

      - name: Setup MSVC environment
        # lld-link needs the MSVC and UCRT library paths exported by vcvarsall;
        # the LLVM prefix is configured in the following setup-llvm step.
        if: env.RUN_CODE_PATH == 'true'
        uses: ./.github/actions/setup-msvc

      - name: Install LLVM ${{ env.LLVM_VERSION }}
        if: env.RUN_CODE_PATH == 'true'
        uses: ./.github/actions/setup-llvm
        with:
          version: ${{ env.LLVM_VERSION }}

      - uses: ./.github/actions/setup-rust-build
        if: env.RUN_CODE_PATH == 'true'
        with:
          tools: nextest
          cache-shared-key: build-test-windows

      - name: Build Rust crates
        if: env.RUN_CODE_PATH == 'true'
        # hew-codegen-rs (inkwell -> llvm-sys-221) compiles end-to-end
        # because the upstream LLVM tarball is provisioned above.
        run: cargo build -p adze-cli -p hew-runtime -p hew-lsp -p hew-cli --release

      - name: Install WASI runner prerequisites
        # Windows had no equivalent of the Linux tarball / macOS brew install,
        # so `wasmtime` was absent here and every WASI-dependent hew-cli test
        # either failed with "`wasmtime` not found" or — via the deleted
        # try_require_wasi_runner — passed having asserted nothing. Provision
        # the runner from the same pinned WASMTIME_VERSION the other two jobs
        # use, and prove it runs inside this step: a broken install must fail
        # the job here, not resurface as "not found" inside a test.
        if: env.RUN_CODE_PATH == 'true'
        shell: pwsh
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          rustup target add wasm32-wasip1
          if ($LASTEXITCODE -ne 0) { throw "rustup target add wasm32-wasip1 failed" }

          $dir = "wasmtime-${env:WASMTIME_VERSION}-x86_64-windows"
          gh release download "${env:WASMTIME_VERSION}" --repo bytecodealliance/wasmtime --pattern "$dir.zip" --clobber
          if ($LASTEXITCODE -ne 0) { throw "downloading $dir.zip from bytecodealliance/wasmtime failed" }

          Expand-Archive -Path "$dir.zip" -DestinationPath . -Force
          $wasmtimeBin = Join-Path (Get-Location).Path $dir
          $wasmtimeExe = Join-Path $wasmtimeBin "wasmtime.exe"
          if (-not (Test-Path -LiteralPath $wasmtimeExe)) {
            throw "wasmtime.exe not found at $wasmtimeExe after extracting $dir.zip"
          }
          Add-Content -Path $env:GITHUB_PATH -Value $wasmtimeBin

          & $wasmtimeExe --version
          if ($LASTEXITCODE -ne 0) { throw "wasmtime installed but 'wasmtime --version' exited $LASTEXITCODE" }

          # wasm-ld comes from the LLVM install provisioned above; hew's WASM
          # link step shells out to it, so an unreachable wasm-ld is the same
          # class of silent gap as a missing runtime.
          $wasmLd = Get-Command wasm-ld -ErrorAction SilentlyContinue
          if (-not $wasmLd) { throw "wasm-ld not found on PATH (expected from the LLVM ${env:LLVM_VERSION} install)" }
          & $wasmLd.Source --version
          if ($LASTEXITCODE -ne 0) { throw "wasm-ld found at $($wasmLd.Source) but 'wasm-ld --version' exited $LASTEXITCODE" }

      - name: Build hew-runtime for wasm32-wasip1 (debug)
        # Mirrors the macOS job. The WASI tests run as debug binaries and
        # bootstrap_wasi_runner builds this archive on demand when it is
        # absent, so without a pre-build every parallel nextest process races
        # to cargo-build the same non-atomically written staticlib and wasm-ld
        # intermittently fails to open it.
        if: env.RUN_CODE_PATH == 'true'
        run: cargo build -p hew-runtime --target wasm32-wasip1 --no-default-features

      - name: Build hew-std for wasm32-wasip1 (debug)
        # Same race, same medicine: bootstrap_wasi_runner also builds
        # libhew_std.a on demand when it is missing.
        if: env.RUN_CODE_PATH == 'true'
        run: cargo build -p hew-std --target wasm32-wasip1

      - name: Run platform smoke tests
        # Default Windows path: prove the platform build links and the
        # platform-sensitive crates pass on windows-msvc, without re-running the
        # whole 9500-test workspace that Linux already owns. The smoke set is the
        # native/ABI/codegen/CLI surface where Windows-specific breakage lives
        # (linking, path separators, process spawning).
        # hew-cabi excluded — its #[no_mangle] symbols conflict with hew-runtime
        # when both link into a test binary.
        if: env.RUN_CODE_PATH == 'true' && env.RUN_FULL_NEXTEST != 'true'
        run: >-
          cargo nextest run
          -p hew-runtime
          -p hew-codegen-rs
          -p hew-compile
          -p hew-cli
          -p hew-lib
          --profile ci

      - name: Run full Rust workspace tests
        # Platform/toolchain/ABI change: run the full workspace nextest on
        # Windows so a native-linking or cross-platform regression is caught
        # per-PR, not only by the nightly matrix.
        # hew-wasm is excluded on every platform (profile.ci's default-filter
        # does the same) — the browser/tooling WASM surface has its own
        # playground-wasm-build job. hew-cabi has #[no_mangle] symbols that
        # conflict with hew-runtime when both link into a test binary; the
        # C-ABI step below is its coverage.
        # Default features include "full" (encryption + profiler + quic),
        # so all QUIC transport tests run without an explicit flag.
        if: env.RUN_CODE_PATH == 'true' && env.RUN_FULL_NEXTEST == 'true'
        run: >-
          cargo nextest run --workspace
          --exclude hew-wasm
          --exclude hew-cabi
          --profile ci

      - name: Run C-ABI crate tests
        # Standalone because the workspace run above must exclude hew-cabi.
        # Unconditional on the code path: the crate is 82 fast tests and it is
        # the only place the C-ABI ownership contract is exercised on Windows.
        if: env.RUN_CODE_PATH == 'true'
        run: make test-cabi

      - name: Upload test reports
        if: always() && env.RUN_CODE_PATH == 'true'
        uses: mikepenz/action-junit-report@3a81627bfac62268172037048872e8ebd4207e6d  # v6.4.1
        with:
          report_paths: target/nextest/ci/junit.xml
          check_name: Test Results (Windows)

  # ─────────────────────────────────────────────────────────────────────────
  # Build & test — macOS (Apple Silicon)
  # ─────────────────────────────────────────────────────────────────────────
  build-and-test-macos:
    name: Build & test (macOS arm64)
    needs: changes
    runs-on: macos-14
    timeout-minutes: 60
    env:
      # platform_smoke gates the whole macOS build+smoke required check;
      # platform_full additionally promotes the test step to the full workspace
      # nextest. Linux owns full correctness; macOS proves the Apple Silicon
      # linker/WASI prerequisites + platform-sensitive crates per-PR, with the
      # full duplicated nextest reserved for platform/toolchain/ABI changes and
      # the nightly cross-platform matrix.
      RUN_CODE_PATH: ${{ needs.changes.outputs.platform_smoke }}
      RUN_FULL_NEXTEST: ${{ needs.changes.outputs.platform_full }}
    steps:
      - name: No-op for docs-only changes
        if: env.RUN_CODE_PATH != 'true'
        run: echo "No code changes detected; skipping heavy macOS arm64 CI while satisfying the required check."

      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10  # v6.0.3
        if: env.RUN_CODE_PATH == 'true'

      - uses: ./.github/actions/setup-rust-build
        if: env.RUN_CODE_PATH == 'true'
        with:
          targets: 'aarch64-apple-darwin, x86_64-apple-darwin'
          tools: nextest
          cache-shared-key: build-test-macos

      - name: Install LLVM ${{ env.LLVM_VERSION }}
        if: env.RUN_CODE_PATH == 'true'
        uses: ./.github/actions/setup-llvm
        with:
          version: ${{ env.LLVM_VERSION }}

      - name: Set macOS deployment target
        if: env.RUN_CODE_PATH == 'true'
        run: echo "MACOSX_DEPLOYMENT_TARGET=13.0" >> "$GITHUB_ENV"

      - name: Build libhew.a (runtime + stdlib)
        if: env.RUN_CODE_PATH == 'true'
        run: |
          cargo build -p hew-lib --release
          cargo build -p hew-lib

      - name: Build frontend (release)
        if: env.RUN_CODE_PATH == 'true'
        run: cargo build -p hew-cli --release

      - name: Smoke test the release binary
        if: env.RUN_CODE_PATH == 'true'
        # Drives a trivial program through `hew compile` with the RELEASE
        # binary. Guards the ABI-mismatch abort class (#1606): a release build
        # that crashes at process exit while every debug test passes, because
        # the two use different C++ runtime linkage.
        run: make test-release-binary

      - name: Install WASI runner prerequisites
        if: env.RUN_CODE_PATH == 'true'
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          rustup target add wasm32-wasip1
          WASMTIME_DIR="wasmtime-${WASMTIME_VERSION}-aarch64-macos"
          gh release download "$WASMTIME_VERSION" --repo bytecodealliance/wasmtime \
            --pattern "${WASMTIME_DIR}.tar.xz" --clobber
          tar -xJf "${WASMTIME_DIR}.tar.xz"
          echo "$PWD/${WASMTIME_DIR}" >> "$GITHUB_PATH"
          "./${WASMTIME_DIR}/wasmtime" --version
          command -v wasm-ld
          wasm-ld --version

      - name: Build hew-runtime for wasm32-wasip1 (release)
        if: env.RUN_CODE_PATH == 'true'
        run: cargo build -p hew-runtime --target wasm32-wasip1 --no-default-features --release

      - name: Build hew-runtime for wasm32-wasip1 (debug)
        # The platform smoke tests run as debug binaries (cargo nextest run, no
        # --release). bootstrap_wasi_runner in support/mod.rs derives the build
        # profile from the test binary path and looks for the same profile's
        # libhew_runtime.a before falling back to an on-demand build. Without
        # this step the debug staticlib is absent at test startup, causing
        # multiple parallel test processes to each launch `cargo build` for it
        # concurrently — Cargo writes the archive non-atomically, so races
        # corrupt or transiently delete the file and wasm-ld then fails with
        # "cannot open .../wasm32-wasip1/debug/libhew_runtime.a".
        if: env.RUN_CODE_PATH == 'true'
        run: cargo build -p hew-runtime --target wasm32-wasip1 --no-default-features

      - name: Build hew-std for wasm32-wasip1 (debug)
        # Pre-build the stdlib WASI archive for the same reason: bootstrap_wasi_runner
        # builds libhew_std.a on demand if absent, same concurrent-write race applies.
        if: env.RUN_CODE_PATH == 'true'
        run: cargo build -p hew-std --target wasm32-wasip1

      - name: Run platform smoke tests
        # Default macOS path: prove the Apple Silicon build links and the
        # platform-sensitive crates pass, without re-running the whole
        # 9500-test workspace that Linux already owns. The smoke set is the
        # native/ABI/codegen/CLI surface where macOS-specific breakage lives
        # (linker, dylib, WASI prereqs, process spawning).
        # hew-cabi excluded — its #[no_mangle] symbols conflict with hew-runtime
        # when both link into a test binary.
        # Oracle integration binaries are intentionally excluded from ordinary
        # hosted jobs, including release-gate, for runner cost/reliability.
        # The macOS leak-oracle authority is explicit local macOS validation
        # before release; Linux supplies structural/ASan coverage where applicable.
        if: env.RUN_CODE_PATH == 'true' && env.RUN_FULL_NEXTEST != 'true'
        run: >-
          cargo nextest run
          -p hew-runtime
          -p hew-codegen-rs
          -p hew-compile
          -p hew-cli
          -p hew-lib
          --profile ci
          -E 'not binary(~oracle)'

      - name: Run full Rust workspace tests
        # Platform/toolchain/ABI change: run the full workspace nextest on
        # macOS so a native-linking or cross-platform regression is caught
        # per-PR, not only by the nightly matrix.
        # hew-cabi has #[no_mangle] symbols that conflict with hew-runtime
        # when both link into a test binary.
        #
        # Oracle integration binaries are intentionally excluded from ordinary
        # hosted jobs, including release-gate, for runner cost/reliability.
        # The macOS leak-oracle authority is explicit local macOS validation
        # before release; Linux supplies structural/ASan coverage where applicable.
        if: env.RUN_CODE_PATH == 'true' && env.RUN_FULL_NEXTEST == 'true'
        run: >-
          cargo nextest run --workspace
          --exclude hew-wasm
          --exclude hew-cabi
          --profile ci
          -E 'not binary(~oracle)'

      - name: Run C-ABI crate tests
        # Standalone because the workspace run above must exclude hew-cabi.
        # Unconditional on the code path so Apple Silicon keeps C-ABI coverage
        # even when the full workspace nextest is deduped off this job.
        if: env.RUN_CODE_PATH == 'true'
        run: make test-cabi
