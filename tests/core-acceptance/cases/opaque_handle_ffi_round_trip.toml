[[case]]
id = "opaque_handle_ffi_round_trip"
intent = "The fixture runs to completion and exits 3."
source = "../vertical-slice/accept/opaque_handle_ffi_round_trip.hew"
suites = ["acceptance"]
timeout_seconds = 30

[case.env]
HEW_WORKERS = "4"

[case.expected]
stdout = ""
exit = 3
