[[case]]
id = "ownership-balance-vec-iter-later-arg-exit"
intent = "Ownership-balance fixture. Driven 200,000 times, it releases every obligation exactly once with no leak under ASan/LSan."
source = "fixtures/ownership-balance/vec_iter_later_arg_exit.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 120

[case.expected]
stdout = "39999800000\n"
exit = 0
