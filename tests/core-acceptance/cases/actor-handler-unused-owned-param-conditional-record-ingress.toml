[[case]]
id = "actor-handler-unused-owned-param-conditional-record-ingress"
intent = "Actor-handler owned-parameter disposition oracle. Shape `conditional_record_ingress` (conditional record ingress retains before handler drop) runs to its exact outcome with no leak, double free or use after free under ASan/LSan."
source = "cases/actor-handler-unused-owned-param-conditional-record-ingress.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 60

[case.expected]
stdout = ""
exit = 0
