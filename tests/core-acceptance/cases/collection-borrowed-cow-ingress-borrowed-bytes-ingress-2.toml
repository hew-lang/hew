[[case]]
id = "collection-borrowed-cow-ingress-borrowed-bytes-ingress-2"
intent = "Carrier-general collection-ingress exactly-once oracles: the borrowed `bytes` parameter, the REASSIGNED parameter generation, and the `VecIter` `string` yield binder. Shape `borrowed_bytes_ingress_high` (borrowed bytes value ingress leak slope below tolerance) runs to its exact outcome with no leak, double free or use after free under ASan/LSan."
source = "cases/collection-borrowed-cow-ingress-borrowed-bytes-ingress-2.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 60

[case.expected]
stdout = ""
exit = 0
