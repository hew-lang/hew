[[case]]
id = "example-algo-remove_duplicates"
intent = "The published example compiles, runs and prints exactly what it advertises."
source = "../../examples/algos/remove_duplicates.hew"
suites = ["acceptance"]
timeout_seconds = 30

[case.env]
HEW_WORKERS = "4"

[case.expected]
stdout_file = "../../examples/algos/remove_duplicates.expected"
exit = 0
