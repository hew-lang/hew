[[case]]
id = "bytes-update-values"
intent = "Byte replacement updates a retained slice independently of its original buffer and alias, preserving zero and 255 values."
source = "cases/bytes-update-values.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 30

[case.expected]
stdout = "byte updates passed\n"
stderr = ""
exit = 0
