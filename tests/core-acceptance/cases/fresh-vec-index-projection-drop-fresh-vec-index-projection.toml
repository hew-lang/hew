[[case]]
id = "fresh-vec-index-projection-drop-fresh-vec-index-projection"
intent = "Direct projection from a fresh `Vec` composite-index result must release the cloned root once, without changing the bound-index control's ownership. Shape `fresh_vec_index_projection_high` (direct fresh vec projection has no per frame leak, direct fresh vec projection returns exact result under malloc scribble) runs to its exact outcome with no leak, double free or use after free under ASan/LSan."
source = "cases/fresh-vec-index-projection-drop-fresh-vec-index-projection.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 60

[case.expected]
stdout = ""
exit = 0
