[[case]]
id = "generator-env-clone-generator-actor-param-transfer"
intent = "Standalone memory-safety oracle for owned generator environment snapshots. Shape `generator_actor_param_transfer_high` (generator actor param transfer has zero leak slope, generator and closure env clone are malloc scribble clean) runs to its exact outcome with no leak, double free or use after free under ASan/LSan."
source = "cases/generator-env-clone-generator-actor-param-transfer.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 60

[case.expected]
stdout = "50:1800:OK"
exit = 0
