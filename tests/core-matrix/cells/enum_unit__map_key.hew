enum Colour {
    Red;
    Green;
}

fn main() {
    let k: Colour = Red;
    let m: HashMap<Colour, i64> = HashMap.new();
    m.insert(k, 9);
    match m.get(k) {
        Option.Some(v) => println(f"got {v}"),
        None => println("missing"),
    }
}
