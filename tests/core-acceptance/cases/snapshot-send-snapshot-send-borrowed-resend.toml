[[case]]
id = "snapshot-send-snapshot-send-borrowed-resend"
intent = "Compiled leak and poisoned-allocator oracle for actor snapshot sends. Shape `snapshot-send-borrowed-resend_high` (snapshot send borrowed resend has flat leak slope, snapshot send borrowed resend is owned scribble clean and leak free) runs to its exact outcome with no leak, double free or use after free under ASan/LSan."
source = "cases/snapshot-send-snapshot-send-borrowed-resend.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 60

[case.expected]
stdout = "1:1:50"
exit = 0
