[[case]]
id = "ownership-balance-e-generation-ended"
intent = "E: a string var grown in a loop and returned from inside it. The ownership checker balances every obligation, so the program checks with no diagnostics."
source = "fixtures/ownership-balance/e_generation_ended.hew"
suites = ["acceptance"]
timeout_seconds = 30
kind = "check"
