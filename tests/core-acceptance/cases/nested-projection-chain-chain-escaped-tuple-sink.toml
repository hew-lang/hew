[[case]]
id = "nested-projection-chain-chain-escaped-tuple-sink"
intent = "Nested-projection-chain ownership oracle — the compiled-binary pins for a MULTI-level field-projection alias chain. Shape `chain_escaped_tuple_sink_high` (cloned projection sinks are clean under malloc scribble, escaped into tuple leak slope below tolerance) runs to its exact outcome with no leak, double free or use after free under ASan/LSan."
source = "cases/nested-projection-chain-chain-escaped-tuple-sink.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 60

[case.expected]
stdout = ""
exit = 0
