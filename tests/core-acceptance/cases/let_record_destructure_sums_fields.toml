[[case]]
id = "let_record_destructure_sums_fields"
intent = "The fixture runs to completion and exits 3."
source = "../vertical-slice/accept/let_record_destructure_sums_fields.hew"
suites = ["acceptance"]
timeout_seconds = 30

[case.env]
HEW_WORKERS = "4"

[case.expected]
stdout = ""
exit = 3
