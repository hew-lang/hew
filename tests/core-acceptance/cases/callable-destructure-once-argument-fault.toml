[[case]]
id = "callable-destructure-once-argument-fault"
intent = "A fault in the argument after destructuring a once callable releases heap captures, the uncalled sibling and any retained original field."
source = "cases/callable-destructure-once-argument-fault.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 30

[case.expected]
stdout = ""
stderr = "hew: failure: DivideByZero (202)\n"
exit = 1
