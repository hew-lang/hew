trait Named {
    fn name(val: Self) -> string;
}

type Person {
    name: string,
}

impl Named for Person {
    fn name(person: Person) -> string {
        person.name
    }
}

fn make_person() -> dyn Named {
    Person { name: "Ada" }
}

fn _maybe_person() -> Option<dyn Named> {
    Some(Person { name: "Grace" })
}

fn main() {
    let person = make_person();
    println(person.name());
}
