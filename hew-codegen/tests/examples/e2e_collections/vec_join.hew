fn main() {
    var words: Vec<string> = Vec::new();
    words.push("hello");
    words.push("world");
    words.push("foo");
    println(words.join(", "));
    println(words.join("-"));
    println(words.join(""));

    var empty: Vec<string> = Vec::new();
    println(empty.join(","));

    var single: Vec<string> = Vec::new();
    single.push("only");
    println(single.join(","));
}
