[[case]]
id = "record-clone-generic-no-double-free"
intent = "Drop-safety oracle for GENERIC record clone (`clone Pair<i64, i64>` and owned-field instantiations). Shape `no_double_free` (generic record clone release is exactly once under malloc scribble) runs to its exact outcome with no leak, double free or use after free under ASan/LSan."
source = "cases/record-clone-generic-no-double-free.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 60

[case.expected]
stdout = "18OK"
exit = 0
