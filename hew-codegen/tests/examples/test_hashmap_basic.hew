fn main() {
    let m: HashMap<String, int> = HashMap::new();
    m.insert("x", 10);
    m.insert("y", 20);
    m.insert("z", 30);
    println(m.len());
    match m.get("x") {
        Some(v) => println(v),
        None => println("none"),
    }
    match m.get("y") {
        Some(v) => println(v),
        None => println("none"),
    }
    match m.get("z") {
        Some(v) => println(v),
        None => println("none"),
    }
    println(m.contains_key("x"));
    println(m.contains_key("w"));
}
