[[case]]
id = "composite_return_option_some"
intent = "The fixture runs to completion and exits 42."
source = "../vertical-slice/accept/composite_return_option_some.hew"
suites = ["acceptance"]
timeout_seconds = 30

[case.env]
HEW_WORKERS = "4"

[case.expected]
stdout = ""
exit = 42
