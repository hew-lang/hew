[[case]]
id = "actor_init_narrow_param"
intent = "The fixture runs to completion and exits 123."
source = "../vertical-slice/accept/actor_init_narrow_param.hew"
suites = ["acceptance"]
timeout_seconds = 30

[case.env]
HEW_WORKERS = "4"

[case.expected]
stdout = ""
exit = 123
