pub enum Colour {
    Red;
    Green;
    Blue;
}

pub fn colour_name(c: Colour) -> String {
    match c {
        Red => "red",
        Green => "green",
        Blue => "blue",
        _ => "unknown",
    }
}
