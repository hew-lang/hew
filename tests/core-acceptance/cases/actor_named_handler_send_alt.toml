[[case]]
id = "actor_named_handler_send_alt"
intent = "The fixture runs to completion and exits 42."
source = "../vertical-slice/accept/actor_named_handler_send_alt.hew"
suites = ["acceptance"]
timeout_seconds = 30

[case.env]
HEW_WORKERS = "4"

[case.expected]
stdout = ""
exit = 42
