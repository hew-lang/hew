[[case]]
id = "receive_gen_fn_stream_early_return"
intent = "The fixture runs to completion and prints exactly its recorded output."
source = "../vertical-slice/accept/receive_gen_fn_stream_early_return.hew"
suites = ["acceptance"]
timeout_seconds = 30

[case.env]
HEW_WORKERS = "4"

[case.expected]
stdout = "found at 3\n"
exit = 0
