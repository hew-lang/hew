// Test 8: Vectors/Arrays
// Testing Vec operations
fn main() {
    let v: Vec<i32> = Vec::new();
    v.push(10);
    v.push(20);
    v.push(30);
    println(v.len());
    let first = v.get(0);
    let second = v.get(1);
    let third = v.get(2);
    println(first);
    println(second);
    println(third);
}
