#[resource]
type Tok {
    id: i64
}

impl Tok {
    fn close(self) {
        println(f"close {self.id}");
    }
}

fn main() {
    let h: HashMap<string, Tok> = HashMap.new();
    h.insert("k", Tok { id: 1 });
    println("body");
}
