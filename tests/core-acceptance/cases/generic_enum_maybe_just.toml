[[case]]
id = "generic_enum_maybe_just"
intent = "The fixture runs to completion and exits 42."
source = "../vertical-slice/accept/generic_enum_maybe_just.hew"
suites = ["acceptance"]
timeout_seconds = 30

[case.env]
HEW_WORKERS = "4"

[case.expected]
stdout = ""
exit = 42
