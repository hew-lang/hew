[[case]]
id = "fresh-composite-push-move-fresh-composite-push-nested-single-move"
intent = "Leak oracle for FORM 1 — a `Vec::push` of a fresh COMPOSITE call-result whose producer tail-returns its construction through a `let`-bound local (the `[..]` array-literal desugar). Shape `fresh_composite_push_nested_single_move_df` (push nested single move composite does not double free, push nested single move composite has no per cycle leak slope) runs to its exact outcome with no leak, double free or use after free under ASan/LSan."
source = "cases/fresh-composite-push-move-fresh-composite-push-nested-single-move.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 60

[case.expected]
stdout = ""
exit = 0
