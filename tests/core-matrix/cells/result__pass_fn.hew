fn use_it(v: Result<i64, string>) {
    match v { Result.Ok(v_v) => println(f"ok {v_v}"), Err(v_e) => println(f"err {v_e}"), }
}

fn main() {
    let x: Result<i64, string> = Result.Ok(6);
    use_it(x);
}
