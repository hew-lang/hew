[[case]]
id = "discarded-owned-option-discarded-get"
intent = "Leak and exactly-once guards for discarded caller-owned `Option<T>` results. Shape `discarded_get_scribble` (discarded get does not release the maps copy) runs to its exact outcome with no leak, double free or use after free under ASan/LSan."
source = "cases/discarded-owned-option-discarded-get.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 60

[case.expected]
stdout = "STILL-OWNED"
exit = 0
