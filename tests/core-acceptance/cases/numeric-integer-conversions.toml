[[case]]
id = "numeric-integer-conversions"
intent = "Execute checked numeric boundaries and preserve source values across conversion branches."
source = "cases/numeric-integer-conversions.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 30

[case.expected]
stdout = "source once\ninteger conversions\n"
exit = 0
