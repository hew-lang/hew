pub type Greeting {
    who: String;
}

impl Display for Greeting {
    fn fmt(val: Greeting) -> String {
        "hello, " + val.who
    }
}

fn main() {
    print(Greeting { who: "world" });
    println(Greeting { who: "!" });
}
