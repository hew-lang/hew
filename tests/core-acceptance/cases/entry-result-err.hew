enum AppError {
    Failed(string),
}

impl Display for AppError {
    fn fmt(self) -> string {
        match self {
            AppError.Failed(message) => "failed: " + message,
        }
    }
}

impl Error for AppError {}

fn check(value: i64) -> Result<(), AppError> {
    if value > 2 {
        return Err(AppError.Failed("too large"));
    }
    Ok(())
}

fn main() -> Result<(), AppError> {
    let held = "held:" + "err";
    println(held);
    check(3)?;
    println("unreachable");
    Ok(())
}
