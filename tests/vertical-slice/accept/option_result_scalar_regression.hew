fn main() {
    let opt: Option<i64> = Some(7);
    let res: Result<i64, i64> = Ok(7);
    if opt.is_some() && res.is_ok() {
        let o = opt.expect("opt is present");
        let r = res.expect("res is present");
        println(f"{o}-{r}");
    }
}
