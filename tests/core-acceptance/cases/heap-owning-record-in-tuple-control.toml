[[case]]
id = "heap-owning-record-in-tuple-control"
intent = "Constant-leak oracle for a heap-owning record reached as a TUPLE MEMBER. Shape `control` (enum composite record payload no leak, generic record in tuple no leak) runs to its exact outcome with no leak, double free or use after free under ASan/LSan."
source = "cases/heap-owning-record-in-tuple-control.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 60

[case.expected]
stdout = ""
exit = 0
