[[case]]
id = "interior-alias-clone-interior-alias-neg-control"
intent = "Interior-alias clone-out leak oracle (hew-lang/hew gap #54). Shape `interior_alias_neg_control` (interior alias true alias no double free under malloc scribble) runs to its exact outcome with no leak, double free or use after free under ASan/LSan."
source = "cases/interior-alias-clone-interior-alias-neg-control.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 60

[case.expected]
stdout = "nz"
exit = 0
