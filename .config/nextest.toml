# Nextest configuration for the Hew workspace.
# https://nexte.st/docs/configuration/

# ── Test groups ────────────────────────────────────────────────────────────────
# subprocess-intensive: tests that spawn the full `hew` compiler binary
# (compile + codegen + link + run).  Each test consumes several cores while the
# compiler runs, so allowing all 16 to run in parallel causes self-starvation:
# compile times balloon and timeout hang-guards fire even though no test is
# genuinely stuck.  Capping at 8 concurrent subprocess tests leaves headroom for
# the OS scheduler and avoids the oversubscription that causes eval_e2e flakes.
[test-groups]
subprocess-intensive = { max-threads = 8 }
# ffi-native-link: tests that invoke clang/ld to link a real FFI executable.
# Keep these links serial so their cold native work does not contend with sibling
# FFI links during the full workspace run.
ffi-native-link = { max-threads = 1 }
# process-timing: tests that measure process-group signal delivery timing (SIGKILL
# propagation to grandchildren, process-group watchdog latency).  Under 16-core
# concurrent load the OS scheduler takes variable time to deliver signals and
# reap children; capping at 4 concurrent process-timing tests prevents the
# scheduler saturation that caused bounded_child_timeout false-failures in S117.
process-timing = { max-threads = 4 }
# real-timing: tests whose assertions irreducibly measure real wall-clock / OS-level
# timing — SIGKILL delivery to a process group, two-process loopback round-trips —
# and therefore cannot be made deterministic via the simulated-time seam (simtime is
# in-process).  They run ONE-AT-A-TIME (max-threads = 1) so no concurrent test steals
# the scheduler headroom their delivery windows need, and they are excluded from the
# smoke fast tier (see that profile's default-filter) so they never gate fast
# iteration.  This is a quarantine, not a parking lot: a timing test that CAN be made
# deterministic is fixed at the root by the de-flake initiative, never parked here.
real-timing = { max-threads = 1 }
# supervision-lifecycle-accounting: each test process installs a native runtime
# and worker pool, then relies on process exit to close nextest's capture pipes.
# On Darwin, launching all cases at once can delay nextest's asynchronous EOF
# observation past its leak timeout even though process-tree and lsof snapshots
# show no surviving child and no inherited sibling pipe. Run these small tests
# one-at-a-time so `leak-timeout = fail` below is a reliable ownership gate
# instead of a concurrent output-drain false positive.
supervision-lifecycle-accounting = { max-threads = 1 }

[profile.default]
# Exclude hew-wasm by default (requires wasmtime). Exclude sandbox parity from
# generic nextest runs because it spawns the hew-sandbox-vm Node runner; CI
# enforces it through the provisioned `make sandbox-parity` step.
# Exclusion is whole-binary, not per-test: a binary is excluded here if ANY
# function it contains spawns the Node runner, regardless of whether every
# #[test] in that binary reaches it directly, transitively through a helper,
# or only via some indirection (dynamic dispatch, a function pointer) that
# static analysis of the call graph cannot prove reaches or does not reach a
# given test. scripts/check-sandbox-parity-coverage.py enforces this
# contract: per-test carve-outs inside a VM-touching binary are not trusted.
# binary(parity) is the standalone parity binary. binary(parity_ratchet)
# also holds VM-dependent work — its live_gate test spawns the Node runner —
# so the WHOLE binary is excluded here, including its non-VM structural
# ratchet tests; those run only under the provisioned `make sandbox-parity`
# gate now. binary(playground) and binary(ios_subset) are single-test
# binaries whose only test each spawns the same Node runner.
# make sandbox-parity runs all four via plain `cargo test` (no nextest profile),
# so every test in every VM-dependent binary stays enforced where the deps
# are provisioned.
default-filter = "not package(hew-wasm) - binary(parity) - binary(parity_ratchet) - binary(playground) - binary(ios_subset)"
# Global concurrency cap: defaults to all available cores (num-cpus), which is
# fine for pure in-process tests.  The subprocess-intensive group above provides
# the finer-grained cap for tests that spawn child processes.
test-threads = "num-cpus"

[[profile.default.overrides]]
# eval_e2e and package-manager eval tests each spawn the full hew compiler binary (AOT
# compile + codegen + link + subprocess execute).  Route them into the
# subprocess-intensive group so at most 8 run concurrently, preventing
# compiler-process oversubscription that inflates compile times and causes
# the eval hang-guard to fire spuriously.
filter = "binary(eval_e2e)"
test-group = "subprocess-intensive"

[[profile.default.overrides]]
# This executable ownership oracle runs a complete compiler + codegen + native
# link + actor execution under a strict 30-second child deadline. On a hosted
# runner the same healthy test twice exhausted that deadline while competing
# with the full workspace's other LLVM/link subprocesses; isolated exact-host
# runs completed in under a second. Reserve the whole nextest thread pool for
# this one compiler subprocess so the deadline continues to mean "the ownership
# oracle hung", not "the compiler was starved". The executable keeps strict
# free_cstring abort and value assertions and carries no retry.
filter = "binary(run_e2e) and test(/^run_actor_sent_string_not_double_freed$/)"
threads-required = "num-test-threads"

[[profile.default.overrides]]
# Serial process execution makes handle-leak detection deterministic for this
# runtime-lifecycle binary. Any capture handle still open after nextest's normal
# 200 ms grace period is therefore a hard failure.
filter = "binary(supervision_lifecycle)"
test-group = "supervision-lifecycle-accounting"
leak-timeout = { period = "200ms", result = "fail" }

[profile.ci]
# CI profile: runs all tests and produces JUnit XML for GitHub Actions reporting.
# `test_runner_e2e` used to be excluded here against "the ctest E2E suite";
# no such suite exists in this repository, so the exclusion covered nothing.
# The binary now runs in CI, throttled into the subprocess-intensive group by
# the override below because each of its tests spawns the full hew binary.
# Exclude sandbox parity from generic nextest runs because it needs
# `npm --prefix hew-sandbox-vm ci`; CI enforces it through the provisioned
# `make sandbox-parity` step. Exclusion is whole-binary: see profile.default's
# comment for why per-test carve-outs inside a VM-touching binary are not
# trusted (scripts/check-sandbox-parity-coverage.py enforces this).
# binary(parity) is the standalone parity binary. binary(parity_ratchet) also
# holds VM-dependent work — its live_gate test drives the Node VM — so the
# WHOLE binary is excluded, including its non-VM structural ratchet tests;
# those run only under `make sandbox-parity` now. binary(playground) and
# binary(ios_subset) are single-test binaries whose only test each drives
# the same Node VM, so the whole binary is excluded here too. make
# sandbox-parity runs all four via plain `cargo test`, so every test in
# every VM-dependent binary stays enforced where the npm deps are
# provisioned.
default-filter = "not package(hew-wasm) - binary(parity) - binary(parity_ratchet) - binary(playground) - binary(ios_subset)"
# Defense-in-depth for tests that bypass the bounded exec helper: nextest kills
# tests after 3 × 100 s = 300 s without status updates/output.
# timeout-class: standard
slow-timeout = { period = "100s", terminate-after = 3 }

[[profile.ci.overrides]]
# The Intel macOS release-gate runner is roughly three times slower than the
# other supported runners: its release build alone takes 28.8 minutes, versus
# about 7 minutes on Linux. The per-test ceiling exists to catch hung tests, so
# this host needs a proportionally longer but still finite 900-second window.
# Apply it platform-wide rather than encoding whichever tests happened to be
# slow when this rule was written; every other platform retains the 300-second
# ceiling below.
platform = "cfg(all(target_os = \"macos\", target_arch = \"x86_64\"))"
# timeout-class: intel-macos
slow-timeout = { period = "300s", terminate-after = 3 }

[[profile.ci.overrides]]
# Every test_runner_e2e case spawns the full hew binary to drive `hew test`.
# Cap the concurrency the same way eval_e2e is capped so the suite does not
# oversubscribe a 4-core hosted runner.
filter = "binary(test_runner_e2e)"
test-group = "subprocess-intensive"

[[profile.ci.overrides]]
# Keep the local ownership gate intact in the CI profile as well: serialize
# this binary and fail rather than merely annotate a genuine leaked handle.
filter = "binary(supervision_lifecycle)"
test-group = "supervision-lifecycle-accounting"
leak-timeout = { period = "200ms", result = "fail" }

[[profile.ci.overrides]]
# Codegen exec/e2e integration binaries spawn full compile+link+run child
# processes; on 4-core hosted runners an unthrottled exec test starves under
# workspace-wide parallelism. Same medicine as eval_e2e: cap concurrency,
# change no coverage.
filter = "binary(~exec)"
test-group = "subprocess-intensive"
# Hosted 4-core runners run throttled exec tests past the default 300s kill
# window even when healthy (each spawns compile+link+run children sharing 4
# cores with up to 7 siblings). Wider terminate budget on ci only; the tests'
# internal timeouts still bound genuine hangs.
# timeout-class: subprocess
slow-timeout = { period = "120s", terminate-after = 4 }

[[profile.ci.overrides]]
# Native FFI clang/ld linking is CI-cold-slow and load-variable, so serialize
# this dedicated native-link binary. The `leaks(1)` probe has its own five-second
# process-tree deadline; this modest outer budget now remains a final backstop.
filter = "binary(ffi_link_e2e)"
test-group = "ffi-native-link"
# timeout-class: ffi-link
slow-timeout = { period = "60s", terminate-after = 3 }

[[profile.ci.overrides]]
# await_e2e builds each fixture once, then launches the native executable under
# both the default and single-worker pools. Keep those fixture builds in the
# subprocess group so workspace-wide LLVM/link work cannot oversubscribe small
# hosted runners.
filter = "binary(await_e2e)"
test-group = "subprocess-intensive"
# timeout-class: subprocess
slow-timeout = { period = "120s", terminate-after = 4 }

[[profile.ci.overrides]]
# eval_e2e tests spawn the full hew compiler binary (AOT compile + codegen +
# link + subprocess execute).  Route them into the subprocess-intensive group
# so at most 8 run concurrently, preventing compiler-process oversubscription
# that inflates compile times and causes the eval hang-guard to fire spuriously.
filter = "binary(eval_e2e)"
test-group = "subprocess-intensive"

[[profile.ci.overrides]]
# This test uses the child stdout pipe as both its startup and process-tree
# termination handshake. Serialize it with the other real-OS timing tests so
# its one-second timeout remains a process-tree behaviour check.
filter = "test(bounded_child_timeout_kills_grandchild_process_group)"
test-group = "real-timing"
# Defense-in-depth: the test's internal wait_with_timeout fires at 1 s; 90 s
# nextest kill (30 s × 3) bounds a wedged test that bypasses that guard.
# Does NOT cover A1 (worker/disk death) or A2 (process-wide starvation) —
# those defeat per-test timeouts entirely; see task #47 / supervisor.rs / channel.rs.
# timeout-class: timing
slow-timeout = { period = "30s", terminate-after = 3 }

[[profile.ci.overrides]]
# These hew-runtime connection tests coordinate real OS threads through blocking
# channel handshakes. They wait for protocol events rather than scheduler-sensitive
# per-operation deadlines, while serialization keeps this real-thread class from
# competing with the workspace-wide parallel run.
filter = "binary(hew_runtime) and test(/^connection::tests::(conn_actor_drop_closes_transport_before_join|connmgr_remove_releases_connections_lock_before_reader_wake|install_connection_actor_shutdown_releases_lock_before_reader_wake|connmgr_free_waits_for_self_removed_reader_lifecycle|connmgr_remove_skips_stale_route_cleanup_after_replacement|connmgr_publish_skips_removed_connection_(while_establish_blocks|without_routing_table))$/)"
test-group = "real-timing"
# Single process-level deadlock backstop; the positive channel waits have no deadlines.
# timeout-class: thread-handshake
slow-timeout = { period = "30s", terminate-after = 2 }

[[profile.ci.overrides]]
# The two-process remote-ask pair (echo, timeout) spawns a second OS process and
# round-trips an ask over loopback TCP. These run in the real-timing group
# (max-threads = 1) because their assertions irreducibly measure a real
# cross-process round-trip that the in-process simtime seam cannot fake; the
# serialisation keeps them off the fast tiers and gives the round-trip scheduler
# headroom. They carry NO retry: the connection-drop race that once forced a
# retry shim (a graceful node-stop abandoned an already-computed inbound reply,
# surfacing a spurious ConnectionDropped) was fixed at the root — hew_node_stop
# now drains in-flight inbound-ask reply sends before tearing down the
# connection, so a correct echo/timeout outcome is deterministic under load.
filter = "test(two_process_remote_ask_echo_double_returns_42) | test(remote_ask_two_process_echo_client_helper) | test(remote_ask_two_process_echo_server_helper) | test(two_process_remote_ask_timeout_returns_timeout_under_1500ms) | test(remote_ask_two_process_timeout_client_helper) | test(remote_ask_two_process_timeout_server_helper)"
test-group = "real-timing"
# Defense-in-depth: the test's internal wait_output fires at 40 s; 90 s nextest
# kill (30 s × 3) bounds a wedged test that bypasses that guard.
# Does NOT cover A1 (worker/disk death) or A2 (process-wide starvation) —
# those defeat per-test timeouts entirely; see task #47 / supervisor.rs / channel.rs.
# timeout-class: timing
slow-timeout = { period = "30s", terminate-after = 3 }

[[profile.ci.overrides]]
# The compiled-.hew two-process distributed harness spawns a server and a client
# process — each a native binary built from dist_node.hew — that round-trip a
# remote ask over loopback TCP. Unlike the runtime-crate two-process tests above,
# this one COMPILES a real .hew program first (the proving gate for codegen +
# transport + cluster), so its slow-timeout must cover a cold compile (~90 s
# under hosted-runner load) in addition to the cross-process round-trip. It runs
# in the real-timing group (max-threads = 1): its assertion irreducibly measures
# a real cross-process exchange the in-process simtime seam cannot fake, and the
# serialisation keeps it off the fast tiers and gives the round-trip headroom.
# No retry: the round-trip is deterministic (server READY signal + client
# bounded-poll lookup), so a correct outcome does not depend on scheduler luck.
# wire_cbor_cross_process_round_trip is the same harness with a #[wire]-typed
# payload (explicit @N tags); it shares the compiled binary via OnceLock and runs
# in the same serialized group for the same reasons.
# remote_ask_under_partition_fails_closed_not_hang is the same compiled harness
# (server + client processes built from dist_node.hew) exercising the fail-closed
# no-hang partition gate; it belongs in the same serialized real-timing group as
# its siblings above (it was the only one in this binary missing the assignment,
# which left it exposed to the concurrent-load timing flake).
filter = "test(remote_ask_round_trip_returns_exact_values) | test(wire_cbor_cross_process_round_trip) | test(remote_ask_under_partition_fails_closed_not_hang)"
test-group = "real-timing"
# timeout-class: distributed
slow-timeout = { period = "120s", terminate-after = 3 }

[[profile.ci.overrides]]
# tcp_stream_backing_records_error_on_reset is a loopback-TCP reset test: it
# gates the RST on an explicit "accepted" handshake, forces it via SO_LINGER=0 +
# drop, joins the peer thread (so the RST is sent before the read), then blocks
# on a single read that returns None on RST/EOF. It is determinism-clean — the
# join + blocking read ARE the synchronization, with no wall-clock "let the RST
# arrive" sleep (removed 2026-06-18). This slow-timeout is purely a HANG CEILING
# for genuine physical-runner latency: inside the FreeBSD VM under QEMU emulation
# the connect/accept/RST round-trip can exceed the default 300s ci slow-timeout.
# It is NOT a jitter-absorb window — the test makes no timing bet, so a real
# regression fails fast (wrong read result), and the wide period only prevents a
# false KILL on a slow VM. The assertion stays strict (next() must return None).
filter = "test(tcp_stream_backing_records_error_on_reset)"
# timeout-class: qemu-network
slow-timeout = { period = "120s", terminate-after = 6 }

[[profile.ci.overrides]]
# close_reaps_a_live_blocked_reader_without_hanging and
# attach_reader_delivers_decrypted_data_and_reaps_on_close each measure how
# long hew_tls_close's blocking join of the attached reader thread takes
# (close_elapsed < 2s) after tearing down a real loopback TLS connection.
# The join itself is deterministic (JoinHandle::join, not a sleep-poll),
# but the elapsed-time assertion is an irreducibly real OS-scheduling
# measurement — it times how promptly two real OS threads get scheduled to
# completion. Under full-workspace parallel nextest load the scheduler can
# delay that handoff past the 2s bound even though no reader is actually
# stuck (#2358, verified pre-existing; passes reliably in isolation and
# under the lane profile — load-only under full-suite parallelism). The
# real-timing group (max-threads = 1) removes the contention at its source
# instead of widening the in-test deadline, which would mask a genuine
# future hang rather than fix the flake.
filter = "test(close_reaps_a_live_blocked_reader_without_hanging) | test(attach_reader_delivers_decrypted_data_and_reaps_on_close)"
test-group = "real-timing"

[profile.ci.junit]
path = "junit.xml"

[profile.ci-cabi]
# `make test-cabi` runs hew-cabi on its own (see that target's comment on why
# it is excluded from every workspace-wide nextest invocation) after the
# workspace run has already completed in the same job. Reusing `profile.ci`
# here previously meant both runs wrote to the same
# target/nextest/ci/junit.xml — the cabi run's ~80-test report silently
# overwrote the workspace run's ~13,000-test report before the upload step
# read it, so the PR checks UI showed only hew-cabi's results. Nextest scopes
# JUnit output by profile name (target/nextest/<profile>/junit.xml), so a
# distinct profile name is sufficient to give this run its own report; no
# other ci behaviour needs duplicating for a single small crate.
default-filter = "all()"
# timeout-class: standard
slow-timeout = { period = "100s", terminate-after = 3 }

[profile.ci-cabi.junit]
path = "junit.xml"

[profile.ci-focused]
# For a narrow supplementary nextest invocation (a single crate/binary/test)
# that a job runs AFTER its main `--profile ci` workspace pass, in the same
# job. Reusing `profile.ci` for that follow-up run is the same
# overwrite bug `profile.ci-cabi` exists to prevent: both invocations would
# write target/nextest/ci/junit.xml, and the later one wins silently. First
# use: freebsd.yml's/gate-freebsd's dedicated dwarf_debugger_locals_e2e run,
# which follows the workspace and hew-cabi passes in the same FreeBSD job.
default-filter = "all()"
# timeout-class: standard
slow-timeout = { period = "100s", terminate-after = 3 }

[profile.ci-focused.junit]
path = "junit.xml"

[profile.smoke]
# Smoke profile: curated deterministic in-process oracle only.  Designed to
# give a pass/fail signal before committing to the full Rust workspace body.
# Used by the direct local `ci-preflight-smoke` target.
#
# What it runs: a small deterministic oracle complement to fmt/clippy that does
# not spawn the full `hew` compiler binary or the sandbox Node runner, does not
# need pre-built libhew.a or wasm32 runtimes, and does not connect to network
# services.
# Key oracle: `fmt_roundtrip_corpus::every_hew_file_roundtrips_through_formatter`
# in hew-parser — a deterministic corpus scan that surfaced the S115/S119
# formatter-AST regressions before the heavy tier ran.
#
# What it excludes (intentional; the ci profile covers these):
#   eval_e2e     — spawns `hew` binary, needs pre-built runtimes, slow
#   test_runner_e2e — same (subprocess-intensive)
#   parity        — needs Node runner + npm install
#   parity_ratchet — lives in the `parity_ratchet` binary (which `binary(parity)`
#     does NOT match); its live_gate test spawns the Node VM and asserts
#     npm-provisioned coverage, so the whole binary is excluded, same as
#     profile.default/profile.ci (whole-binary, not per-test — see there).
#   hew-wasm      — requires wasmtime
#
# Coverage contract: the smoke target is a FAST local signal, while the ci
# profile provides full coverage and includes this selected test.
default-filter = "test(every_hew_file_roundtrips_through_formatter)"
test-threads = "num-cpus"
# timeout-class: smoke
slow-timeout = { period = "60s", terminate-after = 2 }

[[profile.smoke.overrides]]
# Route any eval_e2e tests that slip past the default-filter into the group cap.
# This is a safety net; the default-filter above should exclude them entirely.
filter = "binary(eval_e2e)"
test-group = "subprocess-intensive"
