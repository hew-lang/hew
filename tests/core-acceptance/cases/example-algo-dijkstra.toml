[[case]]
id = "example-algo-dijkstra"
intent = "The published example compiles, runs and prints exactly what it advertises."
source = "../../examples/algos/dijkstra.hew"
suites = ["acceptance"]
timeout_seconds = 30

[case.env]
HEW_WORKERS = "4"

[case.expected]
stdout_file = "../../examples/algos/dijkstra.expected"
exit = 0
