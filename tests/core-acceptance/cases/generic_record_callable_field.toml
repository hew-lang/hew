[[case]]
id = "generic_record_callable_field"
intent = "The fixture runs to completion and prints exactly its recorded output."
source = "../vertical-slice/accept/generic_record_callable_field.hew"
suites = ["acceptance"]
timeout_seconds = 30

[case.env]
HEW_WORKERS = "4"

[case.expected]
stdout = "even:true\neven:false\nlong:true\nlong:false\n"
exit = 0
