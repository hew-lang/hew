fn main() {
    let x: bool = true;
    var m: HashMap<string, bool> = HashMap.new();
    m.insert("k", x);
    match m.get("k") {
        Option.Some(e) => {
            println(f"{e}");
        },
        Option.None => println("missing"),
    }
}
