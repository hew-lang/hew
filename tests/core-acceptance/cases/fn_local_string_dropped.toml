[[case]]
id = "fn_local_string_dropped"
intent = "W5-011 behavioural proof — function-scope drop of a heap-owning `string` local that never escapes."
source = "../vertical-slice/accept/fn_local_string_dropped.hew"
suites = ["acceptance"]
timeout_seconds = 30

[case.env]
HEW_WORKERS = "4"

[case.expected]
stdout = "done\n"
exit = 0
