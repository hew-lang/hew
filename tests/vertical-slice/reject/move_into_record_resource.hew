#[resource]
type File { fd: i64, }

impl File {
    fn close(consume self) { }
}

type Holder { file: File, }

fn main() -> i64 {
    let file = File { fd: 1 };
    let _holder = Holder { file: file };
    file.fd
}
