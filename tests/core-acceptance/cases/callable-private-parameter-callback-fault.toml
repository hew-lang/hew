[[case]]
id = "callable-private-parameter-callback-fault"
intent = "A fault inside a private mutable parameter callback releases its copied owned capture and callback locals, then cleans the original caller environment without continuing."
source = "cases/callable-private-parameter-callback-fault.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 15

[case.expected]
stdout = ""
stderr = "hew: failure: DivideByZero (202)\n"
exit = 1
