[[case]]
id = "machine_state_resource_payload_reenter"
intent = "A direct `#[resource]` state payload has no independent value copy, so a step cannot stage it yet; the refusal names the machine, state, field, type and the transition that takes it."
source = "../vertical-slice/reject/machine_state_resource_payload_reenter.hew"
suites = ["acceptance"]
timeout_seconds = 30
kind = "reject"

[[case.expected.diagnostics]]
code = "MachineExhaustivenessError"
line = 22
column = 40
message = "machine `Session` state `Active` field `h` holds `Handle`, which has no independent value copy"
