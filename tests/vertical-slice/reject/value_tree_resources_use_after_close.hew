// TOML retains an independent resource API; released values cannot be used.
import std.encoding.toml;

fn use_toml() {
    let value = toml.from_int(1);
    value.free();
    let _ = value.get_int();
}

fn main() {}
