[[case]]
id = "mailbox_block_supervised_childref_single_worker"
intent = "A cooperative bounded send through ChildRef must resolve and pin the stable supervisor role."
source = "../vertical-slice/accept/mailbox_block_supervised_childref_single_worker.hew"
suites = ["acceptance"]
timeout_seconds = 30

[case.env]
HEW_WORKERS = "1"

[case.expected]
stdout = "CHILDREF_BLOCK_WORK_DELIVERED\n"
exit = 42
