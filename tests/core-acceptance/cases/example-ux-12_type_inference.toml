[[case]]
id = "example-ux-12_type_inference"
intent = "The published example compiles, runs and prints exactly what it advertises."
source = "../../examples/ux/12_type_inference.hew"
suites = ["acceptance"]
timeout_seconds = 30

[case.env]
HEW_WORKERS = "4"

[case.expected]
stdout_file = "../../examples/ux/12_type_inference.expected"
exit = 0
