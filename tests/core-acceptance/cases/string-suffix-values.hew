fn main() -> i64 {
    let text = "prefix suffix";
    println(text.ends_with("suffix"));
    println(text.ends_with("prefix"));
    println(text.ends_with(""));
    println("".ends_with(""));
    println(text.contains("suffix"));
    0
}
