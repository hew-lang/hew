[[case]]
id = "match-payload-reassign-move-scope-drop"
intent = "Projected-payload move-out UAF / double-free oracle (issue #2523). Shape `move_scope_drop` (move scope drop no reread single free) runs to its exact outcome with no leak, double free or use after free under ASan/LSan."
source = "cases/match-payload-reassign-move-scope-drop.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 60

[case.expected]
stdout = ""
exit = 6
