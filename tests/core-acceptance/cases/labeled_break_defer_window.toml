[[case]]
id = "labeled_break_defer_window"
intent = "The fixture runs to completion and prints exactly its recorded output."
source = "../vertical-slice/accept/labeled_break_defer_window.hew"
suites = ["acceptance"]
timeout_seconds = 30

[case.env]
HEW_WORKERS = "4"

[case.expected]
stdout = "inner\nouter\nafter\n"
exit = 0
