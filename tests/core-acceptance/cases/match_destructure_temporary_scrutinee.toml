[[case]]
id = "match_destructure_temporary_scrutinee"
intent = "Accept: an all-wildcard record match destructure on a temporary scrutinee."
source = "../vertical-slice/accept/match_destructure_temporary_scrutinee.hew"
suites = ["acceptance"]
timeout_seconds = 30

[case.env]
HEW_WORKERS = "4"

[case.expected]
stdout = "discarded\n"
exit = 0
