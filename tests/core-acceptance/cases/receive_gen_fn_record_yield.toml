[[case]]
id = "receive_gen_fn_record_yield"
intent = "The fixture runs to completion and prints exactly its recorded output."
source = "../vertical-slice/accept/receive_gen_fn_record_yield.hew"
suites = ["acceptance"]
timeout_seconds = 30

[case.env]
HEW_WORKERS = "4"

[case.expected]
stdout = "1,2\n3,4\n"
exit = 0
