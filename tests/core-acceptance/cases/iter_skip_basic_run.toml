[[case]]
id = "iter_skip_basic_run"
intent = "The fixture runs to completion and exits 6."
source = "../vertical-slice/accept/iter_skip_basic_run.hew"
suites = ["acceptance"]
timeout_seconds = 30

[case.env]
HEW_WORKERS = "4"

[case.expected]
stdout = ""
exit = 6
