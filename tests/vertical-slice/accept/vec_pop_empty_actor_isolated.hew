// EXIT: 1
actor VecPopCrasher {
    receive fn boom() {
        var xs: Vec<i64> = Vec.new();
        let _ = xs.pop();
    }
}

actor VecPopProbe {
    receive fn ping() -> i64 {
        0
    }
}

fn main() -> i64 {
    let crasher = spawn VecPopCrasher;
    let probe = spawn VecPopProbe;
    let _ = crasher.boom();
    match probe.ping() {
        .Ok(v) => v,
        .Err(_) => 1,
    }
}
