fn main() -> i64 {
    var total: i64 = 0;
    var i: i64 = 0;
    while i < 50 {
        var v: Vec<i64> = Vec.new();
        v.push(i);
        v.push(i * 2);
        let _c = v.iter();
        total = total + v.len();
        i = i + 1;
    }
    total
}
