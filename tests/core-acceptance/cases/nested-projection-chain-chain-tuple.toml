[[case]]
id = "nested-projection-chain-chain-tuple"
intent = "Nested-projection-chain ownership oracle — the compiled-binary pins for a MULTI-level field-projection alias chain. Shape `chain_tuple_high` (two level tuple chain leak slope below tolerance, two level tuple chain no double free under malloc scribble) runs to its exact outcome with no leak, double free or use after free under ASan/LSan."
source = "cases/nested-projection-chain-chain-tuple.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 60

[case.expected]
stdout = ""
exit = 0
