[[case]]
id = "example-ux-07_enum_payload"
intent = "The published example compiles, runs and prints exactly what it advertises."
source = "../../examples/ux/07_enum_payload.hew"
suites = ["acceptance"]
timeout_seconds = 30

[case.env]
HEW_WORKERS = "4"

[case.expected]
stdout_file = "../../examples/ux/07_enum_payload.expected"
exit = 0
