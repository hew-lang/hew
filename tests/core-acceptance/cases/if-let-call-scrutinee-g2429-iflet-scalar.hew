fn f() -> Option<i64> { .Some(7) }

fn main() -> i64 {
    var total = 0;
    var i = 0;
    while i < 50 {
        if let .Some(b) = f() { total = total + b; }
        i = i + 1;
    }
    if total > 0 { 0 } else { 1 }
}
