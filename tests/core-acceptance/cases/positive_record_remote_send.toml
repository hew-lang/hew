[[case]]
id = "positive_record_remote_send"
intent = "Accept: a record message with an explicit wire schema crosses a RemotePid<T>::send boundary."
source = "../vertical-slice/accept/positive_record_remote_send.hew"
suites = ["acceptance"]
timeout_seconds = 30
kind = "check"

[[case.expected.diagnostics]]
severity = "warning"
code = "dead_code"
line = 17
column = 1
message = "function `send_remote` is never called"
