[[case]]
id = "if-let-call-scrutinee-g2429-iflet-record"
intent = "From-call `if let` scrutinee enum-payload leak oracle (#2429 class). Shape `g2429_iflet_record_high` (iflet record payload loop leak slope below tolerance) runs to its exact outcome with no leak, double free or use after free under ASan/LSan."
source = "cases/if-let-call-scrutinee-g2429-iflet-record.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 60

[case.expected]
stdout = ""
exit = 0
