[[case]]
id = "parked-destroy-owned-local-parked-vec-iter-teardown"
intent = "Destroy-while-parked owned-local leak / double-free oracle (#2395). Shape `parked_vec_iter_teardown` (parked destroy frees first class vec iter snapshot zero leaks) runs to its exact outcome with no leak, double free or use after free under ASan/LSan."
source = "cases/parked-destroy-owned-local-parked-vec-iter-teardown.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 60

[case.expected]
stdout = "stopped\n"
exit = 0
