[[case]]
id = "fixed-array-generators"
intent = "Array replacement and release cooperatively close suspended generator elements in forward index order."
source = "cases/fixed-array-generators.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 30

[case.expected]
stdout = "first\nsecond\nreplacement\nfirst cleaned\n2\nreplacement cleaned\nsecond cleaned\ndone\n"
exit = 0
