[[case]]
id = "example-surface-typed_streams"
intent = "The published example compiles, runs and prints exactly what it advertises."
source = "../../examples/v05/surfaces/typed_streams.hew"
suites = ["acceptance"]
timeout_seconds = 30

[case.env]
HEW_WORKERS = "4"

[case.expected]
stdout_file = "../../examples/v05/surfaces/typed_streams.expected"
exit = 0
