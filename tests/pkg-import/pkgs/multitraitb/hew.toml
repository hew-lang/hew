[package]
name = "hew::multitraitb"
version = "0.1.0"
description = "Multi-method trait collision fixture (module B): exports a DISTINCT trait `Proc` whose `close` returns `i64`, colliding on the bare trait name with hew::multitrait"
authors = ["Hew Contributors"]
license = "MIT OR Apache-2.0"
