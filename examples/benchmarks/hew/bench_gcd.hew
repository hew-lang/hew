fn gcd(a: int, b: int) -> int {
    var x = a;
    var y = b;
    while y != 0 {
        let temp = y;
        y = x % temp;
        x = temp;
    }
    x
}

fn main() {
    let iters = 5000000;
    var sum = 0;
    for i in 0..iters {
        sum = sum + gcd(1071, 462);
    }
    println(iters);
    println(sum);
}
