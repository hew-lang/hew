[[case]]
id = "example-datastruct-bloom_filter"
intent = "The published example compiles, runs and prints exactly what it advertises."
source = "../../examples/datastruct/bloom_filter.hew"
suites = ["acceptance"]
timeout_seconds = 30

[case.env]
HEW_WORKERS = "4"

[case.expected]
stdout_file = "../../examples/datastruct/bloom_filter.expected"
exit = 0
