fn passthru(value: string) -> string { value }
fn main() -> i64 {
    var i: i64 = 0;
    var total: i64 = 0;
    while i < 200 {
        println(f"value={passthru(f"owned-carrier-token-{i}")}");
        total = total + i;
        i = i + 1;
    }
    if total != 19900 { return 83; }
    0
}
