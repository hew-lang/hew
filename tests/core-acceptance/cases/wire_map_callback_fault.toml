[[case]]
id = "wire_map_callback_fault"
intent = "The guard traps: the fixture reports DivideByZero and exits 1 under the one exit rule."
source = "../vertical-slice/accept/wire_map_callback_fault.hew"
suites = ["acceptance"]
timeout_seconds = 30

[case.env]
HEW_WORKERS = "4"

[case.expected]
stdout = ""
stderr = "hew: failure: DivideByZero (202)\n"
exit = 1
