import std::text::regex;

fn main() {
    let re = regex.new("a+");
    let re2 = re.clone();
    let matched = re2.is_match("aaa");
    println(f"re2.is_match: {matched}");
    re.close();
    re2.close();
}
