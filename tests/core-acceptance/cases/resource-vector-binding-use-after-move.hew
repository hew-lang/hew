#[resource]
type Conn { fd: i64 }
impl Conn {
    fn close(consume self) { println(f"close {self.fd}"); }
}
fn main() {
    var v: Vec<Conn> = Vec.new();
    v.push(Conn { fd: 1 });
    v.push(Conn { fd: 2 });
    let w = v;
    println(w.len());
    println(v.len());
}
