[[case]]
id = "generator-env-clone-generator-actor-record-param-transfer"
intent = "Standalone memory-safety oracle for owned generator environment snapshots. Shape `generator_actor_record_param_transfer_high` (generator actor record param transfer has zero leak slope, generator and closure env clone are malloc scribble clean) runs to its exact outcome with no leak, double free or use after free under ASan/LSan."
source = "cases/generator-env-clone-generator-actor-record-param-transfer.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 60

[case.expected]
stdout = "50:9350:OK"
exit = 0
