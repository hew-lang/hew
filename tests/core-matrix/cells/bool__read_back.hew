fn main() {
    let x: bool = true;
    let v: Vec<bool> = Vec.new();
    v.push(x);
    match v.get(0) {
        Option.Some(e) => {
            println(f"{e}");
        },
        None => println("missing"),
    }
}
