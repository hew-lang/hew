fn factorial(n: int) -> int {
    var result = 1;
    for i in 1..(n + 1) {
        result = result * i;
    }
    result
}

fn main() {
    let iters = 1000000;
    var sum = 0;
    for i in 0..iters {
        sum = sum + factorial(20);
    }
    println(iters);
    println(sum);
}
