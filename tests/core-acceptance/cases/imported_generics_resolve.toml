[[case]]
id = "imported_generics_resolve"
intent = "Accept fixture: imported_generics_resolve — importing a module whose generic actor / generic trait / supervisor signatures name their enclosing type params must type-check cleanly."
source = "../vertical-slice/accept/imported_generics_resolve/main.hew"
suites = ["acceptance"]
timeout_seconds = 30

[case.env]
HEW_WORKERS = "4"

[case.expected]
stdout = ""
exit = 0
