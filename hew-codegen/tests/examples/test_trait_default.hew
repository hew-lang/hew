type Widget {
    id: int;
}

trait Describable {
    fn describe(w: Widget) -> int;
    fn default_value(w: Widget) -> int {
        42
    }
}

impl Describable for Widget {
    fn describe(w: Widget) -> int {
        w.id
    }
}

fn main() {
    let w = Widget { id: 10 };
    println(w.describe());
    println(w.default_value());
}
