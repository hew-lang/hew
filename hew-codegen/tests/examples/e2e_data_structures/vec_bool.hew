fn main() -> int {
    let v: Vec<bool> = Vec::new();
    v.push(true);
    v.push(false);
    v.push(true);

    if v.get(0) == true {
        if v.get(1) == false {
            if v.get(2) == true {
                if v.len() == 3 {
                    println("PASS");
                    0
                } else { 1 }
            } else { 1 }
        } else { 1 }
    } else { 1 }
}
