[[case]]
id = "isize_shift_oob_traps"
intent = "The guard traps: the fixture reports ShiftOutOfRange and exits 1 under the one exit rule."
source = "../vertical-slice/accept/isize_shift_oob_traps.hew"
suites = ["acceptance"]
timeout_seconds = 30

[case.env]
HEW_WORKERS = "4"

[case.expected]
stdout = ""
stderr = "hew: failure: ShiftOutOfRange (204)\n"
exit = 1
