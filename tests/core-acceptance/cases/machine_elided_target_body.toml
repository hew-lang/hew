[[case]]
id = "machine_elided_target_body"
intent = "The fixture runs to completion and prints exactly its recorded output."
source = "../vertical-slice/accept/machine_elided_target_body.hew"
suites = ["acceptance"]
timeout_seconds = 30

[case.env]
HEW_WORKERS = "4"

[case.expected]
stdout = "StateB\nStateB\n"
exit = 0
