type Pair<A, B> { a: A, b: B, }

fn main() {
    let p = Pair { a: "alpha", b: "beta" };
    let q = clone p;
    let sum = p.a.len() + p.b.len() + q.a.len() + q.b.len();
    print(sum);
    print("OK");
}
