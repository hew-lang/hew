fn make_pair(base: i64) -> fn(i64) -> i64 {
    let inner = |x: i64| x + base;
    |y: i64| inner(y) + 1
}
fn main() -> i64 {
    var total: i64 = 0;
    var i: i64 = 0;
    while i < 50 {
        let f = make_pair(i);
        total = total + f(10);
        i = i + 1;
    }
    total
}
