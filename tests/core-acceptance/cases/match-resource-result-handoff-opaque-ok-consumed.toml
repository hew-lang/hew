[[case]]
id = "match-resource-result-handoff-opaque-ok-consumed"
intent = "Exact-value ownership oracle for resources selected out of call-result matches. Shape `opaque_ok_consumed` (resource result match handoffs drop every payload exactly once) runs to its exact outcome with no leak, double free or use after free under ASan/LSan."
source = "cases/match-resource-result-handoff-opaque-ok-consumed.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 60

[case.expected]
stdout = ""
exit = 0
