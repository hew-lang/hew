[[case]]
id = "composite-yield-record-yield"
intent = "Per-yield leak / double-free oracle for owned-composite stream elements: a `receive gen fn` yielding a record/enum whose fields own heap must free BOTH per-yield copies exactly once — the pump's producer copy (released on the stream-send… Shape `record_yield_df` (record yield stream freed exactly once under malloc scribble) runs to its exact outcome with no leak, double free or use after free under ASan/LSan."
source = "cases/composite-yield-record-yield.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 60

[case.expected]
stdout = ""
exit = 0
