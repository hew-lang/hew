fn main() {
    let x: (i64, string) = (1, "x0");
    let y: (i64, string) = (2, "x1");
    let v: Vec<(i64, string)> = Vec.new();
    v.push(x);
    v.push(y);
    let n = v.len();
    println(f"len={n}");
}
