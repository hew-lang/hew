// Regression: regex.Pattern values created in helper functions and nested
// scopes can be explicitly released before scope exit without crashing.

import std::text::regex;

fn check_digit(s: String) -> bool {
    let re = regex.new("[0-9]+");
    let matched = re.is_match(s);
    re.free();
    matched
}

fn main() {
    {
        let re = regex.new("^hello");
        println(re.is_match("hello world"));
        println(re.is_match("world hello"));
        re.free();
    }

    println(check_digit("abc123"));
    println(check_digit("no digits"));
    println("done");
}
