fn std_channels_probe() -> i32 {
    7
}

fn std_channels(
    stream: Stream<i32>,
    sink: Sink<i32>,
    duplex: Duplex<i32, string>,
) -> i32 {
    stream.recv();
    sink.send(std_channels_probe());
    let _ = duplex.send(1);
    std_channels_probe()
}
