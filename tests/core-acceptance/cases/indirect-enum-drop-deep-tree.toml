[[case]]
id = "indirect-enum-drop-deep-tree"
intent = "Indirect-enum heap-node drop leak oracle. Shape `deep_tree_high` (indirect enum deep tree leak slope below tolerance) runs to its exact outcome with no leak, double free or use after free under ASan/LSan."
source = "cases/indirect-enum-drop-deep-tree.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 60

[case.expected]
stdout = "ok"
exit = 0
