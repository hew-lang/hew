import ambiguity.machine_left.{ ClashMachine as LeftMachine };
import ambiguity.machine_right.{ ClashMachine as RightMachine };

fn main() {
    let _left = LeftMachine.Idle;
    let _right = RightMachine.Idle;
}
