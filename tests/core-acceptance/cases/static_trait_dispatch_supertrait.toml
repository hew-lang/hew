[[case]]
id = "static_trait_dispatch_supertrait"
intent = "Accept fixture: Static trait dispatch — supertrait method via sub bound Method declared in Base, accessed through T: Extended (where Extended: Base)."
source = "../vertical-slice/accept/static_trait_dispatch_supertrait.hew"
suites = ["acceptance"]
timeout_seconds = 30

[case.env]
HEW_WORKERS = "4"

[case.expected]
stdout = ""
exit = 0
