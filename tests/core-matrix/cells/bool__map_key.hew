fn main() {
    let k: bool = true;
    let m: HashMap<bool, i64> = HashMap.new();
    m.insert(k, 9);
    match m.get(k) {
        Option.Some(v) => println(f"got {v}"),
        None => println("missing"),
    }
}
