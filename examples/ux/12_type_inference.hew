// Can the compiler infer types?

fn add(a: i32, b: i32) -> i32 {
    a + b
}

fn main() {
    let x = 10;         // infer i32?
    let y = 20;         // infer i32?
    let result = add(x, y);  // infer i32?
    println(result);
}
