[[case]]
id = "enum_resource_heap_sibling_asan"
intent = "The fixture runs to completion and prints exactly its recorded output."
source = "../vertical-slice/accept/enum_resource_heap_sibling_asan.hew"
suites = ["acceptance"]
timeout_seconds = 30

[case.env]
HEW_WORKERS = "4"

[case.expected]
stdout = "root\n"
exit = 0
