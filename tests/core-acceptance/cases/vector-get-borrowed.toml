[[case]]
id = "vector-get-borrowed"
intent = "D490: `get` copies the element out, so a Vec of a clone-free `#[resource]` type refuses every `get` - in bounds, repeated and past the end alike - rather than handing out a second owner. The owning drain stays the way an element leaves."
source = "cases/vector-get-borrowed.hew"
suites = ["acceptance"]
timeout_seconds = 30
kind = "check"

[[case.expected.diagnostics]]
code = "InvalidOperation"
line = 26
column = 18
message = "E_ELEMENT_NO_COPY: `Vec<Token>` cannot be read with `get`"

[[case.expected.diagnostics]]
code = "InvalidOperation"
line = 27
column = 18
message = "E_ELEMENT_NO_COPY: `Vec<Token>` cannot be read with `get`"

[[case.expected.diagnostics]]
code = "InvalidOperation"
line = 28
column = 21
message = "E_ELEMENT_NO_COPY: `Vec<Token>` cannot be read with `get`"
