[[case]]
id = "closure-env-drop-gen-transitive-capture-outlives-poisoned"
intent = "Closure-environment scope-exit drop oracles: per-iteration leak slope plus poisoned-allocator no-double-free pins for the heap-env closure shapes this lane completes (nested env-of-env, type-parameterised owned capture, and the `BorrowMut`… Shape `gen_transitive_capture_outlives_poisoned` (generator transitive capture outliving its generator is not a use after free, generator transitive capture outliving its generator survives the poisoned allocator) runs to its exact outcome with no leak, double free or use after free under ASan/LSan."
source = "cases/closure-env-drop-gen-transitive-capture-outlives-poisoned.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 60

[case.expected]
stdout = "after=11\n"
exit = 0
