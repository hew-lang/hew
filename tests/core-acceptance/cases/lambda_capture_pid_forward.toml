[[case]]
id = "lambda_capture_pid_forward"
intent = "The fixture runs to completion and exits 42."
source = "../vertical-slice/accept/lambda_capture_pid_forward.hew"
suites = ["acceptance"]
timeout_seconds = 30

[case.env]
HEW_WORKERS = "4"

[case.expected]
stdout = ""
exit = 42
