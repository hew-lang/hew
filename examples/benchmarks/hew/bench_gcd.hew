fn gcd(a: int, b: int) -> int {
    var x = a;
    var y = b;
    while y != 0 {
        let temp = y;
        y = x % temp;
        x = temp;
    }
    x
}

const ITERS: int = 5000000;

fn main() {
    var sum = 0;
    for i in 0..ITERS {
        sum = sum + gcd(1071, 462);
    }
    println(ITERS);
    println(sum);
}
