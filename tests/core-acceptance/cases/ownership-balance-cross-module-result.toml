[[case]]
id = "ownership-balance-cross-module-result"
intent = "Ownership-balance fixture. The ownership checker balances every obligation, so the program checks with no diagnostics."
source = "fixtures/ownership-balance/cross_module_result/main.hew"
suites = ["acceptance"]
timeout_seconds = 30
kind = "check"
