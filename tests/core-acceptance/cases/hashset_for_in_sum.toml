[[case]]
id = "hashset_for_in_sum"
intent = "The fixture runs to completion and exits 60."
source = "../vertical-slice/accept/hashset_for_in_sum.hew"
suites = ["acceptance"]
timeout_seconds = 30

[case.env]
HEW_WORKERS = "4"

[case.expected]
stdout = ""
exit = 60
