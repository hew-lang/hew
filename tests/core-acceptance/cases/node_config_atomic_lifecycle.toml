[[case]]
id = "node_config_atomic_lifecycle"
intent = "The fixture runs to completion and exits 0."
source = "../vertical-slice/accept/node_config_atomic_lifecycle.hew"
suites = ["acceptance"]
timeout_seconds = 30

[case.env]
HEW_WORKERS = "4"

[case.expected]
stdout = ""
exit = 0
