[[case]]
id = "var-overwrite-record-consume-reassign"
intent = "`var`-local overwrite leak oracles: reassigning an owned `var` in a loop must release the prior value, not just the final one (#53). Shape `record_consume_reassign` (record consume then reassign no double free) runs to its exact outcome with no leak, double free or use after free under ASan/LSan."
source = "cases/var-overwrite-record-consume-reassign.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 60

[case.expected]
stdout = ""
exit = 5
