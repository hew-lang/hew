[[case]]
id = "gen_drop_before_first_next"
intent = "Proving-gate fixture (leak fix): CONSTRUCT-AND-DROP BEFORE FIRST `.next()`."
source = "../vertical-slice/accept/gen_drop_before_first_next.hew"
suites = ["acceptance"]
timeout_seconds = 30

[case.env]
HEW_WORKERS = "4"

[case.expected]
stdout = "done\n"
exit = 0
