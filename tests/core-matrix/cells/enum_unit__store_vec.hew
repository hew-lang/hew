enum Colour {
    Red,
    Green,
}

fn main() {
    let x: Colour = .Red;
    let y: Colour = .Green;
    let v: Vec<Colour> = Vec.new();
    v.push(x);
    v.push(y);
    let n = v.len();
    println(f"len={n}");
}
