[[case]]
id = "let_else_tuple_payload"
intent = "The fixture runs to completion and prints exactly its recorded output."
source = "../vertical-slice/accept/let_else_tuple_payload.hew"
suites = ["acceptance"]
timeout_seconds = 30

[case.env]
HEW_WORKERS = "4"

[case.expected]
stdout = "ok=payload:42\nerr=fallback\n"
exit = 0
