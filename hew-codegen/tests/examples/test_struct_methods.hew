type Point {
    x: f64;
    y: f64;
}

trait PointOps {
    fn sum(pt: Point) -> f64;
    fn scale(pt: Point, factor: f64) -> Point;
}

impl PointOps for Point {
    fn sum(pt: Point) -> f64 {
        pt.x + pt.y
    }

    fn scale(pt: Point, factor: f64) -> Point {
        Point { x: pt.x * factor, y: pt.y * factor }
    }
}

fn main() {
    let p = Point { x: 3.0, y: 4.0 };
    println(p.x);
    println(p.y);
    let s = p.sum();
    println(s);
    let p2 = p.scale(2.0);
    println(p2.x);
    println(p2.y);
}
