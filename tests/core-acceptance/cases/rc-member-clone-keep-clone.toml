[[case]]
id = "rc-member-clone-keep-clone"
intent = "Ownership oracle for `Rc` members under structural clone. Shape `keep_clone_leaks` (vec of rc dropping original before returned clone does not over release, vec of rc dropping original before returned clone leaks nothing) runs to its exact outcome with no leak, double free or use after free under ASan/LSan."
source = "cases/rc-member-clone-keep-clone.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 60

[case.expected]
stdout = ""
exit = 0
