import ambiguity.actor_left.{ClashActor};
import ambiguity.actor_right.{ClashActor};

fn main() {
    let worker = spawn ClashActor();
    match worker.ping() {
        .Ok(value) => println(value),
        .Err(_) => println("ask failed"),
    }
}
