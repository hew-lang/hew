[[case]]
id = "recv-loop-gen-stream-bytes-early-return"
intent = "Per-iteration recv-result drop on the loop back-edge. Shape `gen_stream_bytes_early_return_high` (gen stream bytes early return no per frame leak slope) runs to its exact outcome with no leak, double free or use after free under ASan/LSan."
source = "cases/recv-loop-gen-stream-bytes-early-return.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 60

[case.expected]
stdout = ""
exit = 0
