[[case]]
id = "handle-error-early-return-loan"
intent = "A channel send releases its sender loan on the fault edge its element evaluation opens, so an early return out of a handle-error block inside a receive-fn loop leaves no live borrow."
source = "cases/handle-error-early-return-loan.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 30

[case.expected]
stdout = "read: first\ninvalid: invalid UTF-8 at byte 0 (1-byte sequence)\n"
stderr = ""
exit = 0
