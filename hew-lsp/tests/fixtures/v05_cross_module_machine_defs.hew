pub machine Toggle {
    events {
        Flip,
    }

    state Off,
    state On,

    on Flip: Off => Toggle.On {
        Toggle.On
    }
    on Flip: On => Toggle.Off {
        Toggle.Off
    }
}
