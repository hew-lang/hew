[[case]]
id = "file-import-hir-diagnostic-provenance"
intent = "A HIR refusal on a file-imported declaration names the file that declared it. The splice pass owns the declaration, so the refusal carries the declaring file and is reported once."
source = "fixtures/file-import-hir-diagnostic-provenance/main.hew"
suites = ["acceptance"]
timeout_seconds = 30
kind = "check"

[[case.expected.diagnostics]]
code = "ResourceBoundaryParamMustConsume"
file = "file-import-hir-diagnostic-provenance/token.hew"
line = 13
column = 5
