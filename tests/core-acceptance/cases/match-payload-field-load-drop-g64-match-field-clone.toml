[[case]]
id = "match-payload-field-load-drop-g64-match-field-clone"
intent = "Retained string field-load drop leak oracle — the match/if-let payload arm. Shape `g64_match_field_clone_high` (match payload field clone leak slope below tolerance) runs to its exact outcome with no leak, double free or use after free under ASan/LSan."
source = "cases/match-payload-field-load-drop-g64-match-field-clone.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 60

[case.expected]
stdout = ""
exit = 132
