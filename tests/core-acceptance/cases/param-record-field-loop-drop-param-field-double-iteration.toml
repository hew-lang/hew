[[case]]
id = "param-record-field-loop-drop-param-field-double-iteration"
intent = "Regression oracle for a `for` loop iterating a field of a BY-VALUE record parameter. Shape `param_field_double_iteration` (param field double iteration reads live handle twice) runs to its exact outcome with no leak, double free or use after free under ASan/LSan."
source = "cases/param-record-field-loop-drop-param-field-double-iteration.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 60

[case.expected]
stdout = "total=60\n"
exit = 0
