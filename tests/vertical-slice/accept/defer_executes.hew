// Verify defer body actually executes at scope exit by calling exit().
// If defer runs, the process exits with 42 overriding the function return of 0.
fn main() -> i64 {
    defer exit(42);
    0
}
