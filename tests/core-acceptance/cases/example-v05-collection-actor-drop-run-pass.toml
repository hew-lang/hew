[[case]]
id = "example-v05-collection-actor-drop-run-pass"
intent = "An actor holding collections releases them at shutdown and the program runs to completion."
source = "../../examples/v05/collection_actor_drop_run_pass.hew"
suites = ["acceptance"]
timeout_seconds = 30

[case.env]
HEW_WORKERS = "4"

[case.expected]
stdout = "ok\n"
exit = 0
