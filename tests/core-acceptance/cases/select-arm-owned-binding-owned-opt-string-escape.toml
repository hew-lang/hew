[[case]]
id = "select-arm-owned-binding-owned-opt-string-escape"
intent = "Select-arm owned-binding leak oracle (#1875, select-arm half). Shape `owned_opt_string_escape_high` (opt string escape loop leak slope below tolerance) runs to its exact outcome with no leak, double free or use after free under ASan/LSan."
source = "cases/select-arm-owned-binding-owned-opt-string-escape.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 60

[case.expected]
stdout = ""
exit = 0
