// A normal return cannot discard an unfinished transaction.
#[linear]
type Txn {
    id: i64,
}

impl Txn {
    fn commit(consume self) -> i64 {
        0
    }
}

fn main() -> i64 {
    let t = Txn { id: 0 };
    42
}
