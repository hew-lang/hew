type Bag { items: Vec<i64> }

fn build() -> i64 {
    let b = Bag { items: [1, 2, 3] };
    let bs = [b; 3];
    bs.len()
}

fn main() {
    var total: i64 = 0;
    for i in 0..8 {
        total = total + build();
    }
    print(total);
    print("OK");
}
