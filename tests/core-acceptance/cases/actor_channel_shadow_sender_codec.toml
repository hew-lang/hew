[[case]]
id = "actor_channel_shadow_sender_codec"
intent = "The fixture runs to completion and prints exactly its recorded output."
source = "../vertical-slice/accept/actor_channel_shadow_sender_codec.hew"
suites = ["acceptance"]
timeout_seconds = 30

[case.env]
HEW_WORKERS = "4"

[case.expected]
stdout = "sender shadow 20\nreceiver shadow 22\nsum 42\n"
exit = 0
