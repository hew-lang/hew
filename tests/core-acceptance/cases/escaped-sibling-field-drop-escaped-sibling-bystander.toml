[[case]]
id = "escaped-sibling-field-drop-escaped-sibling-bystander"
intent = "Escaped-sibling field-drop oracle (#2212) — the empirical (compiled-binary) half of the sibling-discharge contract whose MIR side lives in `tests/core-acceptance/cases/escaped_sibling_field_drop.hew`. Shape `escaped_sibling_bystander_high` (escaped sibling bystander record leak slope below tolerance) runs to its exact outcome with no leak, double free or use after free under ASan/LSan."
source = "cases/escaped-sibling-field-drop-escaped-sibling-bystander.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 60

[case.expected]
stdout = ""
exit = 0
