fn main() {
    let x: HashMap<string, i64> = HashMap.new();
    x.insert("a", 1);
    x.insert("b", 2);
    let y = x;
    let y_l = y.len();
    let y_a = match y.get("a") { Option.Some(y_g) => y_g, None => -1, };
    println(f"len={y_l} a={y_a}");
}
