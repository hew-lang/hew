trait Greetable {
    fn greet(val: Self) -> int;
}

type Dog {
    legs: int;
}

type Cat {
    lives: int;
}

impl Greetable for Dog {
    fn greet(d: Dog) -> int {
        return d.legs;
    }
}

impl Greetable for Cat {
    fn greet(cat: Cat) -> int {
        return cat.lives;
    }
}

fn call_greet(obj: dyn Greetable) -> int {
    return obj.greet();
}

fn main() {
    let d = Dog { legs: 4 };
    let c = Cat { lives: 9 };
    println(call_greet(d));
    println(call_greet(c));
}
