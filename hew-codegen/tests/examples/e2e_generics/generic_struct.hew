type Pair<T> {
    first: T;
    second: T;
}

fn make_pair<T>(a: T, b: T) -> Pair<T> {
    Pair { first: a, second: b }
}

fn main() {
    let p = make_pair<int>(10, 20);
    println(p.first);
    println(p.second);
}
