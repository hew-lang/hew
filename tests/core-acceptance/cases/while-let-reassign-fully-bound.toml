[[case]]
id = "while-let-reassign-fully-bound"
intent = "Leak and exactly-once guards for reassigned `BindingRef` while-let scrutinees. Shape `while_let_reassign_fully_bound_high` (while let reassigned fully bound payload has flat leak slope) runs to its exact outcome with no leak, double free or use after free under ASan/LSan."
source = "cases/while-let-reassign-fully-bound.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 60

[case.expected]
stdout = ""
exit = 0
