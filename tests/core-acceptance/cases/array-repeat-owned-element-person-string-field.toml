[[case]]
id = "array-repeat-owned-element-person-string-field"
intent = "Double-free / independent-owner oracle for array-repeat `[x; N]` of an owned (heap-owning) record element. Shape `person_string_field` (string field record array repeat no double free) runs to its exact outcome with no leak, double free or use after free under ASan/LSan."
source = "cases/array-repeat-owned-element-person-string-field.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 60

[case.expected]
stdout = "24OK"
exit = 0
