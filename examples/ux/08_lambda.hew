// Lambda functions

fn apply(f: fn(int) -> int, x: int) -> int {
    f(x)
}

fn double_fn(n: int) -> int {
    n * 2
}

fn triple_fn(n: int) -> int {
    n * 3
}

fn main() {
    println(apply(double_fn, 10));
    println(apply(triple_fn, 10));
}
