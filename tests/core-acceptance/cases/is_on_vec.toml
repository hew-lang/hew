[[case]]
id = "is_on_vec"
intent = "`is` on `Vec` is rejected: D340 narrows the admission set to actor handles only, and `Vec` is a copy-on-write value with structural `==` (HEW-SPEC-2026 §3.4.3's value row)."
source = "../vertical-slice/reject/is_on_vec.hew"
suites = ["acceptance"]
timeout_seconds = 30
kind = "reject"

[[case.expected.diagnostics]]
code = "InvalidOperation"
line = 7
column = 22
message = "`is` compares identity, and `Vec<i64>` is a value type (E_IS_VALUE_TYPE) — use `==` to compare it by value; `is` applies to handles: actor references"
