[[case]]
id = "example-algo-pascal_triangle"
intent = "The published example compiles, runs and prints exactly what it advertises."
source = "../../examples/algos/pascal_triangle.hew"
suites = ["acceptance"]
timeout_seconds = 30

[case.env]
HEW_WORKERS = "4"

[case.expected]
stdout_file = "../../examples/algos/pascal_triangle.expected"
exit = 0
