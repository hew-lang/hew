[[case]]
id = "example-surface-http_await_service"
intent = "The published example compiles, runs and prints exactly what it advertises."
source = "../../examples/net/http_await_service.hew"
suites = ["acceptance"]
timeout_seconds = 30

[case.env]
HEW_WORKERS = "4"

[case.expected]
stdout_file = "../../examples/net/http_await_service.expected"
exit = 0
