fn main() -> i64 {
    var result = "";
    for outer in ["A", "B"] {
        for inner in ["skip", "keep", "end"] {
            if inner == "skip" {
                continue;
            }
            result = result + outer + inner;
            break;
        }
    }
    println(result);
    0
}
