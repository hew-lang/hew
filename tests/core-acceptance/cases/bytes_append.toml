[[case]]
id = "bytes_append"
intent = "`bytes.append(other)` extends the receiver with a copy of `other`."
source = "../vertical-slice/accept/bytes_append.hew"
suites = ["acceptance"]
timeout_seconds = 30

[case.env]
HEW_WORKERS = "4"

[case.expected]
stdout = ""
exit = 0
