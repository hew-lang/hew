[[case]]
id = "example-surface-pipe_record_elements"
intent = "The published example compiles, runs and prints exactly what it advertises."
source = "../../examples/v05/surfaces/pipe_record_elements.hew"
suites = ["acceptance"]
timeout_seconds = 30

[case.env]
HEW_WORKERS = "4"

[case.expected]
stdout_file = "../../examples/v05/surfaces/pipe_record_elements.expected"
exit = 0
