[[case]]
id = "mailbox_declared_admission"
intent = "A `mailbox(..)` view with no `on_full:` takes the destination's declared admission: `overflow fail` refuses a full queue with SendError.Full."
source = "../vertical-slice/accept/mailbox_declared_admission.hew"
suites = ["acceptance"]
timeout_seconds = 30

[case.env]
HEW_WORKERS = "1"

[case.expected]
stdout = "DERIVED_REJECT\n"
exit = 43
