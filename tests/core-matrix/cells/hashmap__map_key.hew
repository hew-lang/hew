fn main() {
    let k: HashMap<string, i64> = HashMap.new();
    k.insert("a", 1);
    k.insert("b", 2);
    let m: HashMap<HashMap<string, i64>, i64> = HashMap.new();
    m.insert(k, 9);
    match m.get(k) {
        Some(v) => println(f"got {v}"),
        None => println("missing"),
    }
}
