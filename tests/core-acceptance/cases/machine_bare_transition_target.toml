[[case]]
id = "machine_bare_transition_target"
intent = "The fixture runs to completion and prints exactly its recorded output."
source = "../vertical-slice/accept/machine_bare_transition_target.hew"
suites = ["acceptance"]
timeout_seconds = 30

[case.env]
HEW_WORKERS = "4"

[case.expected]
stdout = "Idle\nRunning\n"
exit = 0
