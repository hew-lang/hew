[[case]]
id = "dyn-trait-vec"
intent = "A heterogeneous `Vec<dyn Named>` accepts both implementors and the consuming iterator moves each trait object out in order, dispatching on it."
source = "cases/dyn-trait-vec.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 15

[case.expected]
stdout = "count=3\nperson ada\nmachine 7\nperson lin\nseen=3\n"
exit = 0
