[[case]]
id = "indirect-enum-drop-record-of-inline-enum"
intent = "Indirect-enum heap-node drop leak oracle. Shape `record_of_inline_enum_high` (indirect enum record of inline enum leak slope below tolerance, indirect enum record of inline enum no corruption under malloc scribble) runs to its exact outcome with no leak, double free or use after free under ASan/LSan."
source = "cases/indirect-enum-drop-record-of-inline-enum.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 60

[case.expected]
stdout = "ok"
exit = 0
