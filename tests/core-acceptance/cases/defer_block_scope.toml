[[case]]
id = "defer_block_scope"
intent = "The fixture runs to completion and exits 7."
source = "../vertical-slice/accept/defer_block_scope.hew"
suites = ["acceptance"]
timeout_seconds = 30

[case.env]
HEW_WORKERS = "4"

[case.expected]
stdout = ""
exit = 7
