fn main() {
    let x: string = "ab";
    let v: Vec<string> = Vec.new();
    v.push(x);
    match v.get(0) {
        Option.Some(e) => {
            println(f"{e}");
        },
        Option.None => println("missing"),
    }
}
