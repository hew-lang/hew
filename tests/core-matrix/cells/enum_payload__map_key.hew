enum Shape {
    Empty;
    Circle(i64);
    Rect { w: i64; h: i64 }
}

fn main() {
    let k: Shape = .Circle(3);
    let m: HashMap<Shape, i64> = HashMap.new();
    m.insert(k, 9);
    match m.get(k) {
        Option.Some(v) => println(f"got {v}"),
        None => println("missing"),
    }
}
