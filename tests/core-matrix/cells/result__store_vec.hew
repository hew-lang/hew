fn main() {
    let x: Result<i64, string> = Ok(6);
    let y: Result<i64, string> = Ok(7);
    let v: Vec<Result<i64, string>> = Vec::new();
    v.push(x);
    v.push(y);
    let n = v.len();
    println(f"len={n}");
}
