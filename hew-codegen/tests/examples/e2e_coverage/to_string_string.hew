// Tests to_string with a String argument (identity path — returns the value as-is).
// to_string(s: String) → s is legal because String implements Display.
fn main() {
    let s = "hello";
    println(to_string(s));
    println(to_string("world"));
}
