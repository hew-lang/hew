trait A {
    fn tag(self) -> string;
    fn alpha(self) -> string { "alpha default" }
}
type T { v: i64 }
impl A for T { fn tag(self) -> string { "A" } }
impl A for i64 { fn tag(self) -> string { "A" } }
fn via_a<X: A>(x: X) -> string { x.tag() }
fn main() {
    let t = T { v: 1 };
    println(via_a(t));
    let n: i64 = 3;
    println(via_a(n));
    println(t.alpha());
    println(n.alpha());
}
