[[case]]
id = "std_net_websocket_execution"
intent = "The fixture runs to completion and prints exactly its recorded output."
source = "../vertical-slice/accept/std_net_websocket_execution.hew"
suites = ["acceptance"]
timeout_seconds = 30

[case.env]
HEW_WORKERS = "4"

[case.expected]
stdout = "websocket:ok\n"
exit = 0
