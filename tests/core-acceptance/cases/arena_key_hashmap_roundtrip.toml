[[case]]
id = "arena_key_hashmap_roundtrip"
intent = "HashMap::get and HashMap::remove lower through Option<V> payload slots."
source = "../vertical-slice/accept/arena_key_hashmap_roundtrip.hew"
suites = ["acceptance"]
timeout_seconds = 30

[case.env]
HEW_WORKERS = "4"

[case.expected]
stdout = "through-map\nthrough-map\n"
exit = 0
