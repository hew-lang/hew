import std::stream;

fn main() {
    let (sink, input) = stream.pipe(8);

    sink.write("hello ");
    sink.write("world");
    sink.close();

    let result = input.collect();
    println(result);
}
