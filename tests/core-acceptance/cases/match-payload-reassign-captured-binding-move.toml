[[case]]
id = "match-payload-reassign-captured-binding-move"
intent = "Projected-payload move-out UAF / double-free oracle (issue #2523). Shape `captured_binding_move` (captured binding move out is valid) runs to its exact outcome with no leak, double free or use after free under ASan/LSan."
source = "cases/match-payload-reassign-captured-binding-move.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 60

[case.expected]
stdout = ""
exit = 0
