# W3.038 — Stage 0 audit (auto-locks substrate baseline)

**Base SHA pin:** `6a551a92` (v05-int-tip; `feat(parser,types): generic-actor spawn`).

## §1.2 Baseline probes (re-run actuals)

| Probe | Expected | Actual at base SHA | Verdict |
|---|---|---|---|
| B1 `LockInjectionFact` | 0 | 0 | ✓ no prior substrate |
| B2 `ReleaseAutoLock` | 0 | 0 | ✓ |
| B3 `enum ScopeExitAction` site | 1 (W3.021) | confirmed via `DropKind::Resource` LIFO infrastructure in `hew-mir/src/lower.rs` (no explicit `ScopeExitAction` symbol — W3.021 substrate folded into `DropPlan`/`ElabDrop` shape); auto-lock release rides the same LIFO drop stream | ✓ (substrate-shape match) |
| B4 `fn is_sync` inference | `hew-types/src/traits.rs:688` | confirmed `pub fn is_sync(&self, ty: &Ty) -> bool` at line 688 | ✓ |
| B5 `BorrowMut` capture mode | post-W3.035 ≥1 | confirmed at `hew-types/src/check/types.rs:381-384` (`BorrowMut` inferred from mutating projections) | ✓ PC-1 satisfied |
| B6 cancel-scope facts / `ObserveCancel` | ≥1 | live cancel-token operand swap landed at `1ba81af5`; `Terminator::ObserveCancel` cancel-observation seam wired (W3.036 c206264c) | ✓ PC-2 satisfied via per-DM6 alternative substrate |
| B7 runtime lock symbol | 0 (Q239 gap) | 0 (`hew_auto_mutex_*` mentioned in `closure_env.rs:57` doc-comment only — no FFI defined) | ✓ Q239 Option A unblocked |
| B8 language Mutex type | 0 | 0 (`std/` has no `Mutex` type — only `std::semaphore`) | ✓ no user-visible primitive |
| B9 W3.030 drop site | 1 region | confirmed `DropKind::Resource` LIFO interleaving in `hew-mir/src/lower.rs:12336-12639` and `:13092` | ✓ |
| B10 WASM lock TODO | implementer records | no pre-existing WASM mutex; `parking_lot` available native; WASM substrate is single-threaded scheduler (sequential execution invariant — see §1.4 below) | recorded |

## §1.3 Critical preconditions verdict

- **PC-1 (W3.035 Cluster A `BorrowMut` + `is_sync`)** — ✓ **SATISFIED**. Diagnostic `NonSyncMutCaptureCrossesSuspend` already emits at `hew-types/src/check/expressions.rs:4236` when (BorrowMut ∧ ¬is_sync ∧ has_suspend). Auto-lock substrate inverts this — when the trigger fires we now inject locks instead of (or in addition to) emitting the diagnostic.
- **PC-2 (cancel-scope facts)** — ✓ **SATISFIED** via the live cancel-token operand seam landed at `1ba81af5` plus `Terminator::ObserveCancel` at suspend points. Full `CancelScopeFacts` side-table was deferred per DM6 — but the substrate it would feed (suspend-point enumeration) is already discoverable through `HirExprKind` variants and the foundation API `ClosureEnvLayout::has_suspend_in_body()`. W3.038 consumes the foundation API directly per plan §1.3 fallback path.
- **PC-3 (W3.036 generator state-field exposure)** — ✓ **SATISFIED** via `Place::GenState { local, field }` substrate (see `hew-mir/src/model.rs:100, 448, 490` and `hew-cli/src/diagnostic.rs:462`). Explicit `GenStateFieldFact` deferred per DM6, but the Place projection is sufficient for the per-(binding, capture) granularity Q241 ratifies (R267=A: one lock per (binding, capture), shared across all state-machine field projections — fewer projections, not more, simplifies the substrate).
- **PC-4 (Q239 runtime primitive sourcing)** — ✓ **RATIFIED OPTION A** by orchestrator brief (R265=A). NEW runtime FFI: `hew_auto_mutex_alloc / _lock / _unlock / _free`. Compiler-only emission; no user-visible Hew `Mutex<T>` type.

## §1.4 Ratifications carried into this lane (from orchestrator brief)

- **R265 = Option A** — NEW runtime FFI `hew_auto_mutex_{alloc,lock,unlock,free}`. Compiler-only emission. (Resolves Q239.)
- **R266 = Option A** — per-access bracketing (acquire→access→release), **suspend OUTSIDE the lock window**. Avoids classic async-mutex deadlock. (Resolves Q240.)
- **R267 = Option A** — one lock per `(binding, capture)` pair; shared across state-machine field projections. (Resolves Q241.)
- Per-shared-capture granularity, cross-actor forbidden by design, no `#[no-auto-lock]` escape hatch — all carried forward from W3.034.

## §1.5 WASM parity note (R-1 mitigation)

Native: `parking_lot::Mutex<()>` boxed behind `*mut HewAutoMutex` opaque handle. WASM: this lane targets the substrate ABI; the WASM scheduler runs single-threaded (cooperative await-based) so the lock primitive can use a degenerate `std::cell::Cell<bool>` "claim" guard that fail-closes (`hew_actor_trap`-style) on re-entry — preserving Tenet 5 (WASM-parity: no silent always-unlocked stub). Implementation lands native path first; WASM parity stub fail-closes on contention.

## §1.6 R7 sentinel — no mutex bake-in beyond `hew_auto_mutex_*`

Final grep contract before commit body amend:
```
rg -n 'parking_lot|std::sync::Mutex|spin::Mutex' --type=rust | rg -v 'hew-runtime/src/auto_mutex.rs|^Cargo.lock'
```
Expected: ZERO hits in compiler-emitted symbol surface (allowlist, lowering, codegen). Only `hew-runtime/src/auto_mutex.rs` implementation interior may name the backing primitive.

## §1.7 Go/block verdict

**GO.** Stages 1–5 dispatchable. No blocker surfaced; all four PC gates satisfied.
