// Compiler corpus fixture: thunks and closure storage.
//
// Collects closure capture and higher-order function source shapes for
// compiler checking and raw-MIR parity. Focused codegen tests own closure-call
// and clone/drop thunk invariants.
fn apply(f: fn(i64) -> i64, x: i64) -> i64 {
    f(x)
}

fn apply_twice(f: fn(i64) -> i64, x: i64) -> i64 {
    f(f(x))
}

fn make_adder(n: i64) -> fn(i64) -> i64 {
    |x| x + n
}

fn make_multiplier(n: i64) -> fn(i64) -> i64 {
    |x| x * n
}

fn main() {
    let double = |x: i64| x * 2;
    println(apply(double, 21));
    println(apply_twice(double, 3));
    let add10 = make_adder(10);
    println(add10(32));
    let triple = make_multiplier(3);
    println(apply(triple, 7));

    // closure capturing two variables
    let base = 100;
    let step = 5;
    let stepper = |n: i64| base + n * step;
    println(stepper(4));
}
