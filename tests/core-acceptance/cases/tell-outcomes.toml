[[case]]
id = "tell-outcomes"
intent = "A call through a `mailbox(..)` view submits without waiting: it yields `Result<Delivery, SendFailure<_>>` and `let _ =` discards a refusal deliberately, while a call on the actor handle waits for the handler."
source = "cases/tell-outcomes.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 15

[case.expected]
stdout = "accepted\nprocessed a\nprocessed b\n2\n"
exit = 0
