[[case]]
id = "match_destructure_guarded_record"
intent = "Accept: a guard on a record match-destructure arm."
source = "../vertical-slice/accept/match_destructure_guarded_record.hew"
suites = ["acceptance"]
timeout_seconds = 30

[case.env]
HEW_WORKERS = "4"

[case.expected]
stdout = "b-payload\n"
exit = 0
