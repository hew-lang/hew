[[case]]
id = "actor-handle-legacy-types-refused"
intent = "The spellings an actor's own type replaces are refused where they are written, and each refusal names the type to write instead."
source = "cases/actor-handle-legacy-types-refused.hew"
suites = ["acceptance"]
timeout_seconds = 30
kind = "check"

[[case.expected.diagnostics]]
code = "E_ACTOR_HANDLE_TYPE"
line = 7
column = 20
message = "E_ACTOR_HANDLE_TYPE: `LocalPid<Greeter>` is not a type; an actor is the type of its handle"

[[case.expected.diagnostics]]
code = "E_ACTOR_HANDLE_TYPE"
line = 11
column = 14
message = "E_ACTOR_HANDLE_TYPE: `Pid<Greeter>` is not a type; an actor is the type of its handle"

[[case.expected.diagnostics]]
code = "E_ACTOR_HANDLE_TYPE"
line = 15
column = 24
message = "E_ACTOR_HANDLE_TYPE: `LambdaPid<i64, i64>` is not a type; an actor is the type of its handle"
