fn main() {
    let k: (i64, string) = (1, "x0");
    let m: HashMap<(i64, string), i64> = HashMap.new();
    m.insert(k, 9);
    match m.get(k) {
        .Some(v) => println(f"got {v}"),
        .None => println("missing"),
    }
}
