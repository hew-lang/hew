[[case]]
id = "wire_json_fresh_concat_operand"
intent = "The fixture runs to completion and exits 42."
source = "../vertical-slice/accept/wire_json_fresh_concat_operand.hew"
suites = ["acceptance"]
timeout_seconds = 30

[case.env]
HEW_WORKERS = "4"

[case.expected]
stdout = ""
exit = 42
