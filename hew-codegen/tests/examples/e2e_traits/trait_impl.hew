trait Greetable {
    fn greet(val: Self) -> string;
}

type Person {
    name: string;
}

impl Greetable for Person {
    fn greet(p: Person) -> string {
        string_concat("Hello, ", p.name)
    }
}

fn main() {
    let p = Person { name: "Alice" };
    println(p.greet());
}
