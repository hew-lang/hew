[[case]]
id = "i32_match_literal"
intent = "Regression: bare integer literal match arms must coerce to the i32 scrutinee type instead of defaulting the comparand to i64."
source = "../vertical-slice/accept/i32_match_literal.hew"
suites = ["acceptance"]
timeout_seconds = 30

[case.env]
HEW_WORKERS = "4"

[case.expected]
stdout = ""
exit = 42
