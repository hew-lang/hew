fn first<T>(a: T, b: T) -> T {
    a
}

fn second<T>(a: T, b: T) -> T {
    b
}

fn main() {
    println(f"{first(10, 20)}");
    println(f"{second(10, 20)}");
    println(first("alpha", "beta"));
    println(second("alpha", "beta"));
}
