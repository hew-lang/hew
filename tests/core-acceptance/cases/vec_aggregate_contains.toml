[[case]]
id = "vec_aggregate_contains"
intent = "The fixture runs to completion and exits 0."
source = "../vertical-slice/accept/vec_aggregate_contains.hew"
suites = ["acceptance"]
timeout_seconds = 30

[case.env]
HEW_WORKERS = "4"

[case.expected]
stdout = ""
exit = 0
