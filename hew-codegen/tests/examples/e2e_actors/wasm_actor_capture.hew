// Test: Lambda actor with closure capture
// Exercises: lambda actor, captured outer variable
fn main() {
    let base = 100;
    let adder = actor |x: int| {
        println(base + x);
    };
    adder.send(1);
    adder.send(42);
}
