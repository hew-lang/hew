[[case]]
id = "match-bound-field-original-bound-string-record"
intent = "Bound-string original discharge oracle for match destructures that CONSUME the scrutinee root — the empirical (compiled-binary) half of the per-field accounting matrix whose skipped-field sibling lives in… Shape `bound_string_record_high` (bound string record field leak slope below tolerance) runs to its exact outcome with no leak, double free or use after free under ASan/LSan."
source = "cases/match-bound-field-original-bound-string-record.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 60

[case.expected]
stdout = ""
exit = 0
