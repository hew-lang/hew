fn main() {
    let k = gen {
        yield 1;
        yield 2;
    };
    let m: HashMap<Generator<i64, ()>, i64> = HashMap::new();
    m.insert(k, 9);
    match m.get(k) {
        Some(v) => println(f"got {v}"),
        None => println("missing"),
    }
}
