[[case]]
id = "hashmap_managed_record_key"
intent = "The fixture runs to completion and exits 0."
source = "../vertical-slice/accept/hashmap_managed_record_key.hew"
suites = ["acceptance"]
timeout_seconds = 30

[case.env]
HEW_WORKERS = "4"

[case.expected]
stdout = ""
exit = 0
