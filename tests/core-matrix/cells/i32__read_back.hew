fn main() {
    let x: i32 = 7;
    let v: Vec<i32> = Vec.new();
    v.push(x);
    match v.get(0) {
        Option.Some(e) => {
            println(f"{e}");
        },
        None => println("missing"),
    }
}
