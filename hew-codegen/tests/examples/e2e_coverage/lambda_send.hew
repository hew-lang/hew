// E2E test: lambda actor .send() exercises generateSendExpr.
fn main() {
    let counter = actor |x: int| {
        println(x);
    };
    counter.send(42);
    sleep_ms(50);
    counter.send(99);
    sleep_ms(50);
    println("done");
}
