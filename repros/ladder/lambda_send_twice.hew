actor Relay {
    receive fn go(h: LambdaPid<i64, ()>) {
        h.send(7);
    }
}
fn main() {
    let w = actor |x: i64| { println(x); };
    let r = spawn Relay();
    let _ = r.go(w);
    let _ = r.go(w);
    let _ = w.send(1);
}
