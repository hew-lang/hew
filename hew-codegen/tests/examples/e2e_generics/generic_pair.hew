fn first<T>(a: T, b: T) -> T {
    a
}

fn max_val<T>(a: T, b: T) -> T {
    if a > b {
        a
    } else {
        b
    }
}

fn main() {
    println(first<int>(10, 20));
    println(max_val<int>(30, 50));
}
