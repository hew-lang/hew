fn main() {
    let x: string = "ab";
    let m: HashMap<string, string> = HashMap::new();
    m.insert("k", x);
    match m.get("k") {
        Some(e) => {
            println(f"{e}");
        },
        None => println("missing"),
    }
}
