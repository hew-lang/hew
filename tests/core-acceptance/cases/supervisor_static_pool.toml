[[case]]
id = "supervisor_static_pool"
intent = "The fixture runs to completion and exits 24."
source = "../vertical-slice/accept/supervisor_static_pool.hew"
suites = ["acceptance"]
timeout_seconds = 30

[case.env]
HEW_WORKERS = "4"

[case.expected]
stdout = ""
exit = 24
