[[case]]
id = "actor_arg_value_field_projection"
intent = "The fixture runs to completion and prints exactly its recorded output."
source = "../vertical-slice/accept/actor_arg_value_field_projection.hew"
suites = ["acceptance"]
timeout_seconds = 30

[case.env]
HEW_WORKERS = "4"

[case.expected]
stdout = "scalar=3\nstring=4\nvec=2\ncount=3 name=held items=2\n"
exit = 0
