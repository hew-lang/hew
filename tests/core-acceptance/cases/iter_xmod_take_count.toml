[[case]]
id = "iter_xmod_take_count"
intent = "The fixture runs to completion and exits 3."
source = "../vertical-slice/accept/iter_xmod_take_count.hew"
suites = ["acceptance"]
timeout_seconds = 30

[case.env]
HEW_WORKERS = "4"

[case.expected]
stdout = ""
exit = 3
