name: Codegen Silent-Failure Lint
on:
  pull_request:
    paths:
      - 'hew-codegen/src/**'

jobs:
  codegen-lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Check for silent error swallowing in codegen
        run: make codegen-lint
