[[case]]
id = "fresh_vec_index_projection_drop"
intent = "The fixture runs to completion and exits 0."
source = "../vertical-slice/accept/fresh_vec_index_projection_drop.hew"
suites = ["acceptance"]
timeout_seconds = 30

[case.env]
HEW_WORKERS = "4"

[case.expected]
stdout = ""
exit = 0
