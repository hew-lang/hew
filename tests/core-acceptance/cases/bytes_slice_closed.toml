[[case]]
id = "bytes_slice_closed"
intent = "A closed byte-slice `b[1..3]` yields those bytes and its length is the exit code."
source = "../vertical-slice/accept/bytes_slice_closed.hew"
suites = ["acceptance"]
timeout_seconds = 30

[case.env]
HEW_WORKERS = "4"

[case.expected]
stdout = "bc\n"
exit = 2
