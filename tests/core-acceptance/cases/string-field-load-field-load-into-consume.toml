[[case]]
id = "string-field-load-field-load-into-consume"
intent = "Constant-leak / double-free oracle for reading a `string` field out of an owned aggregate (`r.field`, `t.0`). Shape `field_load_into_consume` (field load into consume no double free) runs to its exact outcome with no leak, double free or use after free under ASan/LSan."
source = "cases/string-field-load-field-load-into-consume.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 60

[case.expected]
stdout = "OK\n"
exit = 0
