fn main() {
    let a: Option<i64> = Option.Some(5);
    let b: Option<i64> = Option.Some(5);
    let c: Option<i64> = Option.Some(6);
    let same = a == b;
    let diff = a == c;
    println(f"{same} {diff}");
}
