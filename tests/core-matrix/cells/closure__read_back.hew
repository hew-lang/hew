fn main() {
    let x = |v: i64| v * 2;
    let v: Vec<fn(i64) -> i64> = Vec.new();
    v.push(x);
    match v.get(0) {
        Some(e) => {
            let e_r = e(21);
            println(f"{e_r}");
        },
        None => println("missing"),
    }
}
