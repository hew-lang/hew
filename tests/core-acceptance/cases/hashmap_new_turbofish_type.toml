[[case]]
id = "hashmap_new_turbofish_type"
intent = "HashMap<K, V>.new() accepts explicit type arguments even though the stdlib catalog row declares no callable type parameters."
source = "../vertical-slice/accept/hashmap_new_turbofish_type.hew"
suites = ["acceptance"]
timeout_seconds = 30

[case.env]
HEW_WORKERS = "4"

[case.expected]
stdout = ""
exit = 0
