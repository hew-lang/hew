//! Registering an ordinary package record must not disturb the builtin Result
//! enum used by unrelated stdlib APIs.

import hew.testffi;

import std.string;

fn main() {
    match string.to_int("42") {
        .Ok(value) => println(value),
        .Err(_) => println("parse failed"),
    }
}
