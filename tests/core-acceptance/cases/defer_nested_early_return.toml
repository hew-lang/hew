[[case]]
id = "defer_nested_early_return"
intent = "The fixture runs to completion and exits 10."
source = "../vertical-slice/accept/defer_nested_early_return.hew"
suites = ["acceptance"]
timeout_seconds = 30

[case.env]
HEW_WORKERS = "4"

[case.expected]
stdout = ""
exit = 10
