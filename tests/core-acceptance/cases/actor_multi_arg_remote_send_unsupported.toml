[[case]]
id = "actor_multi_arg_remote_send_unsupported"
intent = "Reject fixture: a remote send whose ActorMsg names no single-parameter receive member."
source = "../vertical-slice/reject/actor_multi_arg_remote_send_unsupported.hew"
suites = ["acceptance"]
timeout_seconds = 30
kind = "reject"

[[case.expected.diagnostics]]
code = "BoundsNotSatisfied"
line = 21
column = 47
message = "remote actor `Logger` has no receive fn taking `i64` and returning `()`, the message and reply its `ActorMsg` impl names"
