[[case]]
id = "mailbox_declared_admission_role"
intent = "A supervised role's delivery view derives its admission from the role's declared actor type, as a directly spawned handle does."
source = "../vertical-slice/accept/mailbox_declared_admission_role.hew"
suites = ["acceptance"]
timeout_seconds = 30

[case.env]
HEW_WORKERS = "1"

[case.expected]
stdout = "ROLE_DERIVED_REJECT\n"
exit = 43
