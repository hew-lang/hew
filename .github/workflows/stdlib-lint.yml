name: Stdlib Int-Surface Lint
on:
  pull_request:
    paths:
      - 'std/**/*.hew'
      - 'std/*.hew'
      - 'scripts/lint-stdlib-int-surface.sh'
      - 'docs/stdlib-style-contract.md'

jobs:
  stdlib-int-surface:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Check stdlib int surface
        run: make stdlib-lint
