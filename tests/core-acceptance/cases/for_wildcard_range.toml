[[case]]
id = "for_wildcard_range"
intent = "The fixture runs to completion and exits 3."
source = "../vertical-slice/accept/for_wildcard_range.hew"
suites = ["acceptance"]
timeout_seconds = 30

[case.env]
HEW_WORKERS = "4"

[case.expected]
stdout = ""
exit = 3
