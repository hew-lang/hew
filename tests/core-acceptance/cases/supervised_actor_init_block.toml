[[case]]
id = "supervised_actor_init_block"
intent = "The fixture runs to completion and exits 42."
source = "../vertical-slice/accept/supervised_actor_init_block.hew"
suites = ["acceptance"]
timeout_seconds = 30

[case.env]
HEW_WORKERS = "4"

[case.expected]
stdout = ""
exit = 42
