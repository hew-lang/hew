// A transaction is committed on each normal branch.
#[linear]
type Txn {
    id: i64,
}

impl Txn {
    fn commit(consume self) -> i64 {
        0
    }
}

fn main() -> i64 {
    let t = Txn { id: 0 };
    let _r = if 1 == 1 {
        let _a = t.commit();
        7
    } else {
        let _b = t.commit();
        8
    };
    42
}
