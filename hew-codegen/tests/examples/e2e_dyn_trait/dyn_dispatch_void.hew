trait Printable {
    fn show(val: Self);
}

type Dog {
    legs: int;
}

type Cat {
    lives: int;
}

impl Printable for Dog {
    fn show(d: Dog) {
        println(d.legs);
    }
}

impl Printable for Cat {
    fn show(cat: Cat) {
        println(cat.lives);
    }
}

fn display(obj: dyn Printable) {
    obj.show();
}

fn main() {
    let d = Dog { legs: 4 };
    let c = Cat { lives: 9 };
    display(d);
    display(c);
}
