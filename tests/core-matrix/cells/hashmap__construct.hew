fn main() {
    let x: HashMap<string, i64> = HashMap.new();
    x.insert("a", 1);
    x.insert("b", 2);
    let x_l = x.len();
    let x_a = match x.get("a") { Option.Some(x_g) => x_g, Option.None => -1, };
    println(f"len={x_l} a={x_a}");
}
