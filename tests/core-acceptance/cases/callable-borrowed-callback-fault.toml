[[case]]
id = "callable-borrowed-callback-fault"
intent = "An indirect borrowed invocation fault releases callback locals, captured text and caller owners and prevents all post-fault output."
source = "cases/callable-borrowed-callback-fault.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 15

[case.expected]
stdout = ""
stderr = "hew: failure: DivideByZero (202)\n"
exit = 202
