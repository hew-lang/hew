fn hold<T>(consume x: T) -> fn() -> i64 {
    move || { let _h = x; 0 }
}
fn main() -> i64 {
    var i: i64 = 0;
    while i < 50 {
        let g = hold("per-iteration-owned-captured-string");
        i = i + 1;
    }
    0
}
