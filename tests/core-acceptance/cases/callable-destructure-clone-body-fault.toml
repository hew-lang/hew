[[case]]
id = "callable-destructure-clone-body-fault"
intent = "A fault in the body after destructuring a once, clone callable releases heap captures, the uncalled sibling and any retained original field."
source = "cases/callable-destructure-clone-body-fault.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 30

[case.expected]
stdout = "callback entered\n"
stderr = "hew: failure: DivideByZero (202)\n"
exit = 1
