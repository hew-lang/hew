[[case]]
id = "machine_fork_args_spawn"
intent = "Accept fixture: `fork child = worker(42)` inside a machine state entry block compiles end to end."
source = "../vertical-slice/accept/machine_fork_args_spawn.hew"
suites = ["acceptance"]
timeout_seconds = 30

[case.env]
HEW_WORKERS = "4"

[case.expected]
stdout = ""
exit = 0
