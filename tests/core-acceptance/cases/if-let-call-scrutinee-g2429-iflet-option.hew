fn f() -> Option<string> { .Some("g2429-iflet-string".to_upper()) }

fn main() -> i64 {
    var total = 0;
    var i = 0;
    while i < 50 {
        if let .Some(b) = f() { total = total + b.len(); }
        i = i + 1;
    }
    if total > 0 { 0 } else { 1 }
}
