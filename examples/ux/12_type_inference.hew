// Can the compiler infer types?

fn add(a: int, b: int) -> int {
    a + b
}

fn main() {
    let x = 10;         // infer int
    let y = 20;         // infer int
    let result = add(x, y);  // infer int
    println(result);
}
