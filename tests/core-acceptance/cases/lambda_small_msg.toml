[[case]]
id = "lambda_small_msg"
intent = "The fixture runs to completion and prints exactly its recorded output."
source = "../vertical-slice/accept/lambda_small_msg.hew"
suites = ["acceptance"]
timeout_seconds = 30

[case.env]
HEW_WORKERS = "4"

[case.expected]
stdout = "bool true\nbool false\ni32 got 42\ni32 got -1\n"
exit = 0
