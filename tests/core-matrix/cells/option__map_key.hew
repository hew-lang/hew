fn main() {
    let k: Option<i64> = Option.Some(5);
    var m: HashMap<Option<i64>, i64> = HashMap.new();
    m.insert(k, 9);
    match m.get(k) {
        Option.Some(v) => println(f"got {v}"),
        Option.None => println("missing"),
    }
}
