[[case]]
id = "owned-project-param-tail-drop-owned-project-partitioned-consume"
intent = "Leak and poisoned-allocator oracle for owned record/tuple project matches over by-value parameters. Shape `owned_project_partitioned_consume_high` (partitioned consume closes each resource exactly once, partitioned consume leak slope below tolerance) runs to its exact outcome with no leak, double free or use after free under ASan/LSan."
source = "cases/owned-project-param-tail-drop-owned-project-partitioned-consume.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 60

[case.expected]
stdout = "closed-1\nclosed-2\npartitioned-ok\n"
exit = 0
