import std::stream;

fn main() {
    let (sink, input) = stream.pipe(8);

    sink.write("hello");
    sink.write("world");
    sink.close();

    let mapped = input.map((s) => s + "!");

    for await item in mapped {
        println(item);
    }
}
