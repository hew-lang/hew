enum Colour {
    Red;
    Green;
}

fn make() -> Colour {
    let m: Colour = .Red;
    m
}

fn main() {
    let x = make();
    match x {
        .Red => println("Red"),
        .Green => println("Green"),
    }
}
