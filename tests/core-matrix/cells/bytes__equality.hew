fn main() {
    var a: bytes = bytes.new();
    a.push(65);
    a.push(66);
    var b: bytes = bytes.new();
    b.push(65);
    b.push(66);
    var c: bytes = bytes.new();
    c.push(65);
    c.push(66);
    c.push(67);
    let same = a == b;
    let diff = a == c;
    println(f"{same} {diff}");
}
