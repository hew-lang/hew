[[case]]
id = "match-payload-reassign-if-let-borrow"
intent = "Projected-payload move-out UAF / double-free oracle (issue #2523). Shape `if_let_borrow` (if let borrow only keeps scrutinee live) runs to its exact outcome with no leak, double free or use after free under ASan/LSan."
source = "cases/match-payload-reassign-if-let-borrow.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 60

[case.expected]
stdout = ""
exit = 15
