[[case]]
id = "recv-loop-carry-continue-escape"
intent = "Per-iteration recv-result drop on the loop back-edge. Shape `carry_continue_escape` (carry continue payload escape no uaf) runs to its exact outcome with no leak, double free or use after free under ASan/LSan."
source = "cases/recv-loop-carry-continue-escape.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 60

[case.expected]
stdout = "before\nescaped\nafter\n"
exit = 0
