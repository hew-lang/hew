[[case]]
id = "indirect-enum-drop-mutual"
intent = "Indirect-enum heap-node drop leak oracle. Shape `mutual_high` (indirect enum mutual recursion leak slope below tolerance) runs to its exact outcome with no leak, double free or use after free under ASan/LSan."
source = "cases/indirect-enum-drop-mutual.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 60

[case.expected]
stdout = "ok"
exit = 0
