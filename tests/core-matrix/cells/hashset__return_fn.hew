fn make() -> HashSet<i64> {
    let m: HashSet<i64> = HashSet.new();
    m.insert(1);
    m.insert(2);
    m
}

fn main() {
    let x = make();
    let x_l = x.len();
    let x_h = x.contains(1);
    println(f"len={x_l} has1={x_h}");
}
