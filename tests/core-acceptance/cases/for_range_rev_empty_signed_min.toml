[[case]]
id = "for_range_rev_empty_signed_min"
intent = "The fixture prints exactly its recorded output and exits 0."
source = "../vertical-slice/accept/for_range_rev_empty_signed_min.hew"
suites = ["acceptance"]
timeout_seconds = 30

[case.env]
HEW_WORKERS = "4"

[case.expected]
stdout = "999\n"
exit = 0
