[[case]]
id = "enum-callee-consume-drop-result-temp"
intent = "Leak oracle for the callee-side drop of a CONSUMED by-value enum param (#2732). Shape `result_temp_df` (result temp consume does not double free, result temp consume leak slope below tolerance) runs to its exact outcome with no leak, double free or use after free under ASan/LSan."
source = "cases/enum-callee-consume-drop-result-temp.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 60

[case.expected]
stdout = ""
exit = 0
