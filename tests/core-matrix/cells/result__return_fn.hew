fn make() -> Result<i64, string> {
    let m: Result<i64, string> = Ok(6);
    m
}

fn main() {
    let x = make();
    match x { Ok(x_v) => println(f"ok {x_v}"), Err(x_e) => println(f"err {x_e}"), }
}
