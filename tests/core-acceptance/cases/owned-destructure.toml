[[case]]
id = "owned-destructure"
intent = "Record and nested tuple patterns preserve ordinary source values, clean wildcard fields and produce independently mutable bytes."
source = "cases/owned-destructure.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 15

[case.expected]
stdout = "packet:packet:1:2\ninner:inner:66\n"
exit = 0
