[[case]]
id = "wire_generic_hashmap_formats_roundtrip"
intent = "The fixture runs to completion and exits 42."
source = "../vertical-slice/accept/wire_generic_hashmap_formats_roundtrip.hew"
suites = ["acceptance"]
timeout_seconds = 30

[case.env]
HEW_WORKERS = "4"

[case.expected]
stdout = ""
exit = 42
