[[case]]
id = "var-param-mutation-lost"
intent = "Mutating a `var` parameter that nothing goes on to read is reported as a lost write."
source = "cases/var-param-mutation-lost.hew"
suites = ["acceptance"]
timeout_seconds = 30
kind = "check"

[[case.expected.diagnostics]]
code = "var_param_mutation_lost"
severity = "warning"
line = 3
column = 20
message = "mutation of `var` parameter `items` is lost"
