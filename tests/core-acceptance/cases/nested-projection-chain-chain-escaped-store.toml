[[case]]
id = "nested-projection-chain-chain-escaped-store"
intent = "Nested-projection-chain ownership oracle — the compiled-binary pins for a MULTI-level field-projection alias chain. Shape `chain_escaped_store_high` (escaped into record leak slope below tolerance, escaped into record no double free under malloc scribble) runs to its exact outcome with no leak, double free or use after free under ASan/LSan."
source = "cases/nested-projection-chain-chain-escaped-store.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 60

[case.expected]
stdout = ""
exit = 0
