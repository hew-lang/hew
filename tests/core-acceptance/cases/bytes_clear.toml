[[case]]
id = "bytes_clear"
intent = "`bytes.clear()` empties the buffer and leaves it reusable."
source = "../vertical-slice/accept/bytes_clear.hew"
suites = ["acceptance"]
timeout_seconds = 30

[case.env]
HEW_WORKERS = "4"

[case.expected]
stdout = ""
exit = 0
