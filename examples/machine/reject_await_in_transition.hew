machine Door {
    events {
        Toggle,
    }

    state Closed,
    state Open,

    on Toggle: Closed => Open {
        await pending;
        Open
    }
    on Toggle: Open => Closed {
        Door.Closed
    }
}

fn main() {}
