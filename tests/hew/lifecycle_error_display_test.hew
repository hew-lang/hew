//! Display coverage for std.concurrency.lifecycle.Error.

import std.concurrency.lifecycle.{Error};

#[test]
fn test_lifecycle_error_display_prints_the_carried_code() {
    let err = Error.Code(7);
    assert(to_string(err) == "lifecycle error (code 7)");
}
