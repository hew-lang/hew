import "diamond_base.hew";
import "diamond_left.hew";
import "diamond_right.hew";

fn main() {
    let b = base_value();
    let l = left_value();
    let r = right_value();
    println(f"{b}");
    println(f"{l}");
    println(f"{r}");
    println(f"{l + r}");
}
