[[case]]
id = "driver-lost-update-random-1"
intent = "The deterministic driver runs the read-then-write race to the same balance every time for schedule `schedule=random,seed=1`."
source = "cases/driver-lost-update.hew"
suites = ["acceptance"]
timeout_seconds = 15

[case.env]
HEW_DETERMINISTIC = "schedule=random,seed=1"

[case.expected]
stdout = "10\n"
exit = 0
