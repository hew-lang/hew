[[case]]
id = "generic_vec_get_owned_leak"
intent = "#1929 Stage 2 leak proof, updated for the Index-trait `Vec<T>.get(i)` model: `Vec<T>.push`/`.get` for a NON-Copy record element under a type parameter, exercised 100k times."
source = "../vertical-slice/accept/generic_vec_get_owned_leak.hew"
suites = ["acceptance"]
timeout_seconds = 30

[case.env]
HEW_WORKERS = "4"

[case.expected]
stdout = "owned-ok\n"
exit = 0
