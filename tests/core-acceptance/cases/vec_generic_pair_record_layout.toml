[[case]]
id = "vec_generic_pair_record_layout"
intent = "The fixture runs to completion and prints exactly its recorded output."
source = "../vertical-slice/accept/vec_generic_pair_record_layout.hew"
suites = ["acceptance"]
timeout_seconds = 30

[case.env]
HEW_WORKERS = "4"

[case.expected]
stdout = "pair-total=121\n"
exit = 0
