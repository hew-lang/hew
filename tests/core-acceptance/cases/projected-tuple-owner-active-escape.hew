fn build(n: i64) -> (Vec<i64>, i64) {
    var v: Vec<i64> = Vec.new();
    v.push(n);
    let p = (v, 1);
    let items = p.0;
    let _length = items.len();
    p
}

fn main() -> i64 {
    let returned = build(3);
    returned.0.len()
}
