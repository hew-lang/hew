[[case]]
id = "let-else-call-scrutinee-g2429-letelse-divergent"
intent = "From-call `let … else` scrutinee enum-payload leak oracle (#2429 class). Shape `g2429_letelse_divergent_high` (letelse divergent edge loop leak slope below tolerance) runs to its exact outcome with no leak, double free or use after free under ASan/LSan."
source = "cases/let-else-call-scrutinee-g2429-letelse-divergent.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 60

[case.expected]
stdout = ""
exit = 0
