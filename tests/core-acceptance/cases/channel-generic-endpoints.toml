[[case]]
id = "channel-generic-endpoints"
intent = "Generic pipe constructors retain distinct element types through typed function boundaries, and try_recv executes in a non-suspending function for empty, queued and finished streams."
source = "cases/channel-generic-endpoints.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 30

[case.expected]
stdout = "typed pipes and immediate receive\n"
exit = 0
