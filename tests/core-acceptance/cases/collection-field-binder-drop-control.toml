[[case]]
id = "collection-field-binder-drop-control"
intent = "Leak oracle for a heap-owning collection reached as a record FIELD or tuple MEMBER and then read through a BORROWING method (`.len()`, `.get(i)`, …). Shape `control` (bare record collection field read no leak, nested record collection field read no leak) runs to its exact outcome with no leak, double free or use after free under ASan/LSan."
source = "cases/collection-field-binder-drop-control.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 60

[case.expected]
stdout = ""
exit = 0
