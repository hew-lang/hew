fn main() -> i64 {
    var source: HashSet<string> = HashSet.new();
    let value = "member".to_upper();
    println(source.insert(value));
    println(source.insert(value));
    var copy = source;
    println(source.remove(value));
    println(source.contains(value));
    println(copy.contains(value));
    copy.clear();
    println(value);
    println(copy.len());
    0
}
