[[case]]
id = "for-in-vec-iter-match-arm-return"
intent = "`for x in vec` cursor (`VecIter<T>`) scope-exit drop oracles: per-iteration leak slope plus a poisoned-allocator no-use-after-free pin for the place-source reuse shape (issue #1949). Shape `match_arm_return` (for in match arm return leaks nothing) runs to its exact outcome with no leak, double free or use after free under ASan/LSan."
source = "cases/for-in-vec-iter-match-arm-return.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 60

[case.expected]
stdout = "OK"
exit = 0
