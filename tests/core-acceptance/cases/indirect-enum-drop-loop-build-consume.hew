indirect enum Tree { Leaf(i64), Node(Tree, Tree), }
fn val(t: Tree) -> i64 { match t { .Leaf(n) => n, .Node(l, r) => val(l) + val(r), } }
fn run_loop() -> i64 {
    var total = 0;
    for i in 0..50 {
        let t = Tree.Node(.Leaf(1), .Leaf(2));
        let s = val(t);
        total = total + s;
    }
    total
}
fn main() {
    let r = run_loop();
    if r == 150 { print("ok"); } else { print("BAD"); }
}
