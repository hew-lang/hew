trait Sink {
    type Item;

    fn push(sink: Self, item: Self::Item) -> i32;
}

type IntSink {
}

impl Sink for IntSink {
    type Item = Vec<i64>;

    fn push(sink: IntSink, item: Vec<i64>) -> i32 {
        0
    }
}
