[[case]]
id = "vec-param-embed-copy-in-temp-vec-param-embed-arena"
intent = "Prepared parameter carriers embedded in anonymous Vec COPY-IN source temps. Shape `vec_param_embed_arena_high` (arena insert remove param embed has flat leak slope) runs to its exact outcome with no leak, double free or use after free under ASan/LSan."
source = "cases/vec-param-embed-copy-in-temp-vec-param-embed-arena.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 60

[case.expected]
stdout = ""
exit = 0
