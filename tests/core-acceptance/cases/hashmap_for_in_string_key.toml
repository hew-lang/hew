[[case]]
id = "hashmap_for_in_string_key"
intent = "The fixture runs to completion and exits 12."
source = "../vertical-slice/accept/hashmap_for_in_string_key.hew"
suites = ["acceptance"]
timeout_seconds = 30

[case.env]
HEW_WORKERS = "4"

[case.expected]
stdout = ""
exit = 12
