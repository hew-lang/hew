[[case]]
id = "vec-record-local-drop-vec-record-new-push"
intent = "Constant-leak oracle for `Vec<record>` and `Vec<tuple>` (`BitCopy` value-aggregate element) scope-exit release. Shape `vec_record_new_push` (vec record new push no leak) runs to its exact outcome with no leak, double free or use after free under ASan/LSan."
source = "cases/vec-record-local-drop-vec-record-new-push.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 60

[case.expected]
stdout = ""
exit = 0
