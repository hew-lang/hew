[[case]]
id = "var-overwrite-record-self-reassign"
intent = "`var`-local overwrite leak oracles: reassigning an owned `var` in a loop must release the prior value, not just the final one (#53). Shape `record_self_reassign` (record self reassign keeps aliased leaf alive) runs to its exact outcome with no leak, double free or use after free under ASan/LSan."
source = "cases/var-overwrite-record-self-reassign.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 60

[case.expected]
stdout = ""
exit = 7
