[[case]]
id = "example-progressive-01_hello"
intent = "The published example compiles, runs and prints exactly what it advertises."
source = "../../examples/progressive/01_hello.hew"
suites = ["acceptance"]
timeout_seconds = 30

[case.env]
HEW_WORKERS = "4"

[case.expected]
stdout_file = "../../examples/progressive/01_hello.expected"
exit = 0
