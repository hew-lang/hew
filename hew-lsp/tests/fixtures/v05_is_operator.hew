actor Worker {
    let _id: i64,

    receive fn ping() {}
}

fn is_probe() -> i32 {
    7
}

fn is_operator(value: Worker, other: Worker) -> i32 {
    if value is other {
        is_probe()
    } else {
        0
    }
}
