[[case]]
id = "actor-handler-unused-owned-param-direct-string-resource-transfer"
intent = "Actor-handler owned-parameter disposition oracle. Shape `direct_string_resource_transfer` (direct string resource transfer has only affine close authority) runs to its exact outcome with no leak, double free or use after free under ASan/LSan."
source = "cases/actor-handler-unused-owned-param-direct-string-resource-transfer.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 60

[case.expected]
stdout = ""
exit = 0
