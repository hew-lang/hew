[[case]]
id = "ownership-balance-escaping-direct-call-binding"
intent = "Ownership-balance fixture. The ownership checker balances every obligation, so the program checks with no diagnostics."
source = "fixtures/ownership-balance/escaping_direct_call_binding.hew"
suites = ["acceptance"]
timeout_seconds = 30
kind = "check"
