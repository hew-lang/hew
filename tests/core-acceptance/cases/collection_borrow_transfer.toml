[[case]]
id = "collection_borrow_transfer"
intent = "Pushing a borrowed collection element into a second collection transfers a value the source collection still owns: both collections would close it."
source = "../vertical-slice/reject/collection_borrow_transfer.hew"
suites = ["acceptance"]
timeout_seconds = 30
kind = "reject"

[[case.expected.diagnostics]]
code = "E_RESOURCE_CLOSE_MUST_CONSUME"
line = 9
column = 8
message = "`close` on a resource type must consume its receiver; write `fn close(consume self)`"
