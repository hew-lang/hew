[[case]]
id = "generator-env-clone-owned"
intent = "Standalone memory-safety oracle for owned generator environment snapshots. Shape `generator_env_clone_owned` (generator and closure env clone are malloc scribble clean, generator env clone direct and control have zero leaks) runs to its exact outcome with no leak, double free or use after free under ASan/LSan."
source = "cases/generator-env-clone-owned.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 60

[case.expected]
stdout = "128:128:128:128:OK"
exit = 0
