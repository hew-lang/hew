[[case]]
id = "structural-aggregate-clone-keep-clone"
intent = "Leak oracles for both structural-clone drop orders. Shape `keep_clone` (dropping original before returned clone is balanced) runs to its exact outcome with no leak, double free or use after free under ASan/LSan."
source = "cases/structural-aggregate-clone-keep-clone.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 60

[case.expected]
stdout = ""
exit = 0
