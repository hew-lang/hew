[[case]]
id = "closure_param_field_store"
intent = "The fixture runs to completion and exits 42."
source = "../vertical-slice/accept/closure_param_field_store.hew"
suites = ["acceptance"]
timeout_seconds = 30

[case.env]
HEW_WORKERS = "4"

[case.expected]
stdout = ""
exit = 42
