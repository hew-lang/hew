[[case]]
id = "alias_wrappers_no_double_free"
intent = "W5-011 P3 alias-wrapper safety proof — every alias site that moves a heap-owning `string` into a *persistent slot* (tuple element, control-flow join result, call argument, variant payload) must exclude the aliased source from scope-exit dro."
source = "../vertical-slice/accept/alias_wrappers_no_double_free.hew"
suites = ["acceptance"]
timeout_seconds = 30

[case.env]
HEW_WORKERS = "4"

[case.expected]
stdout = "done\n"
exit = 0
