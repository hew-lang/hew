[[case]]
id = "heap-owning-record-in-tuple-generic-record-in-tuple"
intent = "Constant-leak oracle for a heap-owning record reached as a TUPLE MEMBER. Shape `generic_record_in_tuple` (generic record in tuple no leak) runs to its exact outcome with no leak, double free or use after free under ASan/LSan."
source = "cases/heap-owning-record-in-tuple-generic-record-in-tuple.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 60

[case.expected]
stdout = ""
exit = 0
