[[case]]
id = "defer_secures_block_result"
intent = "The fixture runs to completion and exits 5."
source = "../vertical-slice/accept/defer_secures_block_result.hew"
suites = ["acceptance"]
timeout_seconds = 30

[case.env]
HEW_WORKERS = "4"

[case.expected]
stdout = ""
exit = 5
