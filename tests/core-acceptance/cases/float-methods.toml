[[case]]
id = "float-methods"
intent = "to_bits/from_bits round-trip (including negative zero's distinct bit pattern), is_nan/is_finite/is_infinite classify NaN and both infinities, is_sign_negative reads the raw sign bit, and the abs method matches math.abs."
source = "cases/float-methods.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 15

[case.expected]
stdout = "float methods classify NaN, infinity, sign and bit patterns correctly\n"
stderr = ""
exit = 0
