[[case]]
id = "actor_single_arg_pid_payload"
intent = "The fixture runs to completion and prints exactly its recorded output."
source = "../vertical-slice/accept/actor_single_arg_pid_payload.hew"
suites = ["acceptance"]
timeout_seconds = 30

[case.env]
HEW_WORKERS = "4"

[case.expected]
stdout = "echo: heard pinger\npinger: got ack\n"
exit = 0
