[[case]]
id = "supervisor_max_heap"
intent = "The fixture runs to completion and exits 42."
source = "../vertical-slice/accept/supervisor_max_heap.hew"
suites = ["acceptance"]
timeout_seconds = 30

[case.env]
HEW_WORKERS = "4"

[case.expected]
stdout = ""
exit = 42
