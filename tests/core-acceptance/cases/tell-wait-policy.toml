[[case]]
id = "tell-wait-policy"
intent = "A call through a `policy(target, on_full: .Wait)` view suspends the sender on a full bounded mailbox instead of refusing, so every message is delivered."
source = "cases/tell-wait-policy.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 20

[case.expected]
stdout = "all submitted\ndelivered 5\n"
exit = 0
