// Can the compiler infer types?
fn add(a: i64, b: i64) -> i64 {
    a + b
}

fn main() {
    let x = 10; // infer i64
    let y = 20; // infer i64
    let result = add(x, y); // infer i64
    println(result);
}
