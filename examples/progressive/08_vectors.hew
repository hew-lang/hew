// Test 8: Vectors/Arrays
// Testing Vec operations
fn main() {
    let v: Vec<i64> = Vec::new();
    v.push(10);
    v.push(20);
    v.push(30);
    println(v.len());
    let first = v[0];
    let second = v[1];
    let third = v[2];
    println(first);
    println(second);
    println(third);
}
