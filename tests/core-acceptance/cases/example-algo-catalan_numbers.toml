[[case]]
id = "example-algo-catalan_numbers"
intent = "The published example compiles, runs and prints exactly what it advertises."
source = "../../examples/algos/catalan_numbers.hew"
suites = ["acceptance"]
timeout_seconds = 30

[case.env]
HEW_WORKERS = "4"

[case.expected]
stdout_file = "../../examples/algos/catalan_numbers.expected"
exit = 0
