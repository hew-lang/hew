// Ownership markers are valid on nominal product and enum declarations, but
// aliases cannot carry resource semantics. Reject the marker at its source
// attribute instead of silently discarding it before HIR.
#[resource]
type Metres = i64;

fn main() {}
