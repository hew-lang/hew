// Regression: explicit `.free()` on a regex.Pattern must remain safe even when
// the handle then goes out of scope.

import std::text::regex;

fn do_explicit_free() -> bool {
    let re = regex.new("[0-9]+");
    let matched = re.is_match("x1y2");
    re.free();
    matched
}

fn main() {
    println(do_explicit_free());
    println(do_explicit_free());
    println(do_explicit_free());
    println("no double free");
}
