[[case]]
id = "vec-local-drop-vec-string-local-loop"
intent = "Plain `Vec<T>` local scope-exit drop oracles: per-iteration leak slope plus poisoned-allocator no-double-free pins for the escape shapes. Shape `vec_string_local_loop_high` (vec string local loop no per frame leak slope) runs to its exact outcome with no leak, double free or use after free under ASan/LSan."
source = "cases/vec-local-drop-vec-string-local-loop.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 60

[case.expected]
stdout = ""
exit = 100
