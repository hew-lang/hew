// Negative test: to_string rejects a type that has no Display impl at the
// type-checker level.  The error fires before codegen and must point at the
// call site.
type NoDisplay {
    value: i32;
}

fn main() {
    let nd = NoDisplay { value: 42 };
    let s = to_string(nd);
    println(s);
}
