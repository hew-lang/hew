[[case]]
id = "result_handler_heap_oracle"
intent = "The fixture runs to completion and prints exactly its recorded output."
source = "../vertical-slice/accept/result_handler_heap_oracle.hew"
suites = ["acceptance"]
timeout_seconds = 30

[case.env]
HEW_WORKERS = "4"

[case.expected]
stdout = "ok\nnegative input\n"
exit = 0
