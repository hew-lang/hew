[[case]]
id = "pattern_call_scrutinee_while_let"
intent = "`while let` over a CALL scrutinee re-evaluated every iteration. Each header re-entry mints/releases the from-call owner, and the false-arm cleanup block releases it on the tag-mismatch exit. Every owned payload field is bound (no skipped owned sibling), so this rides the owner mint + back-edge release rather than the `while let` skipped-owned refusal."
source = "cases/pattern_call_scrutinee_while_let.hew"
suites = ["acceptance"]
kind = "run"
timeout_seconds = 60

[case.expected]
stdout = ""
exit = 0
