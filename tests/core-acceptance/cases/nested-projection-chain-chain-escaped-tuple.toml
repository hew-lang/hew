[[case]]
id = "nested-projection-chain-chain-escaped-tuple"
intent = "Nested-projection-chain ownership oracle — the compiled-binary pins for a MULTI-level field-projection alias chain. Shape `chain_escaped_tuple_high` (escaped return tuple leak slope below tolerance, escaped return tuple no double free under malloc scribble) runs to its exact outcome with no leak, double free or use after free under ASan/LSan."
source = "cases/nested-projection-chain-chain-escaped-tuple.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 60

[case.expected]
stdout = ""
exit = 0
