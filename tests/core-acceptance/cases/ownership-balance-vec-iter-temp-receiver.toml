[[case]]
id = "ownership-balance-vec-iter-temp-receiver"
intent = "Ownership-balance fixture. Driven 200,000 times, it releases every obligation exactly once with no leak under ASan/LSan."
source = "fixtures/ownership-balance/vec_iter_temp_receiver.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 120

[case.expected]
stdout = "80000300000\n"
exit = 0
