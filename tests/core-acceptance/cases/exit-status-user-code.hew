// A deliberate non-zero exit code survives an unrecovered actor fault: it is
// already a failure and says more than 1.
actor Worker {
    receive fn boom() { panic("worker failed"); }
}

fn main() {
    let w = spawn Worker();
    let _ = w.boom();
    println("chose 7");
    exit(7);
}
