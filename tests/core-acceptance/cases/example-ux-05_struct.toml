[[case]]
id = "example-ux-05_struct"
intent = "The published example compiles, runs and prints exactly what it advertises."
source = "../../examples/ux/05_struct.hew"
suites = ["acceptance"]
timeout_seconds = 30

[case.env]
HEW_WORKERS = "4"

[case.expected]
stdout_file = "../../examples/ux/05_struct.expected"
exit = 0
