fn main() {
    let x: f64 = 3.5;
    let m: HashMap<string, f64> = HashMap::new();
    m.insert("k", x);
    match m.get("k") {
        Some(e) => {
            println(f"{e}");
        },
        None => println("missing"),
    }
}
