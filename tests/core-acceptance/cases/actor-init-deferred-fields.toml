[[case]]
id = "actor-init-deferred-fields"
intent = "Fields without a default that init assigns take their first value inside init: scalar, owned string, let field, a differently named init parameter, a both-arm conditional, the self spelling and a replacement of an initialized owned field."
source = "cases/actor-init-deferred-fields.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 30

[case.expected]
stdout = "READY!\n7\nquick\n1\nLATER!\n42\nSLOW\n1\n"
exit = 0
