[[case]]
id = "collection-borrowed-cow-ingress-vec-iter-yield-share-2"
intent = "Carrier-general collection-ingress exactly-once oracles: the borrowed `bytes` parameter, the REASSIGNED parameter generation, and the `VecIter` `string` yield binder. Shape `vec_iter_yield_share` (vec iter yield share reads back exact values) runs to its exact outcome with no leak, double free or use after free under ASan/LSan."
source = "cases/collection-borrowed-cow-ingress-vec-iter-yield-share-2.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 60

[case.expected]
stdout = "2|rent-heap|3"
exit = 0
