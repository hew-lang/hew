[[case]]
id = "select-arm-owned-binding-g1875-after-wins"
intent = "Select-arm owned-binding leak oracle (#1875, select-arm half). Shape `g1875_after_wins_high` (after wins owned losers loop leak slope below tolerance) runs to its exact outcome with no leak, double free or use after free under ASan/LSan."
source = "cases/select-arm-owned-binding-g1875-after-wins.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 60

[case.expected]
stdout = ""
exit = 0
