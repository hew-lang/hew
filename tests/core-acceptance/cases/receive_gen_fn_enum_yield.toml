[[case]]
id = "receive_gen_fn_enum_yield"
intent = "The fixture runs to completion and prints exactly its recorded output."
source = "../vertical-slice/accept/receive_gen_fn_enum_yield.hew"
suites = ["acceptance"]
timeout_seconds = 30

[case.env]
HEW_WORKERS = "4"

[case.expected]
stdout = "circle 5\nsquare 7\n"
exit = 0
