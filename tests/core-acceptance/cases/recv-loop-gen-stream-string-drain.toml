[[case]]
id = "recv-loop-gen-stream-string-drain"
intent = "Per-iteration recv-result drop on the loop back-edge. Shape `gen_stream_string_drain_high` (gen stream string drain no per frame leak slope) runs to its exact outcome with no leak, double free or use after free under ASan/LSan."
source = "cases/recv-loop-gen-stream-string-drain.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 60

[case.expected]
stdout = ""
exit = 0
