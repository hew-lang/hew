actor Example {
    var items: Vec<i64> = Vec.new(),
    receive fn demo() {
        items.push(1);
        println(items.len());
    }
}
fn main() {
    let e = spawn Example();
    let _ = e.demo();
}
