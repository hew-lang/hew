[[case]]
id = "alias-return-composite-no-double-free-conditional"
intent = "ADVERSARIAL double-free pin — the load-bearing guard against a future over-widening of the fresh-composite sole-owner admission. Shape `conditional` (conditional alias return does not double free) runs to its exact outcome with no leak, double free or use after free under ASan/LSan."
source = "cases/alias-return-composite-no-double-free-conditional.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 60

[case.expected]
stdout = ""
exit = 0
