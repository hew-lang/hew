[[case]]
id = "record_generic_enum_field"
intent = "The fixture runs to completion and exits 7."
source = "../vertical-slice/accept/record_generic_enum_field.hew"
suites = ["acceptance"]
timeout_seconds = 30

[case.env]
HEW_WORKERS = "4"

[case.expected]
stdout = ""
exit = 7
