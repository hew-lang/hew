import std::stream;

fn main() {
    // Create a bounded pipe
    let (sink, input) = stream.pipe(8);

    // Write using method syntax — no manual size needed
    sink.write("hello");
    sink.write("world");
    sink.close();

    // Read using for-await until EOF
    for await item in input {
        println(item);
    }
}
