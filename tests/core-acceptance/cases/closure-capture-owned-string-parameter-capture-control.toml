[[case]]
id = "closure-capture-owned-string-parameter-capture-control"
intent = "Per-iteration leak / double-free oracle for the closure-env KEYSTONE: an escaping (heap-boxed) closure that captures an OWNED value (`string`, `Vec`, a heap-owning record, an owned-payload enum) must free that captured value EXACTLY once… Shape `string_parameter_capture_control_high` (string parameter capture control leak slope below tolerance) runs to its exact outcome with no leak, double free or use after free under ASan/LSan."
source = "cases/closure-capture-owned-string-parameter-capture-control.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 60

[case.expected]
stdout = ""
exit = 0
