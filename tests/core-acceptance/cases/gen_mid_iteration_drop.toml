[[case]]
id = "gen_mid_iteration_drop"
intent = "The fixture runs to completion and prints exactly its recorded output."
source = "../vertical-slice/accept/gen_mid_iteration_drop.hew"
suites = ["acceptance"]
timeout_seconds = 30

[case.env]
HEW_WORKERS = "4"

[case.expected]
stdout = "gen:first\nfirst-A\nafter-break\n"
exit = 0
