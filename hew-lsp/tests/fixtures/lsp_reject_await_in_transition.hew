machine Door {
    state Closed;
    state Open;

    event Toggle;

    on Toggle: Closed -> Open {
        await pending;
        Open
    }

    on Toggle: Open -> Closed {
        Closed
    }
}

fn await_transition_rejection_probe() -> i32 {
    7
}
