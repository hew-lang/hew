[[case]]
id = "match-payload-reassign-borrow-only-guard-fallthrough"
intent = "Projected-payload move-out UAF / double-free oracle (issue #2523). Shape `borrow_only_guard_fallthrough` (borrow only guard fallthrough runs safely) runs to its exact outcome with no leak, double free or use after free under ASan/LSan."
source = "cases/match-payload-reassign-borrow-only-guard-fallthrough.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 60

[case.expected]
stdout = ""
exit = 3
