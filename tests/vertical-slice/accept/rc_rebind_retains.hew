// Rebinding a shared handle retains the allocation, the way rebinding a string
// retains its buffer: both names stay live and both release.
fn main() {
    let rc = Rc.new(1);
    let rebound = rc;
    print(rc.strong_count());
    println(rebound.get());
}
