[[case]]
id = "hashmap_into_iter_field"
intent = "The fixture runs to completion and exits 3."
source = "../vertical-slice/accept/hashmap_into_iter_field.hew"
suites = ["acceptance"]
timeout_seconds = 30

[case.env]
HEW_WORKERS = "4"

[case.expected]
stdout = ""
exit = 3
