[[case]]
id = "if-let-call-scrutinee-g2429-single-iflet"
intent = "From-call `if let` scrutinee enum-payload leak oracle (#2429 class). Shape `g2429_single_iflet_scribble` (single from call iflet no double free under malloc scribble) runs to its exact outcome with no leak, double free or use after free under ASan/LSan."
source = "cases/if-let-call-scrutinee-g2429-single-iflet.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 60

[case.expected]
stdout = "m"
exit = 0
