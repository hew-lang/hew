[[case]]
id = "match-payload-reassign-nested-enum-borrow"
intent = "Projected-payload move-out UAF / double-free oracle (issue #2523). Shape `nested_enum_borrow` (nested enum borrow only is valid) runs to its exact outcome with no leak, double free or use after free under ASan/LSan."
source = "cases/match-payload-reassign-nested-enum-borrow.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 60

[case.expected]
stdout = ""
exit = 3
