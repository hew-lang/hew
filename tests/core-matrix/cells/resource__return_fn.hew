#[resource]
type Tok {
    id: i64
}

impl Tok {
    fn close(self) {
        println(f"close {self.id}");
    }
}

fn make() -> Tok {
    let m = Tok { id: 1 };
    m
}

fn main() {
    let x = make();
    let x_i = x.id;
    println(f"tok {x_i}");
}
