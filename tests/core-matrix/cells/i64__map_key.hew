fn main() {
    let k: i64 = 42;
    let m: HashMap<i64, i64> = HashMap.new();
    m.insert(k, 9);
    match m.get(k) {
        .Some(v) => println(f"got {v}"),
        .None => println("missing"),
    }
}
