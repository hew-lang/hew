[[case]]
id = "var_record_collection_projection_caller_visible"
intent = "#2821 reverse direction: the record itself is a by-value copy, but these concrete projections cross into Vec/HashMap storage still shared with the caller."
source = "../vertical-slice/accept/var_record_collection_projection_caller_visible.hew"
suites = ["acceptance"]
timeout_seconds = 30

[case.env]
HEW_WORKERS = "4"

[case.expected]
stdout = "1\n"
stderr = "hew: failure: UserPanic (212): expect failed: get succeeds\n"
exit = 1
