[[case]]
id = "vec-index-assign-temp-consumed-bound"
intent = "Owned-Vec INDEX-ASSIGNMENT (`v[i] = ..`) leak oracles: neither a fresh aggregate RHS nor a consumed bound-local RHS may leak its heap. Shape `consumed_bound` (vec index assign consumed bound local moves in) runs to its exact outcome with no leak, double free or use after free under ASan/LSan."
source = "cases/vec-index-assign-temp-consumed-bound.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 60

[case.expected]
stdout = "2OK"
exit = 0
