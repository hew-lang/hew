type Rec {
    a: i64,
    b: string,
}

fn main() {
    let k = Rec { a: 1, b: "x0" };
    let m: HashMap<Rec, i64> = HashMap.new();
    m.insert(k, 9);
    match m.get(k) {
        Option.Some(v) => println(f"got {v}"),
        Option.None => println("missing"),
    }
}
