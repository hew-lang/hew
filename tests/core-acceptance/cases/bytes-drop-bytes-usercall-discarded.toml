[[case]]
id = "bytes-drop-bytes-usercall-discarded"
intent = "Bytes ownership leak oracles: sender-local scope-exit drop and actor-state-field overwrite drop. Shape `bytes_usercall_discarded_high` (bytes usercall discarded no per frame leak slope) runs to its exact outcome with no leak, double free or use after free under ASan/LSan."
source = "cases/bytes-drop-bytes-usercall-discarded.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 60

[case.expected]
stdout = ""
exit = 50
