[[case]]
id = "hashset_for_in_tuple_projection"
intent = "The fixture runs to completion and exits 60."
source = "../vertical-slice/accept/hashset_for_in_tuple_projection.hew"
suites = ["acceptance"]
timeout_seconds = 30

[case.env]
HEW_WORKERS = "4"

[case.expected]
stdout = ""
exit = 60
