type Point {
    x: int;
    y: int;
}

impl Point {
    fn sum(self) -> int {
        self.x + self.y
    }
    
    fn display(self) {
        println(self.sum());
    }
}

fn main() {
    let p = Point { x: 3, y: 4 };
    p.display();
    println(p.sum());
}
