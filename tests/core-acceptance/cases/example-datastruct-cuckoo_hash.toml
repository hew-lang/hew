[[case]]
id = "example-datastruct-cuckoo_hash"
intent = "The published example compiles, runs and prints exactly what it advertises."
source = "../../examples/datastruct/cuckoo_hash.hew"
suites = ["acceptance"]
timeout_seconds = 30

[case.env]
HEW_WORKERS = "4"

[case.expected]
stdout_file = "../../examples/datastruct/cuckoo_hash.expected"
exit = 0
