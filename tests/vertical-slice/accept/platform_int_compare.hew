// platform-int-arith: isize/usize comparisons type-check and evaluate.
fn main() -> i64 {
    let a: isize = 3;
    let b: isize = 5;
    assert(a < b);

    let u: usize = 5;
    let v: usize = 5;
    assert(u >= v);

    let c: isize = 4;
    assert(c == 4);
    0
}
