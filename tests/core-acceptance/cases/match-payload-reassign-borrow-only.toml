[[case]]
id = "match-payload-reassign-borrow-only"
intent = "Projected-payload move-out UAF / double-free oracle (issue #2523). Shape `borrow_only` (borrow only projection keeps scrutinee live) runs to its exact outcome with no leak, double free or use after free under ASan/LSan."
source = "cases/match-payload-reassign-borrow-only.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 60

[case.expected]
stdout = ""
exit = 15
