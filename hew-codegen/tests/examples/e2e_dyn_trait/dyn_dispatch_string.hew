trait Describable {
    fn describe(val: Self) -> string;
}

type Dog {
    name: string;
}

type Cat {
    name: string;
}

impl Describable for Dog {
    fn describe(d: Dog) -> string {
        string_concat("Dog: ", d.name)
    }
}

impl Describable for Cat {
    fn describe(cat: Cat) -> string {
        string_concat("Cat: ", cat.name)
    }
}

fn print_description(obj: dyn Describable) {
    let desc = obj.describe();
    println(desc);
}

fn main() {
    let d = Dog { name: "Rex" };
    let c = Cat { name: "Whiskers" };
    print_description(d);
    print_description(c);
}
