// One branch leaves the transaction unfinished at normal return.
#[linear]
type Txn {
    id: i64,
}

impl Txn {
    fn commit(consume self) -> i64 {
        0
    }
}

fn main() -> i64 {
    let t = Txn { id: 0 };
    let _r = if 1 == 1 {
        let _a = t.commit();
        7
    } else {
        8
    };
    42
}
