[[case]]
id = "defer_exit_live_cursor_bare_body"
intent = "The fixture runs to completion and exits 42."
source = "../vertical-slice/accept/defer_exit_live_cursor_bare_body.hew"
suites = ["acceptance"]
timeout_seconds = 30

[case.env]
HEW_WORKERS = "4"

[case.expected]
stdout = ""
exit = 42
