[[case]]
id = "on-start-suspension-low"
intent = "Lifecycle-hook suspend/resume coro-frame reclamation (#2269). Shape `low` (suspending on start no per actor coro frame leak) runs to its exact outcome with no leak, double free or use after free under ASan/LSan."
source = "cases/on-start-suspension-low.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 60

[case.expected]
stdout = ""
exit = 0
