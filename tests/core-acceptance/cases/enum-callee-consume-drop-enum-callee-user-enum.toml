[[case]]
id = "enum-callee-consume-drop-enum-callee-user-enum"
intent = "Leak oracle for the callee-side drop of a CONSUMED by-value enum param (#2732). Shape `enum_callee_user_enum_high` (user enum consume does not double free, user enum consume leak slope below tolerance) runs to its exact outcome with no leak, double free or use after free under ASan/LSan."
source = "cases/enum-callee-consume-drop-enum-callee-user-enum.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 60

[case.expected]
stdout = ""
exit = 0
