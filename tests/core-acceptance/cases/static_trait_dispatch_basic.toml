[[case]]
id = "static_trait_dispatch_basic"
intent = "Accept fixture: Static trait dispatch — basic T: Trait method call A generic function bounded by a trait calls a method from that trait, dispatching to the concrete impl at monomorphization time."
source = "../vertical-slice/accept/static_trait_dispatch_basic.hew"
suites = ["acceptance"]
timeout_seconds = 30

[case.env]
HEW_WORKERS = "4"

[case.expected]
stdout = ""
exit = 0
