[[case]]
id = "generator-env-clone-generator-actor-record-param-suspend"
intent = "Standalone memory-safety oracle for owned generator environment snapshots. Shape `generator_actor_record_param_suspend` (generator actor record param suspended teardown has zero leaks, generator and closure env clone are malloc scribble clean) runs to its exact outcome with no leak, double free or use after free under ASan/LSan."
source = "cases/generator-env-clone-generator-actor-record-param-suspend.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 60

[case.expected]
stdout = "45:OK"
exit = 0
