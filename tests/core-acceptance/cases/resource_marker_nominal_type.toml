[[case]]
id = "resource_marker_nominal_type"
intent = "ownership markers remain valid on nominal `type` declarations and retain their established resource/linear behaviour."
source = "../vertical-slice/accept/resource_marker_nominal_type.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 60

[case.expected]
stdout = ""
exit = 0
