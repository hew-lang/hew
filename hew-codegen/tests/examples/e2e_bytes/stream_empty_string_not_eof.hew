import std::stream;

fn main() {
    let (sink, input) = stream.pipe(8);

    sink.write("");
    sink.write("tail");
    sink.close();

    var count: int = 0;
    for await item in input {
        println(item.len());
        count = count + 1;
    }
    println(count);

    input.close();
}
