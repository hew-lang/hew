type Point {
    x: int;
    y: int;
}

impl Point {
    fn sum(pt: Point) -> int {
        pt.x + pt.y
    }
    
    fn display(pt: Point) {
        println(pt.sum());
    }
}

fn main() {
    let p = Point { x: 3, y: 4 };
    p.display();
    println(p.sum());
}
