[[case]]
id = "example-datastruct-trie"
intent = "The published example compiles, runs and prints exactly what it advertises."
source = "../../examples/datastruct/trie.hew"
suites = ["acceptance"]
timeout_seconds = 30

[case.env]
HEW_WORKERS = "4"

[case.expected]
stdout_file = "../../examples/datastruct/trie.expected"
exit = 0
