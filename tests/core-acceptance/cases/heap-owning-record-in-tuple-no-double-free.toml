[[case]]
id = "heap-owning-record-in-tuple-no-double-free"
intent = "Constant-leak oracle for a heap-owning record reached as a TUPLE MEMBER. Shape `no_double_free` (record in tuple release is exactly once under malloc scribble) runs to its exact outcome with no leak, double free or use after free under ASan/LSan."
source = "cases/heap-owning-record-in-tuple-no-double-free.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 60

[case.expected]
stdout = "12OK"
exit = 0
