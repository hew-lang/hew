import std::string;
import std::encoding::hex;

fn main() {
    let data: bytes = bytes::new();
    data.push(0x48); // H
    data.push(0x69); // i
    let encoded = hex.encode(data);
    println(f"hex: {encoded}");
    let n = string.from_int(42);
    println(f"str: {n}");
}
