[[case]]
id = "actor-handler-unused-owned-param-branch-local-fresh-ingress"
intent = "Actor-handler owned-parameter disposition oracle. Shape `branch_local_fresh_ingress` (branch local fresh ingress is never hoisted past join) runs to its exact outcome with no leak, double free or use after free under ASan/LSan."
source = "cases/actor-handler-unused-owned-param-branch-local-fresh-ingress.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 60

[case.expected]
stdout = ""
exit = 0
