[[case]]
id = "wire-cbor-codec-wire-cbor-anon-to-json-temp"
intent = "Wire CBOR codec ownership leak oracles: decode-owned field drop and the decode-failure partial free-on-error path. Shape `wire_cbor_anon_to_json_temp_high` (anonymous to json temp round trip leaks exactly zero) runs to its exact outcome with no leak, double free or use after free under ASan/LSan."
source = "cases/wire-cbor-codec-wire-cbor-anon-to-json-temp.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 60

[case.expected]
stdout = ""
exit = 201
