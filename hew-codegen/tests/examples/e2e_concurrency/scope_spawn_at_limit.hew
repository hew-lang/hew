actor Worker {
    receive fn ping() {
    }
}

fn main() {
    scope {
        var i: int = 0;
        while i < 64 {
            let worker = spawn Worker;
            worker.ping();
            i = i + 1;
        }
    };
    println("spawned 64");
    println("PASS");
}
