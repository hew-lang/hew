[[case]]
id = "example-surface-unicode_runes"
intent = "The published example compiles, runs and prints exactly what it advertises."
source = "../../examples/v05/surfaces/unicode_runes.hew"
suites = ["acceptance"]
timeout_seconds = 30

[case.env]
HEW_WORKERS = "4"

[case.expected]
stdout_file = "../../examples/v05/surfaces/unicode_runes.expected"
exit = 0
