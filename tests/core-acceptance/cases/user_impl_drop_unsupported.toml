[[case]]
id = "user_impl_drop_unsupported"
intent = "The fixture is refused, and the refusal names this diagnostic."
source = "../vertical-slice/reject/user_impl_drop_unsupported.hew"
suites = ["acceptance"]
timeout_seconds = 30
kind = "reject"

[[case.expected.diagnostics]]
code = "InvalidOperation"
line = 5
column = 1
message = "`impl Drop` is not supported (its `drop` method would not run)"
