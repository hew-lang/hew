fn main() {
    let k: Option<i64> = Option.Some(5);
    let m: HashMap<Option<i64>, i64> = HashMap.new();
    m.insert(k, 9);
    match m.get(k) {
        Option.Some(v) => println(f"got {v}"),
        None => println("missing"),
    }
}
