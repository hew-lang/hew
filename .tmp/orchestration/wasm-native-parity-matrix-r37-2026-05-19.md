# R37 WASM ↔ native runtime parity matrix refresh — 2026-05-19

Supersedes `.tmp/orchestration/wasm-parity-matrix-2026-05-19.md` for the R37 lane. Slice 0 is local-only: tracker IDs below are stable local IDs, not GitHub issues.

Ground rule: every surface is classified as **parity**, **parity-with-cost**, **typed fail-closed**, or **local tracker**. Silent degradation is not an accepted state.

## Audit command log

Representative searches used for the re-audit (three angles per row where practical):

| Row | Search angles |
|---|---|
| S1 | `rg -n "reply_channel\\|mark_current_reply_channel\\|current_reply_channel\\|hew_get_reply_channel" hew-runtime/src/execution_context.rs hew-runtime/src/scheduler.rs hew-runtime/src/scheduler_wasm.rs hew-runtime/src/reply_channel_wasm.rs`; `rg -n "CURRENT_REPLY\\|thread_local!\\|reply channel moved\\|execution_context" hew-runtime/src/scheduler_wasm.rs hew-runtime/src/reply_channel_wasm.rs`; `rg -n "reply.*context\\|ctx.*reply\\|hew_get_reply_channel\\|HEW_CTX_FLAG_REPLY_CHANNEL_CONSUMED\\|nested.*reply" hew-runtime/src` |
| S2 | `rg -n "on_stop\\|terminate\\|terminate_fn\\|hew_actor_terminate" hew-runtime/src/actor.rs hew-runtime/src/scheduler_wasm.rs hew-codegen-rs/src/llvm.rs`; `rg -n "lifecycle\\|trace.*begin\\|trace.*end\\|actor_type_id\\|on\\(stop\\)" hew-runtime/src/scheduler.rs hew-runtime/src/scheduler_wasm.rs hew-runtime/src/actor.rs`; `rg -n "catch_unwind\\|longjmp\\|signal\\|terminate_with" hew-runtime/src/actor.rs hew-runtime/src/scheduler_wasm.rs hew-runtime/src/scheduler.rs` |
| S3 | `rg -n "hew_actor_cooperate\\|cancel_token\\|task_scope\\|return 2" hew-runtime/src/scheduler.rs hew-runtime/src/scheduler_wasm.rs`; `rg -n "cooperate.*2\\|cancel_exit\\|hew_actor_cooperate\\|const_int\\(2" hew-codegen-rs/src/llvm.rs`; `rg -n "WASM-TODO\\(#1451\\).*cancel\\|actor-state cancellation\\|COOPERATIVE_TICK_DEPTH" hew-runtime/src/scheduler_wasm.rs` |
| S4 | `rg -n "HeapExceeded\\|HEW_TRAP_HEAP_EXCEEDED\\|heap_exceeded\\|last_trap" hew-runtime/src`; `rg -n "hew_trap_with_code\\|stamp_current_actor_error_code\\|catch_unwind\\|Crashed" hew-runtime/src/arena_wasm.rs hew-runtime/src/scheduler_wasm.rs hew-runtime/src/trap_code.rs`; `rg -n "heap_exceeded\\|HEW_TRAP_HEAP_EXCEEDED" hew-runtime/src/wasm_parity_tests.rs` |
| S5 | `rg -n "hew_trap_with_code\\|hew_panic\\|HEW_TRAP_\\|ExitReason\\|exit 101" hew-runtime/src hew-codegen-rs/src/llvm.rs`; `rg -n "llvm.trap\\|Terminator::Trap\\|trap code" hew-codegen-rs/src/llvm.rs`; `rg -n "catch_unwind\\|error_code\\|Crashed" hew-runtime/src/scheduler_wasm.rs hew-runtime/src/trap_code.rs hew-runtime/src/actor.rs` |
| S6 | `rg -n "SupervisionTrees\\|PlatformLimitation\\|supervision operations" hew-types/src/check`; `rg -n "pub mod supervisor\\|target_arch = \"wasm32\"\\|cfg\\(not\\(target_arch = \"wasm32\"\\)\\)" hew-runtime/src/lib.rs`; `rg -n "wasm_rejects_supervisor\\|native_supervisor" hew-types/src/check/tests.rs` |
| S7 | `rg -n "hew_duplex\\|WasmUnsupportedSubstrate\\|duplex\\|wasm32" hew-codegen-rs/src hew-codegen-rs/tests scripts/jit-symbol-classification.toml`; `rg -n "uses_wasm_excluded_symbol\\|wasm-ld\\|undefined symbol" hew-codegen-rs/src/llvm.rs`; `rg -n "duplex_pair_call_blocks_wasm_emission\\|duplex_close_drop_blocks_wasm_emission" hew-codegen-rs/tests/wasm_duplex_classification.rs` |
| S8 | `rg -n "hew_select_first\\|hew_reply_wait\\|hew_wasm_sched_tick\\|NoRunnableWork" hew-runtime/src/reply_channel.rs hew-runtime/src/reply_channel_wasm.rs hew-codegen-rs/src/llvm.rs`; `rg -n "select.*activation\\|ready channel\\|timeout" hew-runtime/src/reply_channel_wasm.rs`; `rg -n "select_first\\|select_winner\\|hew_reply_wait" hew-codegen-rs/src/llvm.rs` |
| S9 | `rg -n "hew_trace_lifecycle\\|SPAN_BEGIN\\|SPAN_END\\|actor_type_id" hew-runtime/src/scheduler.rs hew-runtime/src/scheduler_wasm.rs hew-runtime/src/tracing.rs`; `rg -n "WASM-TODO\\(#1451\\).*actor_type\\|hew_register_handler_name" hew-runtime/src/tracing.rs hew-runtime/src/bridge.rs`; `rg -n "trace.*omitted\\|mailbox.*trace_context\\|crate::tracing" hew-runtime/src/scheduler_wasm.rs hew-runtime/src/mailbox_wasm.rs` |
| S10 | `rg -n "wasm_availability\\|target\\|hew_actor_trap\\|hew_seconds\\|hew_milliseconds\\|hew_duplex" scripts/jit-symbol-classification.toml`; `rg -n "stable = \\[\\|codegen-stable = \\[\\|internal = \\[" scripts/jit-symbol-classification.toml`; `rg -n "hew_supervisor_\\|hew_task_scope_\\|hew_tcp_\\|hew_trap_with_code" scripts/jit-symbol-classification.toml` |
| S11 | `rg -n "hew_seconds\\|hew_milliseconds\\|HewDuration\\|io_time\\|duration" hew-runtime/src scripts/jit-symbol-classification.toml`; `rg -n "pub mod duration\\|pub mod io_time\\|wasm_stubs" hew-runtime/src/lib.rs`; `rg -n "seconds_.*converts\\|milliseconds_.*passes" hew-runtime/src/duration.rs` |
| S12 | `rg -n "ignore\\|wasm\\|WASI\\|exit code\\|stdout\\|101\\|200\\|trap" hew-cli/tests/eval_e2e.rs hew-cli/tests/wasi_run_e2e.rs`; `rg -n "wasm32-wasi\\|runtime_failure\\|preserves_pre_failure_stdout" hew-cli/tests/eval_e2e.rs`; `rg -n "curated_playground_examples_run_under_wasi\\|supervisor_stays.*wasi" hew-cli/tests/wasi_run_e2e.rs` |

## Matrix

| # | Surface | Refreshed classification | Evidence | Local tracker / next slice |
|---|---|---|---|---|
| S1 | Reply-channel lookup (global vs ctx) | **Parity (stale row closed).** Reader and consumed bit are target-neutral and ctx-backed; no `CURRENT_REPLY_CHANNEL` store remains in the WASM scheduler surface. | `execution_context.rs:147-194` defines `hew_get_reply_channel` and `mark_current_reply_channel_consumed` over `HewExecutionContext`; native re-exports the same pair at `scheduler.rs:106-121`; WASM re-exports the same pair at `scheduler_wasm.rs:310-322` and `scheduler_wasm.rs:1372-1374`; nested ctx restoration is tested at `execution_context.rs:387-430`. | `WASM-R37-S1` — closed in Slice 0 evidence. Slice 1 should add/ratchet a direct WASM ctx-less fail-closed regression if desired, not re-migrate. |
| S2 | `on(stop)` observability | **Local tracker.** Terminate callbacks exist on both targets, but WASM has only `catch_unwind` protection and omits lifecycle tracing on stop paths. | Native `call_terminate_fn` installs ctx and signal recovery at `actor.rs:1275-1373`; WASM `call_terminate_fn` is catch-unwind-only at `actor.rs:1375-1413`; WASM stop transition calls terminate without trace at `scheduler_wasm.rs:1196-1207`, and the mailbox-closed stop path explicitly documents omitted tracing at `scheduler_wasm.rs:1267-1274`; terminate trampolines use `hew_require_execution_context` at `llvm.rs:6719-6834`. | `WASM-R37-S2` — Slice 2: route stop lifecycle through existing terminate/trace helpers or carry typed WASM-TODO for the missing trace fields. |
| S3 | Cooperate / cancellation propagation | **Local tracker, narrower than original.** WASM now propagates actor-state terminal transitions as cooperate return `2`, but task-scope/cancel-token propagation remains a named `#1451` gap. | Native checks cancel token and task scope and returns `2` at `scheduler.rs:1190-1199`; WASM explicitly leaves cross-task token/scope as `WASM-TODO(#1451)` at `scheduler_wasm.rs:1433-1441`, but returns `2` for terminal actor state at `scheduler_wasm.rs:1450-1466`; codegen treats return `2` as `cancel_exit` at `llvm.rs:6388-6424`. | `WASM-R37-S3` — Slice 3: add WASM cancellation parity test for mailbox/self-stop actor-state cancel and decide whether any task-scope path remains ignored under local tracker. |
| S4 | HeapExceeded trap attribution | **Partial parity; split state.** HeapExceeded actor-dispatch attribution is implemented and tested; non-dispatch/process-level trap/exit attribution is still part of S5/S12. | Native cap exhaustion longjmps with `HEW_TRAP_HEAP_EXCEEDED` at `arena.rs:326-366`; WASM cap exhaustion stamps `HEW_TRAP_HEAP_EXCEEDED` via `trap_code::hew_trap_with_code` and panics at `arena_wasm.rs:364-430`; shared trap constants and `ExitReason` live in `internal/types.rs:227-296`; parity tests cover round-trip and actor crash transition at `wasm_parity_tests.rs:976-1155`. | `WASM-R37-S4A` — closed for actor HeapExceeded attribution. `WASM-R37-S4B` — follow S5/S12 for WASI process-exit attribution outside dispatch. |
| S5 | `hew_trap_with_code` / panic / exit mapping | **Partial local tracker.** Codegen emits `hew_trap_with_code` before `llvm.trap`, and WASM actor-dispatch bridge stamps error code; `hew_panic` still exits 101 outside native longjmp and WASI process-exit mapping remains open. | Codegen `emit_trap_with_code` declares/calls `hew_trap_with_code` then `llvm.trap` at `llvm.rs:5225-5250`; native export longjmps at `supervisor.rs:346-372`; WASM bridge stamps current actor and panics at `trap_code.rs:40-63`; scheduler tests exercise HeapExceeded bridge at `wasm_parity_tests.rs:1048-1155`; `hew_panic` still falls through to `std::process::exit(101)` after native-only longjmp at `actor.rs:3509-3535`. | `WASM-R37-S5` — Slice 4: complete trap/panic/WASI exit attribution across 200-206 and non-actor process failure. |
| S6 | Supervisor restart | **Typed fail-closed.** Runtime remains native-only; checker emits `PlatformLimitation` for WASM supervision. | Native-only module gates at `lib.rs:367-445`; `WasmUnsupportedFeature::SupervisionTrees` is a reject-group compile-time error at `types.rs:774-782`, `types.rs:797`, `types.rs:887-889`; WASM checker tests at `tests.rs:14710-14766` assert `PlatformLimitation` for calls/declarations and no native error. | `WASM-R37-S6` — Slice 9: reaffirm with stable error-code/message tests if the checker diagnostics gain explicit codes. |
| S7 | Duplex substrate | **Typed fail-closed.** WASM emission detects duplex/supervisor excluded symbols before `wasm-ld` and returns `WasmUnsupportedSubstrate`. | Diagnostic type and message at `llvm.rs:127-149`; pre-link scan at `llvm.rs:231-315`; tests assert duplex pair/drop block WASM emission at `wasm_duplex_classification.rs:248-304`; symbol catalogue still lists duplex symbols without target availability at `scripts/jit-symbol-classification.toml:188-198`. | `WASM-R37-S7` — Slice 9 may add checker-level reaffirmation; S10 must still classify target availability in the symbol catalogue. |
| S8 | `select {}` cooperative tick | **Parity-with-cost.** WASM mirrors observable select result while using cooperative ticks rather than native blocking. | Native `hew_select_first` blocks/polls reply channels at `reply_channel.rs:470-500`; WASM `hew_reply_wait` drives one scheduler activation at a time at `reply_channel_wasm.rs:319-354`; WASM `hew_select_first` scans then ticks cooperatively at `reply_channel_wasm.rs:356-425`; codegen calls the same ABI and handles winner dispatch at `llvm.rs:5997-6057`. | `WASM-R37-S8` — no behavioural slice required unless future tests expose timeout/cancel drift. |
| S9 | Trace span / actor_type_id | **Parity with followups; Slice 5a accepted and Slice 5b not needed.** Codegen now registers actor trace metadata before spawn, WASM/test trace JSON resolves deterministic non-zero target-local actor type ids for registered metadata, and `activate_actor_wasm` has balanced begin/end spans. The Slice 5b sweep found all remaining WASM scheduler entry points either funnel through `activate_actor_wasm` or do not dispatch handlers. | Slice 5a commit `9078f820` emits `hew_wasm_register_actor_meta` before `hew_actor_spawn` / `hew_actor_spawn_opts`, copies metadata into the bridge side table, preserves the `HewTraceEvent` C ABI, and keeps unknown metadata as `0` / `null`. Opus implementation review and security review both accepted the slice with non-blocking followups: JSON escaping/empty-name guard, doc lifetime wording, ABI naming/hoisting, and msg_type collision ratchet. Slice 5b sweep inspected init/shutdown/cleanup/tick/timer/enqueue/cooperate/metrics and confirmed dispatch spans centralize through `activate_actor_wasm`. | `WASM-R37-S9A` — closed with followups in Slice 5a. `WASM-R37-S9B` — closed-by-sweep; no implementation slice needed before R42. |
| S10 | JIT symbol classification | **Silent-divergence still open.** The catalogue classifies stable/codegen/internal but has no target availability field, so native-only symbols remain mixed with WASM-available ABI. | File header defines only the three-tier model at `scripts/jit-symbol-classification.toml:1-15`; native-only symbols such as `hew_actor_trap`, duplex, supervisor, task-scope, TCP, and timers are present in the same lists at `scripts/jit-symbol-classification.toml:50-65`, `188-198`, `526-622`, `747-758`; `rg -n "wasm_availability"` returns no entries. | `WASM-R37-S10` — Slice 6: add target availability bucket and parser ratchet. |
| S11 | Duration helpers ABI | **Parity (stale row closed).** Duration helpers are now in always-compiled `duration` module, not only native `io_time`; `io_time` re-exports them for native. | `lib.rs:223-247` always compiles `pub mod duration`; `duration.rs:1-35` exports `HewDuration`, `hew_seconds`, and `hew_milliseconds`; native `io_time.rs:16-20` re-exports those helpers; symbol catalogue includes `hew_milliseconds` and `hew_seconds` at `scripts/jit-symbol-classification.toml:314` and `:456`; unit tests cover helper semantics at `duration.rs:37-68`. | `WASM-R37-S11` — closed in Slice 0 evidence. Optional future link-test can ratchet but no ABI implementation slice is needed. |
| S12 | Exit-code / stdout WASI parity | **Local tracker.** Tests exist but remain ignored during cutover; parity is not enforced. | Contract is documented at `eval_e2e.rs:1291-1296`; inline/file runtime-failure exit-code tests are ignored at `eval_e2e.rs:1297-1351`; pre-failure stdout preservation test is ignored at `eval_e2e.rs:1353-1380`; curated WASI examples are also ignored during cutover at `wasi_run_e2e.rs:47-103`. | `WASM-R37-S12` — Slice 8 depends on S5: unignore/adjust WASI exit/stdout tests once trap mapping is complete. |

## Local tracker table

| Local ID | Status | Owned surface | Next action |
|---|---|---|---|
| `WASM-R37-S1` | closed-by-audit | Reply-channel ctx migration | Slice 1 should only ratchet fail-closed tests if dispatched. |
| `WASM-R37-S2` | open | `on(stop)` observability | Slice 2. |
| `WASM-R37-S3` | open-narrowed | Cooperate cancellation propagation | Slice 3. |
| `WASM-R37-S4A` | closed-by-audit | HeapExceeded actor attribution | No implementation slice. |
| `WASM-R37-S4B` | open-via-S5/S12 | HeapExceeded/non-dispatch WASI exit attribution | Fold into S5/S12. |
| `WASM-R37-S5` | open | Trap/panic/exit mapping | Slice 4. |
| `WASM-R37-S6` | fail-closed-ratchet | Supervisor restart | Slice 9 checker ratchet. |
| `WASM-R37-S7` | fail-closed-ratchet | Duplex substrate | Slice 9/S10 ratchets. |
| `WASM-R37-S8` | closed-with-cost | `select {}` cooperative tick | No implementation slice unless regressions appear. |
| `WASM-R37-S9A` | accepted-with-followups | Spawn-path actor trace metadata and `activate_actor_wasm` span balance | Non-blocking followups: JSON escaping/empty-name guard, doc lifetime wording, ABI naming/hoisting, and msg_type collision ratchet. |
| `WASM-R37-S9B` | closed-by-sweep | Remaining WASM scheduler trace-span parity | No Slice 5b implementation needed; R42 may proceed. |
| `WASM-R37-S10` | open | JIT symbol target availability | Slice 6. |
| `WASM-R37-S11` | closed-by-audit | Duration helper ABI | Optional link-test ratchet only. |
| `WASM-R37-S12` | open | WASI eval/stdout/exit parity | Slice 8 after S5. |

## Slice implications

- Slice 1 should not perform the reply-channel migration; it is already present. If retained, Slice 1 is a regression-test ratchet for ctx-less/outer-ctx reply behaviour.
- Slice 4 should treat HeapExceeded actor attribution as prior art and focus on remaining trap/panic/WASI process-exit attribution.
- Slice 7 (duration helper implementation) is obsolete as an implementation slice; if dispatched, convert it to a narrow WASM link-test ratchet or drop it.
- Local tracker IDs replace GitHub issue references for this local-only lane.
