[[case]]
id = "closure_pid_send_controlflow"
intent = "The fixture runs to completion and exits 42."
source = "../vertical-slice/accept/closure_pid_send_controlflow.hew"
suites = ["acceptance"]
timeout_seconds = 30

[case.env]
HEW_WORKERS = "4"

[case.expected]
stdout = ""
exit = 42
