fn main() {
    let a: Result<i64, string> = Ok(6);
    let b: Result<i64, string> = Ok(7);
    let v: Vec<Result<i64, string>> = Vec::new();
    v.push(a);
    v.push(b);
    for e in v {
        match e { Ok(e_v) => println(f"ok {e_v}"), Err(e_e) => println(f"err {e_e}"), }
    }
}
