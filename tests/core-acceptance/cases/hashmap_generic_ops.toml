[[case]]
id = "hashmap_generic_ops"
intent = "Scalar and string HashMap projections compile and run with exact values."
source = "../vertical-slice/accept/hashmap_generic_ops.hew"
suites = ["acceptance"]
timeout_seconds = 30

[case.env]
HEW_WORKERS = "4"

[case.expected]
stdout = ""
exit = 0
