[[case]]
id = "std_net_dns_execution"
intent = "The fixture runs to completion and exits 1."
source = "../vertical-slice/accept/std_net_dns_execution.hew"
suites = ["acceptance"]
timeout_seconds = 30

[case.env]
HEW_WORKERS = "4"

[case.expected]
stdout = ""
stderr = "hew: failure: UserPanic (212): localhost resolution failed\n"
exit = 1
