machine Door {
    state Closed;
    state Open;

    event Toggle;

    on Toggle: Closed -> Open {
        gen { yield Open; }
    }
    on Toggle: Open -> Closed {
        Closed
    }
}

fn main() {}
