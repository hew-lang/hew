[[case]]
id = "resolved-aliases-modules"
intent = "Qualified and selectively renamed imported aliases retain their canonical module identities, including a nested generic alias in a record field."
source = "fixtures/resolved-aliases-modules/main.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 30

[case.expected]
stdout = "MODULE\nALIAS\n42\n"
exit = 0
