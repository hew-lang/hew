[[case]]
id = "scope_move_closure_owned_capture"
intent = "The fixture runs to completion and prints exactly its recorded output."
source = "../vertical-slice/accept/scope_move_closure_owned_capture.hew"
suites = ["acceptance"]
timeout_seconds = 30

[case.env]
HEW_WORKERS = "4"

[case.expected]
stdout = "owned heap\n"
exit = 0
