import ambiguity::actor_left::*;
import ambiguity::actor_right::*;

fn main() {
    let worker = spawn ClashActor();
    match await worker.ping() {
        Ok(value) => println(value),
        Err(_) => println("ask failed"),
    }
}
