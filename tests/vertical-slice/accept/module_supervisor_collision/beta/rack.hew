pub actor Worker {
    let id: i64,
    receive fn identify() -> i64 { id }
}

pub supervisor Rack {
    strategy: one_for_one,
    intensity: 3 within 60s,
    child worker: Worker(id: 2) restart: temporary,
}
