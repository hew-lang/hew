actor Printer {
    receive fn say(msg: String) {
        println(msg);
    }
}

fn main() {
    let a = spawn Printer;
    let b = spawn Printer;
    a.say("a1");
    a.say("a2");
    await close(a);
    b.say("b1");
    b.say("b2");
    await close(b);
    println("all done");
}
