trait Sink {
    type Item;

    fn push(sink: Self, item: Self::Item) -> i32;
}

type IntSink {
}

impl Sink for IntSink {
    type Item = Vec<int>;

    fn push(sink: IntSink, item: Vec<int>) -> i32 {
        0
    }
}
