[[case]]
id = "state-overwrite-record-temp-swap"
intent = "State-field overwrite leak oracles for collection, record, and enum actor state fields, plus alias-shape use-after-free pins. Shape `record_temp_swap` (record temp bridged swap keeps third alias alive) runs to its exact outcome with no leak, double free or use after free under ASan/LSan."
source = "cases/state-overwrite-record-temp-swap.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 60

[case.expected]
stdout = ""
exit = 100
