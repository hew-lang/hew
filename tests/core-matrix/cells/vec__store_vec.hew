fn main() {
    var x: Vec<i64> = Vec.new();
    x.push(10);
    x.push(20);
    var y: Vec<i64> = Vec.new();
    y.push(11);
    y.push(21);
    var v: Vec<Vec<i64>> = Vec.new();
    v.push(x);
    v.push(y);
    let n = v.len();
    println(f"len={n}");
}
