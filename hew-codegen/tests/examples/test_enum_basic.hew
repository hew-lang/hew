enum Colour {
    Red;
    Green;
    Blue;
}

fn colour_to_int(c: Colour) -> int {
    match c {
        Red => 1,
        Green => 2,
        Blue => 3,
        _ => 0,
    }
}

fn main() {
    let r = Red;
    let g = Green;
    let b = Blue;
    println(colour_to_int(r));
    println(colour_to_int(g));
    println(colour_to_int(b));
}
