type Point { x: i64, y: i64 }

fn build(n: i64) -> i64 {
    let pts = [Point { x: n, y: n }; 3];
    pts.len()
}

fn main() -> i64 {
    var total: i64 = 0;
    for i in 0..64 {
        total = total + build(i);
    }
    if total != 192 { return 73; }
    0
}
