[[case]]
id = "partial-captured-job-history"
intent = "A consumed aggregate capture retains readable heap-owning siblings after its once field is called; mutating a vector sibling of another partially consumed job preserves its contents and remaining fields."
source = "cases/partial-captured-job-history.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 15

[case.expected]
stdout = "RUN:CAPTURED:CAPTURED:QUEUED:8\nMUTABLE:3:QUEUED:RUN:MUTABLE:DONE:7\n"
exit = 0
