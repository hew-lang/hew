[[case]]
id = "static_trait_dispatch_inline_supertrait"
intent = "The fixture runs to completion and prints exactly its recorded output."
source = "../vertical-slice/accept/static_trait_dispatch_inline_supertrait.hew"
suites = ["acceptance"]
timeout_seconds = 30

[case.env]
HEW_WORKERS = "4"

[case.expected]
stdout = "inline\n"
exit = 0
