[[case]]
id = "fn_typed_field_call"
intent = "The fixture runs to completion and exits 50."
source = "../vertical-slice/accept/fn_typed_field_call.hew"
suites = ["acceptance"]
timeout_seconds = 30

[case.env]
HEW_WORKERS = "4"

[case.expected]
stdout = ""
exit = 50
