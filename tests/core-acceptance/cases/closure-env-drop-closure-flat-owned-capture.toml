[[case]]
id = "closure-env-drop-closure-flat-owned-capture"
intent = "Closure-environment scope-exit drop oracles: per-iteration leak slope plus poisoned-allocator no-double-free pins for the heap-env closure shapes this lane completes (nested env-of-env, type-parameterised owned capture, and the `BorrowMut`… Shape `closure_flat_owned_capture_high` (closure flat owned capture no per frame leak slope) runs to its exact outcome with no leak, double free or use after free under ASan/LSan."
source = "cases/closure-env-drop-closure-flat-owned-capture.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 60

[case.expected]
stdout = ""
exit = 14
