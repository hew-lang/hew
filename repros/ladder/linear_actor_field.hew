#[linear]
type Tx { id: i64 }
impl Tx {
    fn commit(consuming self) { println(f"commit {self.id}"); }
}
actor Holder {
    var tx: Tx = Tx { id: 1 };
    receive fn demo() {
        println(tx.id);
    }
}
fn main() {
    let h = spawn Holder();
    h.demo();
}
