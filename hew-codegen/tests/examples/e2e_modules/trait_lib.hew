pub trait Describable {
    fn describe(val: Self) -> string;
}

pub type Circle {
    name: string;
}

pub type Square {
    name: string;
}

pub fn new_circle(n: string) -> Circle {
    Circle { name: n }
}

pub fn new_square(n: string) -> Square {
    Square { name: n }
}
