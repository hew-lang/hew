fn main() {
    let x: Option<i64> = Option.Some(5);
    let y = clone x;
    match y { Option.Some(y_v) => println(f"some {y_v}"), None => println("none"), }
    match x { Option.Some(x_v) => println(f"some {x_v}"), None => println("none"), }
}
