fn main() -> i64 {
    var m: HashMap<string, i64> = HashMap.new();
    var i: i64 = 0;
    while i < 50 {
        let k: string = "fixed-overwrite-key-" + "z";
        m.insert(k, i);
        i = i + 1;
    }
    m.len()
}
