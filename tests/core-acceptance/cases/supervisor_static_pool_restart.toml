[[case]]
id = "supervisor_static_pool_restart"
intent = "The fixture runs to completion and exits 7."
source = "../vertical-slice/accept/supervisor_static_pool_restart.hew"
suites = ["acceptance"]
timeout_seconds = 30

[case.env]
HEW_WORKERS = "4"

[case.expected]
stdout = ""
stderr = "hew: failure: UserPanic (212): test crash — supervisor should restart this pool member\nhew: actor crash in Actor (actor 1): UserPanic\n"
exit = 7
