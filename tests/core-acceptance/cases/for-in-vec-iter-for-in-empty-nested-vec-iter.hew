fn main() -> i64 {
    var i: i64 = 0;
    while i < 50 {
        let rows: Vec<Vec<string>> = Vec.new();
        for row in rows.iter() {
            let _ = row.len();
        }
        i = i + 1;
    }
    i % 7
}
