[[case]]
id = "recv-loop-gen-stream-return-carry"
intent = "Per-iteration recv-result drop on the loop back-edge. Shape `gen_stream_return_carry` (gen stream returned loop var no uaf) runs to its exact outcome with no leak, double free or use after free under ASan/LSan."
source = "cases/recv-loop-gen-stream-return-carry.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 60

[case.expected]
stdout = "before\nescaped\nafter\n"
exit = 0
