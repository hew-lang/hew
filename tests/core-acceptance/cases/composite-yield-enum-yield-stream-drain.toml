[[case]]
id = "composite-yield-enum-yield-stream-drain"
intent = "Per-yield leak / double-free oracle for owned-composite stream elements: a `receive gen fn` yielding a record/enum whose fields own heap must free BOTH per-yield copies exactly once — the pump's producer copy (released on the stream-send… Shape `enum_yield_stream_drain_high` (enum yield stream drain leak slope below tolerance) runs to its exact outcome with no leak, double free or use after free under ASan/LSan."
source = "cases/composite-yield-enum-yield-stream-drain.hew"
suites = ["acceptance", "safety"]
timeout_seconds = 60

[case.expected]
stdout = ""
exit = 0
