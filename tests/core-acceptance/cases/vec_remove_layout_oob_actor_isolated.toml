[[case]]
id = "vec_remove_layout_oob_actor_isolated"
intent = "An out-of-bounds `Vec.remove()` on a laid-out element traps inside the actor and the runtime names the actor it fired in."
source = "../vertical-slice/accept/vec_remove_layout_oob_actor_isolated.hew"
suites = ["acceptance"]
timeout_seconds = 30

[case.env]
HEW_WORKERS = "4"

[case.expected]
stdout = ""
stderr = "hew: failure: IndexOutOfBounds (205)\nhew: actor crash in VecRemoveLayoutCrasher.boom (actor 1): IndexOutOfBounds\n"
exit = 1
